# Memivy 开发计划

状态：2026-09-06 阶段 3 首页与记忆库已实现并完成下方工程验证，待用户试用验收；阶段 1 第二版用户验收与阶段 2 数据验证记录保留；Demo v0.1 未改
依据：[PRD.md](PRD.md)、[TECH_STACK.md](TECH_STACK.md) 与 [DESIGN.md](DESIGN.md)，更新日期 2026-09-06

## 1. 计划目标

在已经通过的阶段 1 技术基础上，按新方向推进后续工作：

> **产品文档同步 → 专项交互研究 → 阶段 1 第二版原生交互实验与用户验收（已完成）→ 确认正式产品交互基准 → 正式产品开发。**

后续授权补充（2026-09-06）：用户要求先研究阶段 2，经现有代码、SQLite 官方资料、Mem/Joplin 与公开用户反馈核对后，确认“好，做吧”。据此先完成不依赖新版布局的核心数据层，并采纳“删除整条记忆及专属原话、历史进入回收站”的规则；原生交互试用与阶段 3 UI 的 Demo 确认边界保留。

此前文档同步任务已结束。用户在交互研究后明确授权改造阶段 1 代码，让悬浮助手和新流程可以亲手使用。原阶段 1 结论与未测边界继续保留；新增实验与原验收分开记录，不重做已通过的整套验证。

正式产品交付以下闭环：

> 记一下 → 原话落盘 → AI 整理并回执 → 找回记忆、问一问 → 接着想 → 确认结论存回记忆 → 下次在 Memivy 或其他 Agent 中继续使用。

本计划只交付 macOS。Windows、iOS、Android 暂缓，不提前构建空壳、兼容接口或发布流程；Linux 不支持。

## 2. 开发原则

1. **按阶段授权推进**：阶段 1 第二版已通过用户手工验收；阶段 2 已单独获得授权，只交付核心数据层与验证；阶段 3 已按下方授权采用当前 dev 源码为基准；后续阶段仍按各自授权推进；
2. **三份依据各司其职**：PRD 决定功能；官方 `DESIGN.md` 决定 Miro 视觉语言；通过的 Demo 固定 Memivy 的布局、组件外观与状态表现。Demo 不增加 PRD 之外的功能，`DESIGN.md` 原文保持不变；
3. **通过后不重新设计**：正式 UI 复用或对照 Demo 实现；需要改变已确认的视觉或交互时，先调整 Demo 并重新确认受影响部分，不在功能开发时自行换版；
4. 原话落盘优先于 AI；一份 Rust 业务核心、一套数据库规则，UI 和 MCP 不复制记忆逻辑；
5. Demo 的模拟行为不等于技术验证；阶段 1 已通过只证明记录中的技术范围，不代表问答、讨论或正式记忆功能已经完成；
6. 不提前实现 embedding、同步、任务系统、富文本、插件或额外平台适配。

产品取舍以用户喜欢和实际使用为依据，可以借鉴成熟做法；已有竞品提供同类能力不构成取消该能力的理由。开发计划不增加“必须独有”或额外商业验证审批门槛。

## 3. 建议仓库结构

```text
memivy/
├── design-demo/             可点击视觉基准；阶段 0 创建，纳入版本管理
│   └── index.html           Demo 固定入口，必要样式和脚本放在同目录
├── src/                     React + TypeScript 界面
├── src-tauri/               Tauri host 与 macOS 系统接线
├── crates/
│   ├── memivy-core/         业务规则、数据、AI、检索、会话、导出
│   └── memivy-mcp/          macOS stdio 入口，只做协议适配
├── migrations/              SQLite migration
├── tests/
│   ├── fixtures/            搜索与 AI 合同案例
│   └── e2e/                 关键用户路径
├── PRD.md
├── TECH_STACK.md
├── DESIGN.md                GetDesign 官方 Miro 原文
├── AGENTS.md
└── DEVELOPMENT_PLAN.md
```

以上是目标结构，不代表全部能力已实现。当前已有 `design-demo/` 与阶段 1 的 `src/`、`src-tauri/` 和两个 core/MCP crate；后续按需扩展，不重新初始化。候选素材和临时截图仍放在忽略的 `research/`，长期使用的 Demo 保留在 `design-demo/`。

`crates/` 只保留 core 和 MCP 两个 Rust crate，另由 `src-tauri/` 承担原生 host。core 内部按模块分层，不为每个名词拆 crate，不引入服务仓库。

## 4. 阶段与验收门槛

### 阶段 0：交互研究与可点击 Demo（待后续推进）

目的：研究新方向的使用方式，再让用户看见、点到并确认完整闭环。本次只记录待办，不开展交互定稿或 Demo 修改。

先研究 [PRD.md](PRD.md) 第 12 节的问题，重点是：

- 快捷窗口如何区分记录和提问、保留草稿，并在小窗与主窗口之间继续同一话题；
- 首页、近期话题、会话续接和记忆库的关系，以及空库和少量记录时的首次体验；
- 回答来源、历史变化与新建议的表达；
- 结论核对、修改、保存去向与撤销如何用少量操作完成；
- 未配置、处理中、无证据、取消、失败与重试的完整交互。

研究可以参考成熟产品，先说明采用理由与使用代价，不为追求新奇增加操作。具体方案与页面布局未确认；下表是未来 Demo 需覆盖的能力，不是已经确定的控件设计。

制作方式：

- 依据 `DESIGN.md` 做一套统一的 Miro 风格，不再并列探索其他品牌或另生成设计系统；
- 用轻量 HTML/CSS/JavaScript 与固定模拟数据制作可在本地浏览器运行的 Demo，不先搭正式应用脚手架；
- 主要按钮可点击，页面和状态可切换；不是截图集、配色板或营销落地页；
- 不连接真实模型、SQLite、MCP 或系统快捷键，不收集真实 API Key；模拟行为明确标注。

最少覆盖：

| 界面 | 要看见和试用的内容 |
|---|---|
| 首页与近期话题 | 记录、提问和继续讨论的入口；能从少量已有内容开始使用，不伪造个人洞察 |
| 快捷窗口与回执 | 明确记录/提问意图，主动附带来源；原话保存与整理动作可见、可纠正；小窗与主窗口的上下文衔接 |
| 记忆问答与讨论 | 有来源的回答、继续追问、历史与建议区分；来源查看、取消、失败重试与恢复会话 |
| 结论保存 | 核对和编辑拟保存内容、知道去向、明确确认后更新记忆；回执、纠正与撤销 |
| 记忆库 | 列表与关键词搜索、当前版本编辑、原始输入与来源、历史；创建、删除与取消 |
| 精简设置与菜单栏示意 | BYOM 配置与连接测试状态、MCP 总开关；单篇导出放在记忆页；菜单栏可用/暂停状态 |

用固定场景覆盖首次使用、少量记忆、关键词无结果、问答证据不足、历史变化、普通讨论未保存、确认保存与撤销、AI 等待配置/处理中/取消/失败，以及长中文内容和较窄 Mac 窗口。保持同一套组件，首页与讨论的组织方式由交互研究决定；不加入待办、统计仪表盘或后台定时洞察。

通过条件：

- 用户实际试用“记录 → 回执 → 找回/提问 → 继续讨论 → 确认保存 → 下次继续”，并核对浏览编辑、来源、纠正与撤销，明确表示本版 Demo 通过；仅认可视觉不算交互已确认；
- 首页、快捷窗口、讨论、记忆库、回执和设置的布局及关键状态已确认；约定窗口尺寸下无非预期文字截断、重叠或不可操作区域；
- 保存同一版 Demo 源文件，并在下方记录确认日期、入口、版本及验收窗口尺寸，不另写验收报告；
- 未通过就继续迭代受影响的 Demo；阶段 1 已通过，不需重做。新版 Demo 通过后按用户授权进入后续实施，不代表真实问答、保存或原生能力已完成。

**Demo 确认记录：**Demo v0.1 已制作，入口为 [design-demo/index.html](design-demo/index.html)。2026-09-05 用户反馈：“这个原型看起来还可以，但是有些功能可能和实际功能可能对不上，只是说视觉参考还是可以。”据此记录为：本版可作为视觉参考，功能与关键交互尚未确认；阶段 0 尚未整体验收通过。视觉参考认可日期：2026-09-05；整体验收日期：—；对应提交：—（未提交）；用户验收窗口尺寸：待确认。

后续沿用本版的视觉方向和已选品牌，功能范围与行为以更新后的 `PRD.md` 为准，实现边界以 `TECH_STACK.md` 为准。v0.1 未包含新问答、讨论与结论保存闭环，不把它的模拟按钮和旧布局当作新交互规格。`DESIGN.md` 原文继续保持不变。

**产品方向变更记录（2026-09-06）：**用户确认“记一下、问一问、接着想”、近期话题首页、可浏览编辑的记忆库，以及结论经确认后进入长期记忆。随后要求：“先把相关的文档全部改掉，Demo 先不改……特别是交互上，我们后面还重点研究一下。”本次同步 PRD、技术方案、开发计划和协作规则；Demo v0.1、品牌、应用代码均保留原状。新版 Demo 尚未制作、编号或验收，阶段 2 仍未获授权。下方 v0.1 制作与阶段 1 验证记录均为历史证据，不能当作新方向的实现结果。

**v0.1 制作、研究与验证记录（2026-09-05）：**

- 采用 Miro 白底、黑色胶囊主按钮、黄色品牌与浅色语义块；列表与正文两栏，当前版本在上、原话来源时间线在下，历史按需查看。中文使用现有规范中的系统字体回退；`DESIGN.md` 保持未修改，未创建生产应用脚手架。
- 官方界面与文档研究落实为：[flomo 快捷记录](https://help.flomoapp.com/basic/quick-input.html) 的直接输入、[mymind Quick Note](https://mymind.com/the-new-quick-note) 的留白与轻量展开、[Bear 排版选项](https://bear.app/faq/typography-options/) 的行宽与行距、[Mem 历史版本](https://help.mem.ai/features/version-history) 的明确历史状态、[Capacities 搜索](https://docs.capacities.io/reference/search) 的命中片段层级。结合 Memivy 保留原话、可撤销回执的要求取舍，未照搬热力图、收藏墙、聊天首页或对象管理系统；研究材料与临时证据留在忽略的 `research/`。
- 已在浏览器 1280×720 验证：捕捉关闭与回执、续接后撤销仍保留原话、另存新记忆、删除取消与恢复、编辑形成 v4 与历史 v1 只读、中文多词及特殊字符搜索、AI 失败重试与暂不判断、设置模拟连接测试及 MCP 开关。Node.js 语法检查与 `research/phase-0-regressions.cjs` 的 7 组回归已通过。另已检查 1440×900 主窗口、820×720 长中文与设置、640×720 折叠布局与空库；无页面横向溢出，设置标题与完成按钮保持可见，Enter 保存 / Shift+Enter 换行和返回列表可操作。
- 上述均为 Demo 模拟行为；原生中文输入法、系统快捷键、SQLite、真实模型和 MCP 均未经验证。阶段 0 仍以用户实际试用并明确通过为准。

### 阶段 1：技术风险样机

前置条件：通常要求阶段 0 已获用户明确通过。本轮例外授权记录：2026-09-05，用户在调研与三项决策汇报后确认“好推进做吧，注意只做阶段一哈”。允许沿用 Demo v0.1 视觉先做技术样机；不倒填阶段 0 已通过，不进入阶段 2 或完整业务开发。正式业务 UI 前仍须校正 Demo 与 PRD 的功能交互。

本轮范围：隔离的 `Memivy Phase 1` 应用和测试数据库、最小捕捉与搜索、原生窗口验证、一个最小 `memory_capture` MCP 工具、模型协议探测。暂不实现正式记忆 CRUD/版本/撤销、AI 自动落位、导出备份、开机启动、签名发布。

确认的行为与计时口径：主窗口关闭继续菜单栏常驻；彻底退出后，已开启的 MCP 可保存原话并返回等待整理，AI 消费待处理记录在阶段 5 实现；关闭 MCP 总开关仍拒绝请求。分别测量“快捷键到可输入”和“提交到事务完成”，各自以一秒为门槛，排除用户打字时间与模型处理时间。

目的：验证最可能推翻技术选型的事项，同时检查通过的视觉在 Mac 应用窗口中的可实现性。

工作：

- 初始化 Tauri 2 + React + Rust，在目标 macOS 版本编译和运行空应用；
- 把通过的 Demo 中主窗口和捕捉小窗的最小界面放入 Tauri，验证字体、布局与窗口约束，不重做视觉；
- 从 Rust 创建 SQLite，验证 bundled SQLite、FTS5 trigram、迁移和中文查询；
- 验证菜单栏、全局快捷键、隐藏/唤起小窗；
- 验证中文输入、长文本滚动、窗口焦点和多显示器；
- 用 `rmcp` 做仅含最小 `memory_capture` 的 stdio 程序，写入隔离测试数据库，验证真实 Agent 调用和界面跨入口读取；
- 验证远程和本地 OpenAI-compatible 端点各一次。

退出条件：

- React 界面与 MCP 都能调用同一 Rust core，访问同一个隔离数据库，并发写入不覆盖、不重复已确认的请求；
- macOS 快捷键能可靠唤起并聚焦输入框；
- 中英文搜索样例通过；
- 至少目标 Agent 可调用 stdio MCP；
- 目标 macOS 版本没有不可接受的输入法、焦点或 WebView 问题。

如果 Tauri 的 macOS WebView、窗口焦点或系统能力在这一阶段失败，再重新比较 Electron 或 SwiftUI；不在正式业务开发中同时维护两条技术路线。技术限制若迫使界面改变，先回到 Demo 确认受影响部分，再继续实现。

**阶段 1 验证记录（2026-09-05 至 09-06，用户已验收通过）：**

2026-09-06 用户确认：“不要搞那么复杂，不要过度设计了，我觉得可以通过了。”据此阶段 1 验收通过并结束本轮工作。下列未测项保留为证据边界，不再作为本阶段收尾阻塞，也不记作测试通过；不继续追加验证或进入阶段 2。

| 项目 | 已取得的证据与边界 |
|---|---|
| 范围与隔离 | `Memivy Phase 1.app` 使用独立 identifier 与测试目录；真实原话始终为 pending，无正式记忆版本、删除、撤销或 AI 整理。Demo 原文件与 `DESIGN.md` 未修改。 |
| 真实构建 | macOS 26.6.2 / Apple M4 Pro、Node 24.12.0、Rust 1.98.1；Tauri 2.11.5 + React 19.2.8 编译并打包本地 `.app`，已启动真实 WKWebView。未做签名、公证、分发或其他系统版本验收。 |
| SQLite 与故障 | bundled SQLite 3.53.2 / FTS5 trigram / WAL / FULL；8 项核心测试覆盖原话逐字保留与禁止改写、重试去重、迁移、中英文特殊字符、开关失败关闭、并发写入与模型故障。另以实际进程验证 24 个并发写入、确认保存后 SIGKILL、重开及 integrity_check。 |
| MCP 与跨入口 | rmcp 3.2.0 的 stdio initialize/list/call 与 EOF 退出通过；仅暴露 `memory_capture`，默认关闭、开关即时生效、重复 UUID 不增加记录。真实 Codex CLI 已调用该工具一次，返回 pending 回执，随后在原生 UI 读到同一个 capture ID。MCP 明确要求用户保存意图，但服务端无法独立证明模型是否正确理解同意；客户端仍需按写入工具管理授权。 |
| 搜索样本 | 当前机器 debug 构建、1 万条合成原话、每词 20 次：长中文、两字中文、混合中文、URL 和无结果查询均通过，P95 分别约 17.03 / 15.26 / 20.41 / 1.55 / 0.61 ms。短词扫描全部合格记录后限制返回条数；没有通过截断候选集伪造速度。非长期或真实用户数据基准。 |
| 本地模型 | Ollama 0.32.15 + `qwen3:0.6b`，同一 Rust OpenAI-compatible schema 探测通过，首次含加载约 3.76 秒。仅证明协议路径，未评估 AI 整理质量，不构成默认模型选择。测试服务仅监听 127.0.0.1:11435，模型位于临时目录。 |
| 原生基础 | 已验证长文本滚动与逐字保存、Shift+Enter 换行、Enter 保存、写入失败保留原话及重试只增一条、关闭主窗口常驻、退出重启数据恢复。已修复诊断持锁等待主线程的潜在死锁与主窗口入口返回目标混用，2 项返回目标回归测试通过；用户确认 Esc 返回主窗口正常。 |
| 中文输入与 Esc | 原生逐键输入 `ni` + 空格实际生成“你”；组合态回车不会提前保存，明确提交才从 5 条变为 6 条，Rust 查询核对原话（含输入中已有空格）一致。新面板中再次验证 Esc 先取消候选而不关闭、选词后回车保存“你”且只增一条。Esc 已从 textarea 移到窗口级处理，输入框失焦后仍可退出。仅覆盖当前机器的当前中文输入法，未确认输入法品牌或跨输入法兼容。 |
| 鼠标关闭 | 初期调整窗口焦点未解决偶发双击；随后本机带单调时间戳的事件记录复现 pointerup 先于 pointerdown，与 [WebKit 219670](https://bugs.webkit.org/show_bug.cgi?id=219670) 的 IME/轻点问题相符。收起按钮处理主键释放，并与 click 去重；包含乱序的 5 次原生坐标点击均一次收起，新 NSPanel 上又通过一次。只用于保留草稿的可逆收起操作。 |
| 快捷键与外部全屏 | 用户选择将冲突的 ⌥ Space 改为 ⌃⌥ M，普通 NSWindow 版跨应用实际唤起、直接打字及 Esc 返回连续 3 次正常。外部访达全屏暴露普通 NSWindow 焦点失败，仅加 fullScreenAuxiliary 仍不足；改用固定提交的非激活 NSPanel 后，同一唤起路径在访达全屏取得原生 key-window 与输入框焦点，选词、保存并返回访达通过。该全屏测试使用诊断区 10 秒延迟入口，不是物理快捷键；面板版最终快捷键复测遇到系统锁屏，仍待补齐。当前只检测到 1 块显示器，多屏未验收。 |
| 计时边界 | 最新 NSPanel 的外部全屏单次样本：唤起到 DOM 输入框及原生 key-window 就绪 9.41 ms，Rust 事务提交 1.44 ms，UI 到 Rust 提交确认约 2 ms；来源为访达，入口为诊断验证。历史主窗口按钮样本 44.65 ms；旧 ⌥ Space 冲突样本 2645.83 ms 未通过，不能混入正常热键统计。尚无最终面板版物理快捷键延迟分布，不用上述样本冒充完整性能验收。 |
| 应用图标 | 按用户反馈由选定 SVG 导出铺满画布的不透明底，保留叶子 m 与黄色，由 macOS 26 应用圆角遮罩；已在访达实际验证黄色底贴合系统外框。网页用品牌源文件保持不变。依据 [Apple 图标规范](https://developer.apple.com/design/human-interface-guidelines/app-icons/)。尚未验证旧版 macOS 图标呈现。 |
| 未测项与收尾状态 | 当前单屏，双显示器定位未测；原生收尾遇到锁屏，最终面板版实际快捷键回归未完成，访达仍留在测试时的全屏状态。远程模型私密配置未提供，远程探测未做。用户已知悉这些边界并确认阶段 1 通过，不要求继续补测；阶段 2 未获授权。 |

原始调研、协议回执、进程测试结果和基准输出保存在忽略的 `research/`，运行命令以 `AGENTS.md` 为准。用户密钥不进入仓库、内容库或这些测试报告。

最终检查：最新面板版的前端 TypeScript/Vite 与原生 app 构建、Rust fmt/clippy（warnings 视为失败）、10 项 Rust 测试（8 项核心与模型、2 项窗口返回目标）通过；此前实际进程/stdio 脚本通过，本轮未修改核心或 MCP，未重复该脚本。文档本地链接和 `git diff --check` 通过。临时 Ollama 服务已卸载模型并停止，模型缓存保留于 `/private/tmp/memivy-ollama-models` 供后续复测；样机进程留开，原生界面收尾等待系统解锁。未提交或推送代码。

### 阶段 1 第二版：桌面助手与记忆讨论实验（2026-09-06）

授权：交互研究后，用户要求把现有阶段 1 改成可以亲手体验的形态，并提出桌面常驻悬浮助手；随后明确确认“好，那你就做吧，做出来我看看”。本次直接在原工程上做原生实验，Demo v0.1、品牌与官方 DESIGN.md 不变，原阶段 1 验收记录保留。

已实现的实验范围：

- 桌面叶子 m 常驻入口，点击后同一个 NSPanel 原地展开输入区；回答继续在面板内显示，支持拖动、固定输入、收起与隐藏，保留 ⌃⌥ M；
- 同一输入区明确选择“记一下 / 问一问”；主窗口提供近期话题、会话续接、记忆库和来源原文；展开到主窗口传递话题、输入模式、草稿和所选记忆；
- 原话真实写入 SQLite；模型先生成检索词，再基于有限原话证据回答；引用短编号由核心映射为真实不可改写记录 ID，拒绝越界引用；
- 会话、问题和未确认建议不进入记忆搜索；结论展示拟保存内容和名称，用户可修改后确认“存为一条新记忆”；回执支持撤销，撤销后不再检索或引用该记忆，原确认文本与会话出处仍保留；
- 问答支持处理中、失败、停止、重试和应用退出后的中断状态；原话保存、关键词搜索不依赖模型；设置保留 MCP 总开关；
- v2 不实现正式 memory/version 编辑、AI 自动整理、续写现有记忆、语音、提醒或同步。浏览器只做明确标注的布局预览，写操作须使用 Mac 样机。

**用户验收（2026-09-06）：**模型配置路径与点击/输入焦点问题修复后，用户确认：“好了，上面你给我的检查和测试的点，我都测了，通过了。”据此记录阶段 1 第二版在已给出的检查清单范围内验收通过：原话保存与搜索、桌面小窗与输入操作、配置模型与带来源问答、继续讨论与确认保存/撤销、停止/重新提问与重启保留，以及本轮点击和粘贴回归。以下自动化记录与当时未测项保留为历史证据，不将用户的整体通过反馈扩写成每一种输入法、跨屏或全屏组合的逐项测试报告；不再要求重做已通过的样机验收。阶段 2 的正式数据层仍未接入界面，此确认不表示正式 UI 或后续阶段已启动。

运行与验证（用户验收前的过程记录）：

| 项目 | 证据与边界 |
| --- | --- |
| 构建与自动化 | `npm run build`、`cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings`、`cargo test --workspace --offline` 通过；共 16 项 Rust 测试。新增会话隔离、确认文本与去重、撤销后原话保留、取消/中断后的迟到回答、越界引用、旧数据库迁移覆盖。已重新打包 debug `.app`；新数据库结构的 MCP/跨进程兼容检查使用现有 `verify_phase1.py`。 |
| 真实模型 | 在合成数据上验证检索、两轮讨论、确认新记录和撤销；第一问不预选来源，实际通过检索找到原话。0.6B 模型仅能证明协议，回答质量弱；本次以本机 qwen3:4b 验证交互链路，不作为正式默认模型或质量验收。 |
| 本机运行条件 | Ollama 服务监听 `127.0.0.1:11435`，模型在 `/private/tmp/memivy-ollama-models/`。默认应用模型配置位于 `~/Library/Application Support/com.memivy.phase1/model.json`，权限 0600、无 API Key；已存在的配置不覆盖。服务需要保持运行，样机不自动安装或启动模型服务。 |
| 数据隔离 | 单独模型烟测使用临时数据库；临时联调配置在 `/private/tmp/memivy-v2-runtime/`。正常双击 app 使用既有独立 Phase 1 默认目录；未迁移或改写旧 `research/runtime/` 测试数据。 |
| 界面检查 | 浏览器已检查首页和记忆库。解锁后已打开真实 `tauri://localhost` 样机，验证入口展开、收起按钮和 Esc；原生面板可呈现输入区。用户反馈图标难以拖动，已将整枚图标与面板标题区接入拖动，禁用图片默认拖拽，以 5 px 阈值区分点击与移动，使用系统物理坐标移动 NSPanel。自动化对非激活面板的拖拽及键盘定位不可靠（曾操作到主窗口），因此拖动、实际键入、中文选词、置顶、全屏和焦点返回仍需手动验收，不能以代码或构建成功代替。 |
| 草稿衔接 | 修正浮窗收起后主窗口仍持有旧草稿的问题；通过原生无障碍接口填入合成中文草稿，验证收起后主窗口收到同一内容、再次展开仍保留，测试后已清空。此验证不代表物理键盘或中文输入法测试通过。 |
| 模型配置路径修复 | 2026-09-06 用户在 dev 中反馈保存配置被“仓库以外”限制阻断。核对发现用户主目录存在 `.git`，旧检查错误覆盖标准应用数据目录。现仅对标准 Phase 1 应用目录豁免 HOME 层级的 `.git`；先解析路径，仍拒绝实际项目、worktree、指向项目的软链接及相对/父级路径。新增 4 项回归覆盖首次保存、读回和 0600 权限；全部 6 项 host 测试、Clippy 与原生程序编译通过。使用临时合成配置，未改写用户配置，未代测用户端点连接。 |
| 点击与输入焦点修复 | 2026-09-06 用户反馈按钮难以点击、「接着想」无法获取光标或粘贴。线程采样未发现主线程死锁；隔离原生副本中观察到窗口/页面切换后焦点落到 HTML 容器。移除直接调用 `resignKeyWindow`（Apple 明确将其定义为系统通知回调），使用 `orderOut` 释放小窗焦点后再激活主窗口；收起图标不再成为 key window；主窗口接受首次点击。React 在页面提交后先恢复 WebKit first responder、再聚焦输入框，设置与来源面板打开期间不抢焦点。原生 dev 副本实测通过：保存合成记录、记忆库导航、记忆卡片「接着想」后点击输入并键入/粘贴中文、小窗展开回主窗口、Esc 收起后立即继续键入、打开/关闭设置；未调用模型，临时库与用户数据分离。前端构建、6 项 host 测试、Clippy 通过。自动化直接中文键入与部分剪贴板检测不可靠，中文粘贴以实际输入值验证；中文输入法选词、跨屏/其他应用全屏焦点本轮未重测，不作为已通过。 |
| 当前状态 | 模型配置与焦点修复后的样机检查清单已由用户手工测试并确认通过；阶段 1 第二版验收完成。原生 dev 检查、前端构建、Clippy 与 6 项 host 单元测试的实际结果见上方记录。 |

再次启动本地测试模型可运行：`OLLAMA_HOST=127.0.0.1:11435 OLLAMA_MODELS=/private/tmp/memivy-ollama-models OLLAMA_CONTEXT_LENGTH=8192 OLLAMA_NO_CLOUD=true ollama serve`。模型质量或速度不合适时，在“模型与设置”连接自己的端点。

### 阶段 2：核心数据层

目的：建立可靠的本地记忆与会话数据层，先落实长期记忆与普通讨论的边界。

状态：2026-09-06 已完成核心接口、迁移与数据验证；未接入正式 UI 或 MCP。阶段 1 第二版交互的用户验收单独记录于上一节。

工作：

- 定义 capture、memory、memory_version、版本来源、本地会话/消息/引用与独立配置边界；
- 完成 migration、事务、WAL、busy timeout 和异常恢复；
- 实现原话创建、记忆 CRUD、用户编辑生成新版本、删除与撤销；
- 实现会话持久化与继续读取，但不自动将消息写为 capture 或纳入记忆索引；
- 实现确认结论保存的基础事务、来源关联和重试去重；来源指向稳定版本，删除会话不连带删除已保存结论；
- 实现 Markdown 导出和一致性数据库备份；
- 建立统一错误类型和脱敏日志。

退出条件：

- 强制终止 AI 或应用都不会丢失已提交原话；
- AI 代码没有修改原话的接口；
- 每个当前版本都能追溯到历史版本和来源；
- 普通讨论不进入记忆搜索或 MCP；确认后保存的内容与原消息角色、来源可核对；
- 回答来源被编辑或删除时，不错误指向新版本或伪造可用原文；
- 新旧数据库迁移测试、备份恢复测试通过。

**本轮实现与实测记录（2026-09-06）：**

| 范围 | 实际结果 |
|---|---|
| 正式数据模型 | `memivy_core::memory::MemoryStore` 保存原话、当前记忆、不可覆盖版本、版本来源、回执及每条记忆的变更、会话/消息/引用和确认保存意图。默认正式目录为 `~/Library/Application Support/com.memivy.app/`；测试全部使用临时合成数据，未迁移样机或创建用户正式库。 |
| 写入与撤销 | 原话先独立提交；版本、来源、回执同事务保存。UUID + 请求摘要保证重试一致性；版本校验拒绝过期写入。恢复/撤销追加完整版本，纠正归属的撤销同时恢复两边，任一边已被编辑则整体拒绝，不留下半次变更。 |
| 删除与来源 | 整条记忆和专属原话进回收站，普通搜索与来源解析不可用；共享来源保留，单独删除原话不随记忆恢复。显式清空擦除正文和全文索引项，来源 ID 保留不可用状态；撤销整理保留原话。 |
| 会话边界 | 问题、建议、草稿不入记忆索引。确认文字、名称、目标和原消息角色独立保留；目标失效返回 `needs_review`，不换目标。删除会话后，确认保存和相同请求重试仍正确。引用固定版本；取消/中断/来源失效阻止迟到回答保存为完成。 |
| 搜索基础 | 一个 FTS5 trigram 索引随事实表写入/擦除维护；短词使用字面匹配。中文、英文、特殊字符、标题与原话组合、旧版本和回收站排除测试通过。排序权重、来源/时间/项目过滤和索引重建入口仍在阶段 3。 |
| 迁移与恢复 | 正式 v1 → v2 保留已有原话，DDL 中途失败完整回滚；拒绝 Phase 1、无关和较新数据库。SQLite backup API 生成一致性备份，恢复到新空目录；版本、回收站、会话和重试记录可恢复。Markdown 同一读快照导出完整有效历史与来源，会话单独标识；备份/导出均不包含独立配置密钥。 |
| 自动检查 | `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings`、`cargo test --workspace --offline` 通过。共 35 项测试：19 项新数据层测试、原有 14 项 core 测试及 2 项 host 测试；`npm run build` 通过。 |
| 真实进程 | `cargo build -p memivy-core --example memory_probe --offline` 后运行 `python3 scripts/verify_phase2.py` 通过：24 个独立进程保存；8 次同请求并发重试只产生一条；原话及版本/回执提交后实际 SIGKILL 仍恢复；另一个进程持续写入期间备份一致；最终 47 条合成原话、1 个版本，SQLite 3.53.2，integrity 与 foreign-key 检查通过。 |
| 范围边界 | 新接口不调用模型。未切换现有 UI/MCP、未修改 Demo、品牌或 DESIGN.md；未做本轮原生交互、真实模型质量或物理断电测试，未生成新原生包、提交或推送。正式 UI 与 MCP 接入、完整检索排序及 AI 整理由后续阶段完成。 |

**同日 Review 修复与提交前验证：**

- 修复永久删除的隐藏副本：删除原话时同步擦除关联的已撤销版本及 FTS 正文，支持显式清空已撤销记忆；备份正文检查、失败回滚、共享内容保留及重复清空通过。
- 修复撤销纠错后的操作中断：新回执指向恢复后的归属和新版本；新建/已有来源与目标的四种组合均连续纠错、撤销三轮，重试去重和后续编辑保护通过。
- 修复并发首次打开误报 `Schema`：初始化元数据在同一读快照读取，写锁内再次核对。100 轮、每轮 4 个连接的回归测试通过；真实进程验证新增 8 个空库、每库 4 个进程同时打开，正常锁竞争可重试，未再误报格式错误。
- 提交前 `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings`、`cargo test --workspace --offline`（44 项：24 项正式数据层、14 项原有 core、6 项 host）、`npm run build` 通过；`python3 scripts/verify_phase2.py` 的首次打开、并发写入、去重、SIGKILL 和在线备份验证，以及 `python3 scripts/verify_phase1.py` 的原有进程/stdio MCP 验证均通过。测试仅使用临时合成数据；本次数据修复未改变已验收的原生交互。

### 阶段 3：按新版 Demo 实现首页与记忆库基础界面

目的：把确认的主窗口基准接到真实本地数据，让没有模型的用户也能完整记录和查找。

**本轮授权与基准（2026-09-06）：**用户先要求研究现有 dev、行业实践和竞品，并明确不另写方案文件；随后选择“采用紧凑列表＋宽正文”，澄清“是我们 dev 环境的代码，我可能没有 build 吧”，最后确认“好，实现吧”。据此沿用当前 dev 源码的首页、Miro 视觉、品牌与基本组件，仅将记忆库调整为确认的布局并接入正式 MemoryStore。此为明确的阶段 3 开发授权，不倒填旧 HTML Demo 整体验收通过，也不增加一次 Demo 审批。首轮未包括阶段 4 快捷入口、阶段 5 正式 AI 闭环和阶段 6 MCP 接入；随后用户要求恢复已有问答与讨论，范围修正见下文。

工作：

- 按上述当前 dev 基准实现首页的本地近期话题、记录入口、记忆库、关键词搜索、当前版本编辑、来源、历史与设置，复用组件；
- 以确认过的布局、窗口尺寸、空状态和无结果状态为基准，接入真实本地读写；依据后续用户纠正，恢复已可用的问答与讨论体验，直接接到正式数据与配置的模型，不运行模拟回答；
- 完成 FTS5 trigram 索引、标题权重、来源/时间/项目过滤；
- 小于三个字符的查询采用受限普通匹配；
- 对索引提供删除后重建能力。

退出条件：

- 未配置模型时，创建、编辑、历史、搜索、导出全部可用；
- 中文短语、英文词和 URL 搜索案例通过；
- 关键词搜索不产生任何模型请求；
- 索引损坏或删除后能从事实表重建；
- 在当前 dev 的 1140×790 主窗口基准下实测，并核对 760×560 最小窗口的列表/正文切换；使用相同品牌与组件，不自行改变首页布局。


**本轮实现与实测记录（2026-09-06）：**

| 项目 | 实际结果与边界 |
|---|---|
| 正式入口与隔离 | 默认 `npm run dev:app` / `Memivy.app` 使用正式 `MemoryStore` 与 `com.memivy.app/memivy.db`；旧样机通过 `npm run dev:prototype` 保留，MCP 仍用 Phase 1 Store，无自动迁移。浏览器仅显示标明身份的固定布局示例，写入返回不可用提示。 |
| 首页与记忆库 | 沿用当前 dev 的首页与最近记忆卡片；正式库为空时显示真实空状态。记忆库采用紧凑分页列表＋宽正文，包含来源、项目、更新时间过滤、命中原文片段、当前内容、原话与来源、版本历史及回收站。已有正式话题可分页只读；未伪造话题、AI 回答或整理回执。 |
| 版本与草稿 | 原话保存后返回确认；手工编辑创建版本并显示可撤销回执。旧版本恢复会新建版本，陈旧编辑拒绝覆盖并保留草稿；首次版本撤销返回原话。草稿写入独立表，跨导航排队、重试，退出前等待落盘；永久擦除也清理对应草稿，并拒绝迟到写入。 |
| 搜索与数据检查 | 新增 `memory_library` 8 项回归全部通过：标题排序、限量前过滤、分页、中文短词/URL/字面特殊字符、真实来源片段、版本并发保护、草稿不进入搜索/MCP/导出、删除状态隔离、缺失索引重建。最后的独立删除来源边界修正后重跑 8 项通过。 |
| 全部工程检查 | `npm run build`、`cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings` 通过。`cargo test --workspace --offline` 共 52 项通过（含正式数据 24 项、库查询 8 项、样机/模型/host 相关检查）。过程中修复了双 `generate_context!` 导致 dev/test 重复嵌入 plist 的问题，正式与样机现共用一次 context 生成。 |
| 真实进程检查 | 构建 core 两个 probe 与 MCP 后，`verify_phase1.py`、`verify_phase2.py` 均通过：跨进程并发/幂等、确认落盘后 SIGKILL、版本/回执保留、在线备份、SQLite 完整性和 MCP stdio。使用临时合成数据，无真实模型请求。 |
| 原生与视觉 | 已构建并启动未签名 debug `target/debug/bundle/macos/Memivy.app`。在显式临时正式目录中实际完成中文原话保存、手工编辑、原话逐字核对、来源关键词找回、版本历史展示、首次编辑撤销、⌘N/⌘K/⌘S、退出重启草稿恢复、原生目录选择导出。1140×790 原生布局及 760×560 浏览器列表/正文/返回通过视觉核对；编辑框在草稿就绪后获取焦点。 |
| 导出与设置 | 原生导出产生 memories/captures/conversations Markdown 与 manifest，逐项核对包含保存的正文及原话，未提交草稿不在导出中。设置提供索引重建与独立模型配置/固定文字连接测试；配置损坏不阻断记录和搜索，重新填写可显式替换不可读配置。未代测用户模型端点或改写用户凭据。 |
| 证据边界 | 原生中文粘贴以实际输入值核对；部分自动化剪贴板调用报超时但文本已输入，不按工具返回单独判定。未逐项实测物理中文输入法候选词、全屏/多屏或大规模长时性能；回收站恢复、永久擦除、陈旧版本与索引重建的核心行为由回归测试覆盖，未将它们全部标记为原生点击实测。原型此前验收保持，不重新追加原型体验验收。 |

**导出交互修正（2026-09-06）：**用户明确要求“就导出一篇……想导出的时候就导一下”。据此移除设置里的整库导出与对应原生命令，改为每篇记忆折叠“更多”菜单中的“导出 Markdown”，保存为单个 Markdown 文件，仅包含该篇已保存的标题与当前正文；未整理原话也可单篇导出。正在编辑时需先保存，再导出。上表整库导出是首轮历史实测，不再代表当前 UI。单篇导出新增 2 项回归，连同原记忆库 8 项共 10 项通过，覆盖仅一篇正文、原话保真、原子替换、旧版本与回收站拒绝导出；原生独立测试副本已验证“更多”展开、原生保存窗口与中文文件名；实际文件逐字等于该篇已保存标题和当前正文，权限为 0600，未包含其他文章、原话历史或讨论；设置页已无导出入口。

**已有讨论功能回退修正（2026-09-06）：**用户指出“问一问”被禁用、记忆里的直接讨论入口消失，明确要求保留此前可用的体验。原先将正式问答全面推迟的边界在此范围内被用户修正：恢复首页提问、从记忆“接着想”、近期话题续接、可打开的版本引用、取消/重试、经确认保存新记忆及撤销；接到正式 MemoryStore，不把旧样机数据库混入正式搜索。导出作为附属操作收进“更多”，主操作仍是编辑与接着想。正式自动整理、桌面助手与 MCP 接入仍未扩入。新增 3 项讨论回归通过（固定本地协议测试端点）：版本依据、取消/删除/非法引用拒绝迟到完成、讨论不自动写记忆、明确确认后保存、草稿与选定依据重启保留、近期与更早消息分页。在独立原生测试副本和临时正式数据中，实际完成首页问一问、从记忆接着想、两轮回复、打开版本依据、停止、恢复问题后重试、审核并修改结论后确认保存、回执与撤销。提问不增加原话；确认保存才新增，撤销后保留确认原话。前端构建、Rust fmt/Clippy 和全工作区 57 项测试通过，正常 Memivy debug 应用包已重新构建。原生协议验证使用固定本地测试端点，未验证用户模型的实际回答质量；中文粘贴与输入操作通过，未逐一测试中文输入法组合状态。自动审批因可能丢失未保存状态拒绝退出用户当前应用，因此保留其窗口，另建独立 bundle identifier 完成测试；当前已打开的旧窗口尚未重启，不将新构建等同于该窗口已更新。

本轮未提交或推送，未新增方案文件。以上是工程实测结果，尚未记录用户对阶段 3 的最终验收；阶段 4/5/6 不因此获得授权。

**弹窗与页面标题修正（2026-09-06）：**根据用户反馈，正式共用弹窗补齐遮罩点击与显式 Esc 关闭；嵌套弹窗停止事件冒泡，只关闭最上层，正文内留白与从内部开始的拖选不触发遮罩关闭，保留已有保存中保护。去掉重复的顶部“我的记忆 / …”整栏，沿用各页面自身标题；模型配置状态合并到左下角设置入口。参考 [W3C 弹窗交互](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/) 和[面包屑适用范围](https://www.w3.org/WAI/ARIA/apg/patterns/breadcrumb/)。浏览器实际验证输入框内 Esc、嵌套 Esc、焦点恢复和 760×560 首页/记忆正文；独立原生测试副本实际验证设置遮罩/Esc、内部留白不关闭、嵌套确认框遮罩/Esc 仅关闭顶层、引用遮罩关闭、结论正文输入框 Esc 关闭，1140×790 讨论布局已目视核对。测试未提交设置、索引或结论写入；未逐一测试中文输入法组合态和拖选手势。前端构建与原生 debug 构建通过，本轮不涉及 Rust 数据行为变更。

**讨论排版与首次点击修正（2026-09-07）：**移除讨论消息复用旧卡片样式造成的正文贴边，统一标题、消息与输入框的左右留白，区分提问和回答，并调整段落、引用、输入区间距及窄窗口样式。在隔离原生应用中复现输入框聚焦后首次点击无效，临时事件记录确认收到 `pointerup → mouseup → pointerdown → mousedown` 而没有 `click`，与 [WebKit 219670](https://bugs.webkit.org/show_bug.cgi?id=219670) 的输入法相关事件乱序一致。正式界面共用层仅对聚焦编辑器后、同一按钮/菜单/链接的短时同位置逆序手势恢复一次点击；普通点击、键盘、拖动、修饰键和失效控件保持原行为，并防止迟到原生 click 重复执行。新增 `npm run test:ui`，12 项回归通过，覆盖恢复、普通/迟到点击、连续手势、键盘、拖离、失效控件和窗口失焦。原生实测首页“记一下/问一问”、侧栏话题切换、记忆库搜索结果、更多菜单、设置关闭均可一次点击生效；设置复选框原有单次点击正常，未提交设置；未发送的讨论草稿切换后仍保留。常规窗口与接近最小尺寸的讨论排版已目视检查，最后一轮滚动复查因 Mac 锁屏中断；未逐一覆盖物理输入法组合态。临时诊断代码已从正式入口移除，`npm run build`、正常 `Memivy.app` debug 构建与 `git diff --check` 通过。测试仅用隔离合成数据，本轮未更改 Rust 数据逻辑。

**审查问题修复（2026-09-07）：**修复四处跨页面问题：撤销回执绑定目标记忆；草稿按记录串行写入并同步当前页面，发送完成只清空匹配请求的草稿，迟到回调不能覆盖新输入；单条提交只等待自己的草稿，退出时仍检查全部未保存内容；加载历史以当前可见消息为锚点保持位置，仅新消息/回答状态变化触发底部滚动。新增 12 项队列与受控组件生命周期/IPC 回归，连同原有点击测试共 24 项通过，覆盖新草稿在迟到回调前后及清空 IPC 期间的保护、失败重试、跨记录撤销和历史定位。隔离原生副本实际完成 A 编辑→切 B 无误带回执→回 A 撤销，退出后核对 A 恢复、B 未变；无效标题草稿存在时首页捕捉成功，返回仍保留失败草稿，修正后可正常落盘退出；讨论草稿跨页面及退出保留。25 轮讨论加载第 1–5 轮后，第 6 轮保持原屏幕位置；常规与窄窗口已目视核对。原生测试使用临时合成数据，无模型请求；迟到请求的精确时序由受控回归覆盖。前端构建、正常 Memivy debug 应用包构建和 `git diff --check` 通过。本次不涉及 Rust 数据逻辑变更。

### 阶段 4：macOS 快捷入口与捕捉

目的：沿用已验收的原生基础和当前 dev 视觉，将快捷入口接入正式 MemoryStore。

**授权与交互确认（2026-09-07）：**用户要求先研究行业实践与竞品、汇报关键决策，随后表示“好，同意。你可以开始做了吗？”，授权本阶段实现。沿用当前源码的 Miro 视觉与品牌，不重做旧 Demo。确认 ⌘Enter 提交、Enter 换行；原话保存后普通窗口收起，固定窗口保留并清空；收起时回答继续生成，不自动弹回。借鉴 [Drafts 捕捉窗口](https://docs.getdrafts.com/docs/extensions/capture-window) 的低打扰捕捉、[Raycast Quick AI](https://manual.raycast.com/ai/quick-ai) 的对话展开，以及 [Apple 菜单栏规范](https://developer.apple.com/design/human-interface-guidelines/the-menu-bar)；避开 [VoiceOver 的 ⌃⌥M](https://support.apple.com/en-gb/guide/voiceover/mchlp2748/mac)。具体规则已更新至 PRD 5.1。

已实现：

- 正式 NSPanel、叶子 m、单色菜单栏图标、可配置全局快捷键（默认 ⌃⌘M）、固定/拖动/隐藏/暂停，以及读取真实系统状态的可选登录启动；
- 正式 capture/question 独立草稿，逐字原话和可移除应用来源，用户主动附带链接/路径；保存失败保留、请求幂等、跨窗口比较后写入与冲突核对；
- 小窗复用正式问答、引用、取消/重试、继续讨论和确认结论保存；后台任务归 host 管理，收起不取消；
- 小窗到主窗口交接同一话题、引用及草稿，接收窗口准备完成才收起；过期的收起/交接事件不能关闭后来打开的面板；
- 主窗口关闭前保存草稿再隐藏，退出时等待所有窗口确认；通过 [AppKit applicationShouldTerminate](https://developer.apple.com/documentation/appkit/nsapplicationdelegate/applicationshouldterminate(_:)) 接住系统退出并延后答复，失败或未完成弹层会保留窗口，未响应不会强制退出；
- 工作区边界与断开显示器后的位置约束；菜单/窗口操作统一主线程，借用既有 NSPanel 和依赖，无新后台服务。

本阶段继续排除自动 AI 整理和正式 MCP；样机、旧 Demo、官方 DESIGN.md 和品牌原件保持原样。原话保存后显示本地成功反馈，不将其伪称为 AI 整理回执。

实际验证（2026-09-07）：

- 前端构建、29 项 UI/草稿测试、62 项 Rust 工作区测试、fmt 和 clippy（warnings denied）通过；新增跨窗口草稿、来源精确保留、重复提交、叶子辅助功能激活、拖动与按键/IME 防误交测试。文档本地链接与 diff 空白检查通过。
- unsigned debug `Memivy.app` 已实际构建并运行；测试库 `/private/tmp/memivy-phase4-qa/`，受控本机模型桩，仅发送合成测试文本。先发现默认实例启动参数未传递，改用 macOS `open --env` 并核验空库后才进行写入；没有在真实资料库中创建测试记录。
- 原生实测：中文多行原话、Enter 换行与 ⌘Enter 提交；普通保存收起、固定保存保留且输入恢复；退出后草稿恢复；显式来源链接随草稿交接；当前回答、引用及续问草稿一起展开主窗口。引用弹层内容与实际原话一致，Esc 只关闭弹层。
- 受控模型延迟 12 秒：发送后收起，回答完成且不自动弹窗；再次打开仍在原话题，数据库没有新增长期记忆。
- 用户实际按 ⌃⌘M 后反馈“能正常打开”，完成物理全局热键核验；自动化按键没有触发系统热键，未拿自动化结果替代物理验证。
- 原生入口请求到可输入的两个样本为 307ms、277ms；一次从提交动作（含草稿 flush）到原话事务完成 4ms。入口样本来自主窗口按钮，不是物理热键的独立耗时测量；只属于本机本轮样本，非长期性能分位数。
- 最终构建复验：关闭主窗口后仍有叶子入口；叶子原生点击能恢复同一话题，取消固定后的展开也保留续问草稿。暂停/隐藏及恢复操作实际写入设置并反映在界面。修正保存后重新获得输入焦点、样式加载顺序、叶子辅助功能点击，以及交接过程中失焦的竞争。
- 退出复验发现标准 ⌘Q 会绕过 Tauri 的 ExitRequested，已增加 AppKit delegate 的延后退出答复。最终原生实测：结论确认弹层打开时 ⌘Q 不退出，Esc 关闭后可见取消退出提示；输入新的续问草稿后立即 ⌘Q 能退出，随后从 SQLite 核对全文已保存，长期原话仍为 3 条。新 delegate 保留 Tao 原有方法与布局，clippy 和应用包构建通过。测试模型桩已停止，当前打开的新版已回到正式资料库，未写入测试内容。
- 登录项查询实际返回应用包不可用；当前构建不在应用程序目录，未开启登录项或执行真实重新登录。多显示器/显示器拔出、外部全屏应用、物理中文输入法组词，以及菜单栏图标的完整点击流程还未作为真机通过项记录。

尚未记录用户对阶段 4 整体的最终验收；物理热键反馈仅确认该单项，不扩展为整体验收或后续阶段授权。

**开关卡顿修复（2026-09-07）：**用户反馈快捷键唤醒和点叉收起很卡，授权定位并修复。原生分段计时发现 `snapshot` 每次同步查询 `SMAppService.status`，单次 122–131ms；一次打开、收起各重复查询 3 次，全局热键分支还额外构造快照。已从窗口状态中拆出登录项查询，仅设置页打开/回到前台时在工作线程按需读取；原生开关分支改为直接读取所需窗口字段，设置页拒绝迟到状态覆盖新操作。收起前仍等待草稿落盘。

- 同一隔离库、同一已有话题，修复前 2 次打开到原生输入就绪为 431/417ms，点叉处理（含草稿 flush）389/387ms；修复后 4 次打开为 69/22/23/29ms，点叉为 8/19/12/14ms。窗口状态快照从约 125ms 降至 0–2ms；开关过程中不再查询登录项，进入设置后单独查询 127ms 并返回真实状态。
- 上述打开样本通过主窗口快捷入口按钮调用同一原生打开函数，未当作物理热键端到端耗时；收起数据截止到原生处理完成，不包含下一帧显示扫描。样本为本机 debug 包，不代表长期性能分位数。
- 原生复验通过：连续开关、输入后立即点叉再打开，讨论草稿全文和焦点恢复；记一下模式保留多行原话及显式来源 URL；原有话题、回答仍在。数据库核对长期原话仍为 3 条，未产生测试记忆写入正式资料库。
- 本轮 31 项前端测试、3 项桌面 Rust 测试、fmt、clippy（warnings denied）和原生应用包构建通过。新增慢登录项查询不阻塞其余设置、迟到响应不能覆盖登录项操作、窗口状态刷新不触发登录项查询的回归覆盖。已在正式资料库打开修复版，请用户复核物理热键与点叉手感；尚未把待反馈项记为通过。

**阶段 4 审查修复（2026-09-07）：**修复连续空白交接到主窗口后沿用旧来源的问题：空白默认值随当前来源更新，已经开始编辑、恢复或正在写入的草稿仍保留自身来源和显式 URI，成功保存后下一次空白输入采用当前来源。窗口布局改为先计算完整目标矩形，再提交缩放与移动；不再在 Tao 异步缩放后立即读取旧尺寸。展开、收起、回执与默认叶子均复用目标尺寸计算，保留工作区边界与锚点规则。

- 新增 3 项来源生命周期和 2 项窗口几何回归，34 项前端测试、5 项桌面 Rust 测试、类型检查、fmt、clippy（warnings denied）与 debug 原生应用包构建通过。几何测试覆盖旧叶子/回执/输入/讨论尺寸、回执消失后锚点恢复、不同缩放比例和负屏幕坐标；不将计算测试视为跨屏真机验证。
- 原生测试使用全新隔离库 `/private/tmp/memivy-review-fixes-qa/`。连续空白交接后输入并保存两条合成记录，SQLite 核对来源依次为 Google Chrome、Memivy，草稿已清空；原生展开、交接与收起后的 72×76 叶子已目视核对。未调用模型，未向正式资料库写入测试内容；测试结束后已重新打开使用正式资料库的修复版。跨显示器拖动/拔出仍未实测，未扩大阶段整体验收结论。

### 阶段 5：AI 整理、记忆问答、继续讨论与结论保存

目的：完成用户在 Memivy 内“记一下、问一问、接着想、确认保存”的完整闭环。

**调研后决策确认（2026-09-07）：**用户同意前两项建议，并明确系统仍在开发阶段，无需考虑旧数据：

- 自动续接采用补充和局部更新，保留未涉及内容，不自动做整篇压缩重写；明确改变的判断可以更新，犹豫和质疑保留不确定性。
- 结论存入已有记忆时默认补充；需要融合成新的当前理解时，先展示融合结果，由用户一次确认后保存。
- 不安排旧数据兼容、历史记录批量整理或升级迁移交互。

**实现授权（2026-09-07）：**完成剩余问题核对后，用户要求开始写代码，阶段 5 开始实现。沿用当前正式 UI、布局和共用组件，以此前已接通的问答、讨论与新建结论保存为基础；正式 MCP 仍属于阶段 6。实际验证结果在完成后记录，不将授权视为验收。

先接入捕捉后的整理：

- BYOM 设置、连接测试、Key 本机文件保存与日志脱敏；
- 捕捉提交后创建持久 pending 状态；
- FTS/当前项目/最近记忆生成有限候选；
- 一次模型调用返回动作、目标、新建正文或续接的补充/局部修改、标题和检索词；核心基于所用版本保留未涉及内容并生成新版本；
- 严格结构校验后在一个事务中应用动作；
- 实现回执、撤销、改为新记忆和失败后重试；
- 应用启动时恢复被中断的 pending/processing；
- 用真实 AI 状态驱动 Demo 已确认的回执、纠正与错误界面。

再接入问答、讨论与结论保存：

- 同一 BYOM 配置下，结合问题与有限会话上下文生成检索词，由共享 core 检索有限记忆并生成带来源的回答；
- 区分回忆与新建议，引用绑定实际使用的版本/原始输入，处理无证据、旧判断变化与来源不可用；
- 接入首页、快捷窗口和单条记忆的提问与继续讨论，支持本地会话续接；
- 实现处理状态、取消、超时、失败重试、中断恢复与迟到结果隔离；
- 按用户确认的内容和去向保存结论，复用原话落盘、版本、回执、纠正与撤销，防止普通对话自动进入长期记忆；
- 补齐已有记忆作为结论保存目标：默认补充原正文；融合需先展示结果再确认，准确保存确认文本与最终版本正文；
- 结合固定案例核对召回与回答质量，记录 FTS 的真实漏搜，不预先增加 embedding；
- 所有界面与操作按新版已确认 Demo 对照，移除模拟路径。

退出条件：

- 三种动作均有可见回执和确定的数据结果；
- 自动续接保留未涉及正文和原意，不将犹豫或质疑改成已确认事实；已有记忆的结论补充不覆盖旧正文，融合后的版本与用户确认结果一致；
- 非法输出、超时、限流、端点离线时只改变处理状态，不损伤原话；
- 撤销能恢复 AI 动作前的当前版本；
- 相同 job 不会因为重启被应用两次；
- 关键词搜索仍无模型请求；问答只读取有限相关内容，回忆性陈述与引用相符，证据不足时明确说明；
- 多轮追问可接续上下文，取消或失败不制造成功回答、不写记忆；
- 结论明确确认前不创建 capture，确认保存可追溯、去重、纠正和撤销；目标已改变时不静默覆盖；
- 在应用内部完成“记录 → 问答 → 讨论 → 确认保存 → 再次使用”，不依赖外部 Agent 才能成立；
- Key 不出现在日志、SQLite 内容库、Markdown 和备份元数据中；
- 整理、问答、讨论、结论保存及失败状态均与新版 Demo 对照通过，结果来自真实处理，不保留 Demo 模拟逻辑作为运行时回退。

**阶段 5 实现与实际验证（2026-09-07）：**

- 已接通正式 MemoryStore 自动整理：捕捉与 pending 任务原子落盘；单个后台消费者用有限候选执行一次模型调用；new/append/defer、局部更新、派生关键词、重试、回执、纠正与撤销均由 Rust 核心处理。未配置模型不阻塞捕捉和搜索，确认的讨论结论不再自动整理。
- 问答按多个独立关键词合并排名后限量，回忆逐段关联引用，保存实际证据版本及节选位置，区分当前/历史记录和新建议。补齐已有记忆作为结论目标、默认补充、融合预览与准确保存；过时目标保留确认原话并返回可纠正状态。
- Rust 工作区测试 **74 项通过**；UI 行为测试 **37 项通过**。新增覆盖任务抢占/恢复/去重、手动归属与模型竞争、非法局部修改、版本变化/删除、关键词重建与清理、无证据/无引用、长文实际命中片段、历史版本、结论追加/融合/撤销、过期预览与目标加载失败。`cargo fmt --all -- --check`、全目标 Clippy（warnings denied）、前端构建及原生 debug bundle 已执行。
- `scripts/verify_phase5.py` 实际强杀已认领任务的进程，重启后沿用同一 attempt，仅生成一个版本与回执，原话逐字保留；重新执行不重复应用。`scripts/verify_phase2.py` 的并发写入、原话/版本强杀恢复、在线备份与完整性检查继续通过。
- 固定合成整理集位于 `crates/memivy-core/tests/fixtures/phase5_cases.json`：16 个 new、16 个 append、8 个 defer。当前本机配置的 `gpt-5.6-luna` 首轮 **36/40**，其中两次 HTTP 500，两次因缺失指代而错误续接。收紧“候选次序不能解释第二个/刚才”等规则后，全组重跑 **40/40** 通过动作、目标和事务校验；本轮中位耗时约 **4.67 秒**，最大 **8.63 秒**。人工核对了变化、犹豫、否定、人物区分与提示注入案例的正文，未发现将假设变成确定事实；这不是对任意用户资料或其他模型的准确率保证。
- 真实问答链使用独立合成库：当前判断、过去变化、收费追问、新建议、无个人经历证据共 5 轮，以及一次融合预览。早期一次服务端失败；另一轮观察到组合长词造成漏搜，改为独立主题词/关注点后重跑，前 4 轮均找到相关版本，最后一轮无证据时不编造经历。所有问答与预览均未写入记忆。原始结果保存在忽略目录 `research/phase5-eval/`，可通过 `intelligence_probe`、`discussion_probe` 复核。
- 原生实测采用 `com.memivy.phase5qa` 独立构建和 `/private/tmp/memivy-phase5-native` 合成库，明确环境变量后确认空库才输入测试文字。已看到保存后 processing、新建/补充回执、前后正文、撤销后的原话、按原话另存、从记忆讨论、逐段引用及当轮节选、结论选择已有目标、融合预览、手动修改后准确保存，并在重启后找回记忆与话题。确认前仅有 2 条捕捉；确认后恰好增加 1 条结论来源，最终版本与审核文字逐字相同。测试库检查未含配置中的 Key。后续真实端点失败时，原生界面显示保留原话和重新整理入口，点击后回到 processing，最终返回 defer，未新增重复捕捉。默认追加已有记忆也在原生端复核，结果等于此前全文加上审核的结论；保存与撤销均返回真实回执。最终测试窗口已关闭。
- 原生启动曾受 shell 沙箱阻断；早期仅改 plist 的副本被 CUA 自动启动到默认数据上下文，未在该窗口输入或执行内容动作，核查默认库没有整理任务。之后改用独立 Tauri identifier 和明确临时数据环境，并验证空库后继续。正常运行窗口需要重启到本阶段构建；不声称旧进程已加载新代码。
- 验证边界：正式 MCP、签名发布、跨设备、embedding 均未实施；本阶段复用了阶段 4 的快捷窗口与交接，未将本轮验证描述为再次完成全套物理快捷键、登录启动和跨屏测试。真实端点可能限流/离线或返回服务端错误，失败时需用户重试；保留原话不等于保证模型每次整理成功。当前记录是开发验证，不代替用户验收。


**阶段 5 Review 修复与复验（2026-09-07）：**用户要求“修复这些问题”，本轮修复审查中确认的 7 处缺陷：

- 保存目标冲突时，完整审核融合稿与确认标题/去向一起持久保存（schema 5 增加 `merged_body`）；从原话页可查看、审核并明确另存，重启不丢失。保留稿不进入普通检索，擦除原话时一并清理。取消了冲突回执中跳过完整稿核对的直接另存入口。
- 整理回执绑定记录与刷新版本；切换、读取失败和组件卸载后，旧按钮与迟到回调不能操作或打开上一条记录。
- 多查询合并保留各次命中的原话集合；长文按查询覆盖与标题权重选取连续片段，并保留原文 Unicode 偏移。新增端到端回归验证“主题命中当前版、收费命中原话”以及长文靠后的收费事实实际进入模型证据，查看引用返回同一片段。
- 后台失败状态写入遇到数据库锁时保留待写终态并退避重试，不再丢弃第二次 Busy；锁释放后可正常重试整理。候选同时携带项目来源，筛选和事务提交均阻止已标记不同项目的自动续接。
- 真实评估仅接受全新且不存在的目录，原子创建，不能跳过旧结果并改写模型名称；保存模型、配置开关、端点摘要、固定输入与实现代码摘要。进程实测用不可调用的假模型和已有成绩目录，确认直接拒绝，原 summary 不变，也未写入新案例。
- 当前 Rust 全工作区/全目标 **81 项通过**，UI 行为 **41 项通过**；前端构建、Rust fmt、Clippy（warnings denied）和默认原生 debug bundle 通过。Phase 5 强杀/恢复/幂等以及 Phase 2 并发写入、备份、完整性进程检查继续通过。
- 使用独立 `com.memivy.phase5fixqa` 构建和 `/private/tmp/memivy-phase5-fixes-native` 合成库，在原生窗口查看完整保留稿，修改中文及换行后确认另存，再撤销并重启。数据库核对最终正文逐字等于审核输入；捕捉总数仍为 2，短结论与并发修改的目标正文都未改变；重启后原审核稿仍可展开读取。测试窗口已退出，正常用户进程未重启。
- 使用当前本机 `gpt-5.6-luna` 在全新目录重跑 40 条固定案例：**38/40** 通过动作、目标与核心写入校验，中位耗时 **4.72 秒**。另 2 条（new-03、new-06）动作正确但模型返回空标题，被核心拒绝写入；未将旧的 40/40 成绩算作本轮结果，也未把失败解释为成功。证据保存在忽略目录 `research/phase5-fixes/`，其中不包含配置中的 Key。真实模型输出仍存在波动，这份开发验证不代替用户验收。

### 阶段 6：MCP 与 macOS 外部测试版

**授权与范围（2026-09-07）：**用户先要求充分研究并讨论决策，明确“支持 MCP”而不设客户端白名单、不支持旧 Mac、目前没有签名条件；审阅简要方案后以“好，实现它”授权实施。沿用当前正式 UI 与共享核心，仅增加两项 stdio 工具及必要设置。开发包面向 Apple Silicon / macOS 26，采用 ad-hoc 签名，Developer ID 和 notarization 明确延期；不为延期增加云端服务、自动更新或旧数据兼容流程。

**状态：**开发实现与下列本机验证已完成；另一台干净 Mac 和 10–15 人连续试用尚未执行，不据此认定整个 Mac MVP 已通过外部验收。

交付行为：

- 默认 `memivy-mcp` 使用正式 MemoryStore，与原生 UI 共用事务、UUID 幂等、来源和整理队列；历史原型单独使用 `memivy-mcp-prototype`，数据仍隔离。
- 只公开 `memory_capture` / `memory_search`。保存只接受 Agent 来源的明确授权原文，Agent 名必填，项目/URI 缺省为空；先落盘再回执，应用负责后续 AI 整理。
- 搜索不调用模型；非空关键词、默认 5 条、最多 8 条，无分页。复用 FTS5/短词字面搜索、先过滤再限量，结果绑定实际命中的原话或当前版本；不返回未保存会话、草稿、已删除记录或旧版本正文。
- 总开关默认关闭。每个数据调用都受跨进程锁与即时开关检查约束；关闭成功后不再接受新数据调用。协议发现与不读记忆的诊断可用；输入帧限制 1 MiB，stdout 专用于协议，stderr 不包含原话、路径或 Key。
- 设置提供真实开关、同包绝对路径配置复制、连接说明和本地 stdio 检查。明确外部 Agent 可能将结果交给它的模型；区分开关、本机检查与真实外部调用。
- 独立数据库变更监听不依赖模型配置；外部写入自动刷新已有 UI，退出期间的待整理任务在应用重启后继续。未提交草稿不会因刷新被覆盖。
- `npm run build:beta` 生成同版本 app、DMG、SHA-256；DMG 含 Applications 链接与安装/手动替换/卸载/数据保留说明。包内 MCP 不依赖开发机的 Rust、Node 或 Python。

实际验证（2026-09-07）：

| 层面 | 已完成证据与边界 |
|---|---|
| 构建与回归 | 前端 TypeScript/Vite、43 项 Node UI 回归、Rust fmt、Clippy（warnings 视为错误）、全工作区 86 项 Rust 测试通过。新增 6 项核心 MCP 回归与 2 项设置交互回归；原有测试仍通过。 |
| 正式协议与进程 | `verify_phase6.py` 对实际打包 sidecar 通过：2025-11-25 initialize 与 2026-07-28 discover/请求元数据、两个工具、默认关闭/动态关闭、原文与来源、重试去重/冲突、非法参数与未知工具、12 个应用/MCP 并发写入、确认后 SIGKILL、应用未运行时 pending、帧限额、EOF 与 SQLite 完整性。 |
| 真实 Agent 样本 | 本机 Codex CLI 与 Claude Code 分别在独立合成库完成一次明确保存及检索；核对工具调用记录和数据库，均仅保存一次原文，未编造 project/URI。这是标准 MCP 的兼容性样本，不形成客户端白名单，也不承诺任意客户端会主动调用。 |
| 原生设置和生命周期 | 独立 QA bundle 实测开关、配置复制、关闭时本地检查仍可用、检查不宣称 Agent 已连接；无模型时外部记录自动出现，刷新保留正在输入的中文草稿。退出后通过 MCP 保存并检索，重启后草稿仍在；三个 pending 任务通过本机固定协议端点整理为当前记忆，显示回执、版本与 Agent 原话来源。固定端点用于工程闭环，不代表模型质量测试。 |
| 真实包 | release app 与包内 MCP 均为 arm64，Info.plist 最低系统 26.0；`codesign --verify --deep --strict` 通过。最终 DMG 只读挂载及校验通过，包含应用、Applications 链接与说明。从 DMG 复制到隔离安装目录的原始包实际启动、中文保存、同包 MCP 诊断与配置路径通过，安装副本的 MCP 再跑协议检查通过。 |
| 替换与数据保留 | 退出安装副本后执行同版本手动替换，重启仍看到同一条合成记忆；退出并移除该测试应用包后，独立数据目录的原文和完整性仍在。此项不是跨版本数据库升级测试。测试结束退出测试实例、停止固定模型端点并卸载测试 DMG。 |
| 现有数据流程 | 阶段 2 并发/备份/崩溃脚本、阶段 5 整理任务崩溃恢复脚本通过。历史阶段 1 测试脚本仅调整显式原型可执行名，其既有进程/stdio 回归也通过，不重新定义其历史验收。 |

**全量改动 Review 与修复（2026-09-07）：**用户要求检查所有改动并修复问题。本轮发现并修复：后台数据库刷新会关闭已打开的整理变化对比，并干扰同一记录正在执行的保存/撤销；MCP 开关写入期间可关闭设置，且旧状态读取或写入报错可能让界面与已落盘开关不一致；自定义 Cargo 输出目录时，打包脚本可能复制默认目录中的旧应用或旧 MCP。整理回执现在按记录身份保留交互状态，设置等待操作完成并重新读取实际开关，构建从 Cargo 实际产物与目标目录定位输出。MCP 服务版本随 Cargo 包版本生成，打包前拒绝应用与 MCP 版本不一致。

修复验证：新增失败复现与回归后，52 项 Node 前端/构建测试、86 项 Rust 测试、TypeScript/Vite、fmt 和 Clippy 通过。原生隔离 QA 实测文件锁等待期间 Escape 不关闭设置、失败后显示关闭状态、重试成功；打开变化对比期间通过同包 MCP 写入，新记录自动刷新且对比保持打开。重新生成 release app 与 DMG，包内 MCP 协议检查通过；只读挂载最终 DMG，确认应用与 MCP 二进制哈希均与本轮构建一致，签名校验与 DMG SHA-256 通过后卸载。测试未调用真实模型；结束后关闭隔离库 MCP 并退出 QA 应用。

构建和测试证据保存在忽略目录 `research/phase6-*`。默认 Cargo 目录下，应用为 `target/aarch64-apple-darwin/release/bundle/macos/Memivy.app`；DMG 与 SHA-256 固定输出到 `target/release/bundle/dmg/`。本轮不修改 Demo、DESIGN.md 或品牌，不提交或推送代码。

尚未完成的外部验证：另一台干净 Mac 的下载隔离属性/Gatekeeper 首次打开、无开发运行时环境、真实跨版本替换、10–15 名用户的连续试用及效果统计。当前无 Developer ID 和 Apple 公证，不把本机 ad-hoc 签名校验描述为 Gatekeeper 放行或正式公开发布。没有向测试用户发送邀请。

后续外测仍需收集：应用内及跨 Agent 的有效续接、回答可用性、结论保存、落位纠正、无结果和失败原因；样本兼顾使用与不使用外部 Agent 的用户。完成这些真实验证后，才能评价 PRD 定义的 Mac MVP 是否通过外部验收。

## 5. 核心测试资产

**2026-09-07 实施范围：**用户要求“做一下 5. 核心测试资产”。本节将阶段 2–6 已有回归与新增固定材料连接成可重复执行的资产，不增加产品功能、数据库兼容流程或发布验收。原话安全、显式确认和版本冲突规则继续采用 PRD；正式 UI 的基准采用阶段 3–6 已确认的当前源代码，保留历史 Demo v0.1 的原有认可范围。

统一入口：`npm run test:core-assets`。它在忽略的 `research/core-tests/` 下创建全新运行目录，执行 Rust fmt、Clippy、全工作区/全目标测试、Node UI 回归、前端构建、实际进程/stdio 故障脚本，并准备原生视觉合成库。使用 Cargo 返回的实际可执行产物，支持自定义 target 目录；结果记录平台、代码/案例/二进制 SHA-256、各阶段退出码和日志。任一自动检查失败，命令返回非零。默认不读取本机模型配置、不调用外部模型。

真实模型评估：`npm run test:core-assets -- --models-only --model-config /absolute/path/to/private-model.json`。配置必须在仓库之外且为 `0600`，只发送版本控制中的合成材料；配置和 Key 不复制进结果。每次创建新目录，禁止覆盖旧成绩，保留错误案例和人工复核表，并扫描结果是否包含本次配置的 Key。`--models-only` 只运行模型资产，不代表离线回归通过。离线通过、模型结构通过、语义复核、原生交互和用户验收分别记录，不合并为一个准确率。

### 5.1 视觉与交互对照

- 固定操作与窗口规格在 [visual_contract.json](tests/assets/visual_contract.json)：主窗口 1140×790 与最小 760×560，覆盖首页、紧凑列表/宽正文、原话与版本、折叠菜单导出、讨论引用、审核保存、输入法、快捷面板、迟到回答与 MCP 刷新。
- [visual_fixture](crates/memivy-core/examples/visual_fixture.rs) 与搜索测试共用合成材料，只接受不存在的绝对目录，创建带当前版/旧版/原话、回收站、未保存草稿和固定讨论的正式 MemoryStore；MCP 关闭，无模型配置。统一入口生成 `visual-data/` 与初始全为 `not_run` 的 `visual-review.json`。
- 原生 QA 使用独立 bundle identifier 和显式 `MEMIVY_DATA_DIR`，启动后先核对固定合成内容再操作。每张截图记录窗口逻辑尺寸、截图像素、缩放、应用和源码摘要、案例 ID；截图只作证据。首次采集不自动成为用户认可的新基准，不能为了测试变绿更新认可记录。
- 浏览器预览负责基本布局和样式；当前预览不连接上述 SQLite 合成库，不将不同示例内容冒充逐像素同源对照。中文组词、焦点、物理快捷键、窗口交接与退出恢复在原生端逐项记录；本轮不重新定义已验收原型。
- [Tauri 当前测试文档](https://v2.tauri.app/develop/tests/webdriver/)已提供通过嵌入式服务驱动 macOS 的路径。本轮沿用现有原生 UI 检查工具，不引入新的运行时插件或 E2E 依赖；未执行的项目保留 `not_run`。

### 5.2 固定搜索集

[core_search.json](crates/memivy-core/tests/fixtures/core_search.json) 固定 7 条种子与 28 条查询，[core_search.rs](crates/memivy-core/tests/core_search.rs) 自动验证：

- 中文连续短语、错位子串、一/两字符、英文大小写和子串、URL、项目、引号、emoji 与字面符号；
- 当前正文与旧原话分别命中、跨当前正文/原话组合检索、旧版本独有正文不进入普通搜索；
- 时间下界包含/上界不包含、项目/来源过滤、无结果、回收站/草稿/普通会话排除；
- 相同查询通过正式记忆库和 MCP，索引重建前后结果集合一致，返回源可以实际解析；
- 另加入 55 个相近干扰项，核对标题优先、分页/有更多结果、MCP 数量上限和先过滤再限量，不绑定同分结果的随机 UUID 顺序。

使用项目 bundled SQLite，日志报告实际 SQLite 版本；不拿系统 Python SQLite 的 tokenizer 替代。三字符以下单独覆盖的依据是 [SQLite trigram 限制](https://www.sqlite.org/fts5.html#the_trigram_tokenizer)；[Joplin 的中日韩检索说明](https://joplinapp.org/help/apps/search/)也说明不能只用英文分词样本验证笔记检索。这里遵守 Memivy 已定的字面检索规则，不复制竞品的查询语言。

### 5.3 Memory Agent 合同集

- 复用 [phase5_cases.json](crates/memivy-core/tests/fixtures/phase5_cases.json) 的 **40 条语义案例**：16 new、16 append、8 defer；新想法、人物/项目不同、明确改变、犹豫/否定、因果不确定、小样本、缺少指代和提示注入均保留。
- [organization_review.json](crates/memivy-core/tests/fixtures/organization_review.json) 为每例补齐判定依据和必须保留的语义。标签是已有开发预期，**尚未声称经过用户逐条人工裁定**；有争议的标签先请用户裁定，不能看完模型输出后为提高分数改答案。
- [organization_failures.json](crates/memivy-core/tests/fixtures/organization_failures.json) 加入 **8 条独立故障合同**：未知目标、截断、空结果、429、500、连接中断、超限、正式 90 秒超时。由 [core_faults.rs](crates/memivy-core/tests/core_faults.rs) 经真实 loopback HTTP 和正式整理调用验证，不把这些确定性故障算进真实模型准确率。
- [intelligence_probe](crates/memivy-core/examples/intelligence_probe.rs) 保存候选、动作、目标、最终正文、原话保留、重放结果、耗时、固定输入/实现摘要及逐条复核表。动作或目标错误、核心拒绝写入、模型调用失败均计入失败，返回非零；正文含义、未涉及内容和不确定性需另行核对。

这 48 条合同不是模型排行榜，也不是任意用户资料上的准确率承诺。任何错误写入、原话丢失、无确认保存或凭空来源都阻止将相应验收认定为通过；未确认统一的模型质量百分比不擅自设成产品发布门槛。

### 5.4 记忆问答与结论保存案例

[core_discussion.json](crates/memivy-core/tests/fixtures/core_discussion.json) 提供 **12 条真实模型问答案例**与逐例语义要求；[discussion_probe](crates/memivy-core/examples/discussion_probe.rs) 在隔离库中执行：单条回答、空库/无相关证据、当前与历史变化、跨记录背景、不同人物观点冲突、连续追问、新建议、假设、提示注入、中英文与重新打开数据目录。

每轮保留实际回答、完整的已取回依据（含最终未引用的节选）、冻结引用节选、预期来源、独立关键词搜索对照和未写入记忆断言。来源 ID 覆盖是结构检查，不能证明每句话有依据。`review.json` 分别记录检索漏失、回答来源支持度和不确定性；错误必须归因于检索、证据选择、回答或服务故障，不能合并为“回答失败”。[NotebookLM 的来源与引用说明](https://support.google.com/notebooklm/answer/16164461?hl=en)用于参考用户核对来源的需求，不将出现引用号等同于事实正确。

[conclusion_contract.json](tests/assets/conclusion_contract.json) 将 **8 项保存/生命周期合同**映射到现有可执行回归：确认前隔离、逐字保留修改稿、默认追加/审核融合、重试去重、并发冲突、完整稿重启保留、撤销后原话仍在、删除会话保留结论、固定版本与删除来源、取消后的迟到响应。测试模拟显式确认；原生真实点击和跨窗口交接仍通过 5.1 单独记录。

### 5.5 不可妥协的故障测试

| 故障 | 可重复资产及判定 |
|---|---|
| 原话/版本确认后立即杀进程 | [verify_phase2.py](scripts/verify_phase2.py)：实际 SIGKILL 后检查原话、版本、回执与完整性。 |
| 模型坏响应、限流、超时、中断与重复结果 | `core_faults.rs` 的 8 条合同：无部分记忆写入，原话与关键词搜索仍可用；明确重试后只产生一个版本/回执。 |
| AI 事务执行到最后失败 | `organization_failure_after_receipt_and_index_updates_rolls_back_every_effect` 在更新整理任务终态前触发 SQL ABORT；检查版本、来源状态、回执、搜索词及索引回滚，同一 attempt 可恢复。 |
| 已认领整理任务强杀 | [verify_phase5.py](scripts/verify_phase5.py)：重启沿用 attempt，恰好应用一次。 |
| 取消后迟到响应、生成中退出 | `memory_discussion.rs` / `memory_data.rs` 的取消和恢复回归；[verify_core_faults.py](scripts/verify_core_faults.py) 强杀已持久化 pending 问答进程，重启变 interrupted，长期记忆计数不变。 |
| 确认保存与编辑/删除并发 | `memory_intelligence.rs` / `memory_data.rs` 的过期目标、删除来源、完整稿保留及并发编辑回归。 |
| UI 核心入口与 MCP 同时写入 | [verify_phase6.py](scripts/verify_phase6.py)：真实 stdio 与独立 MemoryStore 进程并发写入、幂等、开关、边界及 SIGKILL。原生 UI 刷新另验。 |
| migration 中断后重启 | `verify_core_faults.py` 以仓库迁移 SQL 在 SQLite 未提交事务中设置测试停点并 SIGKILL，确认无半套 schema 后交给 MemoryStore 重新初始化；已有 DDL 失败回归验证迁移整体回滚。停点仅在测试 harness，不是对实际 `MemoryStore::open` 中途插桩，也不等于断电模拟。 |
| 索引删除和重建 | 原有 `rebuilding_missing_index_recovers_facts_and_preserves_erasure` 加 28 条双路径固定搜索集。 |
| 活动库备份与恢复 | `verify_phase2.py` 在并发写入期间调用备份 API，并恢复到全新目录；`memory_data.rs` 核对历史、会话、回收站和来源。 |
| Key 出现在日志/导出/备份 | `core_faults.rs` 将合成 Key 放入真实配置与错误 HTTP 正文，检查脱敏错误、单篇导出、SQLite 及备份；模型运行另扫描整组证据是否包含实际配置 Key。 |

故障设计参考 [SQLite 的崩溃与故障注入测试](https://www.sqlite.org/testing.html)，本项目检查的是业务事务和应用恢复，不声称复现了 SQLite 全套磁盘 I/O 故障测试。

**本轮执行记录（2026-09-07）：**

- 统一离线入口完整通过：**92 项 Rust 全工作区/全目标测试、56 项 Node UI/资产测试**、fmt、Clippy（warnings denied）、TypeScript/Vite 构建，以及 Phase 2/5/6 和新增 core fault 进程脚本。实际 bundled SQLite 为 **3.53.2**。28 条搜索在记忆库/MCP、重建前后共 112 组结果对照通过；8 条正式模型失败合同包含实际 90 秒超时。强杀迁移事务和 pending 问答、在线备份后恢复全新目录均通过。
- 当前本机 `gpt-5.6-luna` 在新目录完成一次 40 条整理：**37/40** 通过动作、目标、核心写入、原话保留和重放检查，中位耗时 **5.35 秒**。new-09 返回空标题，append-09 以记忆标题替代 `M1` 编号，append-14 对短正文的局部修改超过一半保护限额；三者均被核心拒绝写入，原话保留。没有重跑挑选较好成绩，也没有降低核心保护规则。已请用户选择是否将提示词/输出约束修复扩展到本轮；未收到扩展选择时，保持测试资产范围。
- 首轮 12 条真实问答全部完成，来源集合检查 **12/12**，每轮长期记忆计数保持不变；Codex 逐条核对答案及成功整理正文/8 个 defer 理由。随后补齐“已取回但未引用的依据”诊断，并以对应确定性回归及全新目录复验：最终 12 条问答仍全部通过结构检查，但 **qa-09 的结论把收费未定的现状推定为一次性产品**，提出“从一次性产品转向持续交付服务”的无据前提。该轮回忆段有来源，结论推演仍须修正；已单独标记，不能声称语义 12/12。`codex-review.json` 与初始人工复核表分开保存，**不将代理复核冒充用户人工裁定**，也不据此承诺其他模型或真实资料效果。
- 独立 `com.memivy.coreassetsqa` debug bundle 使用上述固定合成库，原生核对首页、中文关键词结果、正文、原话、旧版本、折叠菜单导出、讨论和引用；默认窗口与缩至最小尺寸后的页面均留存截图。审核结论改为两行中文，选择已有目标后显示默认补充；数据库确认保存前 7 条捕捉、保存后 8 条，正文逐字等于旧全文加审核文字。点击撤销后旧正文恢复，确认原话仍在。原生测试包已退出。截图是 CUA 缩放后图像，设备 backing scale 未暴露，未冒充原始像素金图。
- 本轮未重新执行完整中文组词、物理全局快捷键、全屏/跨屏、融合预览、生成中退出和原生 MCP 刷新的全部交互清单；相关核心/组件/进程回归通过不替代原生项目。现有阶段验收记录保留，不声称整套外部发布或用户视觉验收完成。
- 完整原始证据位于忽略目录 `research/core-tests/20260907T133759Z-b320c847/`（最终离线；较早一轮为 `20260907T132020Z-9958a22a`）、`research/core-tests/20260907T132018Z-1a078b04/`（真实模型，整体状态正确记为 failed）及 `research/core-assets-native/`（原生及 `discussion-final/` 最终问答诊断）。两项评估可执行程序拒绝已存在目录且不改写旧结果的实际进程检查也通过。中间一次诊断补充编译失败，临时命令误执行了旧可执行文件；该组已标记不可用于新诊断证明，保留审计说明并排除出上述最终成绩。最终构建与重跑使用当前可执行文件，已验证所有问答案例含新诊断字段。真实 Key 扫描通过。测试材料/脚本纳入版本控制范围，真实运行结果、合成数据库、截图和私密配置不纳入。

## 6. 发布与观察

### macOS 外部测试

首轮观察四周，重点跟踪：

- 第一次记录后是否就能找回、提问并获得有用回应；
- 记忆后来是否真的被用户或 Agent 采用；
- 继续讨论是否帮助思考，用户是否愿意保存结论，以及结论后来是否再次使用；
- 回答是否有正确来源，证据不足与新建议是否表达清楚；
- AI 续错位置的比例；
- 暂不判断是否长期堆积；
- 用户通过关键词搜索、浏览、应用内问答还是外部 Agent 找回；
- 无 embedding 导致的真实漏搜案例；
- 用户是否自然要求手机与电脑共享同一份记忆。

只有手机访问需求被真实验证，才另立移动端与同步项目；当前不预先设计。

### 发布边界

- 只维护 macOS 的签名、安装、升级和卸载检查表；
- GitHub Release 不等于发布完成，必须在干净 Mac 安装；
- 升级必须保留数据库与本机配置；
- 官网、安装文档和发布页只列出 macOS。

## 7. 粗略工作量

原有 7–10 周估算对应以捕捉、整理和检索为主的旧范围，不能继续作为新版总工期。阶段 1 已完成，不重复计入剩余工作；本次不为尚待研究的交互编造排期。

| 工作 | 当前状态与重新估算依据 |
|---|---|
| 产品文档同步 | 本次范围；方向与待研究项在现有文档内同步 |
| 交互研究与新版 Demo | 阶段 3 已指定当前 dev 基准；后续 AI/快捷入口交互按各阶段推进 |
| 阶段 1 技术样机 | 已验收通过，保留现有结果与证据边界 |
| 阶段 2 核心数据 | 已实现并验证，见阶段 2 记录 |
| 阶段 3 首页与记忆库 | 已获开发授权；实现及实测见阶段 3 记录，尚未记录用户最终验收 |
| 阶段 4 快捷入口 | 已授权并接入正式数据；工程与原生验证见阶段 4，尚待整体用户验收 |
| 阶段 5 AI 完整闭环 | 已授权并完成本轮实现与开发验证：自动整理、逐段引用问答、继续讨论、已有目标结论保存与审核融合；详见阶段 5 记录，待用户体验验收 |
| 阶段 6 MCP 与 Mac 发布收口 | 已授权并完成开发包与本机验证；正式签名延期，另一台 Mac 与长期外测待完成 |

在交互研究和新版 Demo 确认后更新剩余工作量，不把本文列出的未来工作当成已经开始开发。

## 8. 开发期间不提前做的事

- 不因“以后可能同步”先加 CRDT 或 change log；
- 不因“以后可能语义搜索”先加 embedding 表和模型下载；
- 不同时实现 stdio 与 HTTP MCP；
- 不为 Windows、iOS、Android 构建空壳或预留适配层；
- 不构建、测试、发布或维护 Linux 版本；
- 不先做富文本、标签、图谱、插件或自动更新；
- 不因新增问答拆独立聊天服务、通用 Agent 框架或全量用户画像系统；
- 不把 API Key 放进内容数据库或导出文件。

## 9. 暂缓范围

Windows、iOS、Android 没有开发排期，也不是 Mac MVP 的验收条件。未来若恢复其中任一平台，单独立项并重新确认捕捉方式、MCP、同步和发布要求；不默认当前实现可以零成本移植。Linux 不支持。
