# Memivy 开发计划

状态：开发顺序已确认；视觉 Demo 尚未制作、尚未验收
依据：[PRD.md](PRD.md)、[TECH_STACK.md](TECH_STACK.md) 与 [DESIGN.md](DESIGN.md)，更新日期 2026-09-05

## 1. 计划目标

先把 Memivy 的视觉和关键交互做成可点击 Demo，由用户明确确认后再开始正式开发：

> **Miro 视觉落地为 Demo → 用户试用并确认 → 技术风险验证 → 数据与功能开发 → 对照 Demo 验收界面 → Mac 测试版。**

正式产品仍然交付同一个记忆闭环：

> 任意位置捕捉原话 → 立即落盘 → AI 新建/续写/暂不判断 → 可见且可撤销的回执 → 关键词或 Agent 再次取回。

本计划只交付 macOS。Windows、iOS、Android 暂缓，不提前构建空壳、兼容接口或发布流程；Linux 不支持。

## 2. 开发原则

1. **先 Demo，后开发**：视觉 Demo 未获用户明确通过前，只迭代 Demo，不初始化正式 Tauri/Rust 工程，不开发数据库、真实 AI 或 MCP；
2. **三份依据各司其职**：PRD 决定功能；官方 `DESIGN.md` 决定 Miro 视觉语言；通过的 Demo 固定 Memivy 的布局、组件外观与状态表现。Demo 不增加 PRD 之外的功能，`DESIGN.md` 原文保持不变；
3. **通过后不重新设计**：正式 UI 复用或对照 Demo 实现；需要改变已确认的视觉或交互时，先调整 Demo 并重新确认受影响部分，不在功能开发时自行换版；
4. 原话落盘优先于 AI；一份 Rust 业务核心、一套数据库规则，UI 和 MCP 不复制记忆逻辑；
5. Demo 的模拟行为不等于技术验证。通过视觉后，仍须先完成技术风险样机，再逐阶段交付可运行、可验证的功能；
6. 不提前实现 embedding、同步、任务系统、富文本、插件或额外平台适配。

## 3. 建议仓库结构

```text
memivy/
├── design-demo/             可点击视觉基准；阶段 0 创建，纳入版本管理
│   └── index.html           Demo 固定入口，必要样式和脚本放在同目录
├── app/                     React + TypeScript 界面
├── src-tauri/               Tauri host 与 macOS 系统接线
├── crates/
│   ├── memivy-core/         业务规则、数据、AI、搜索、导出
│   └── memivy-mcp/          macOS stdio 入口，只做协议适配
├── migrations/              SQLite migration
├── tests/
│   ├── fixtures/            搜索与 AI 合同案例
│   └── e2e/                 关键用户路径
├── PRD.md
├── TECH_STACK.md
├── DESIGN.md                GetDesign 官方 Miro 原文
├── AGENTS.md
└── DEVELOPMENT_PLAN.md
```

以上是规划结构，不代表目录已经存在。阶段 0 只创建 `design-demo/`；正式应用目录从阶段 1 开始按需创建。候选素材和临时截图仍放在忽略的 `research/`，但长期使用的 Demo 不放在那里。

正式工程只保留两个 Rust crate。core 内部按模块分层，不为每个名词拆 crate，不引入服务仓库。

## 4. 阶段与验收门槛

### 阶段 0：视觉设计与可点击 Demo

目的：先让用户看见、点到并确认 Memivy 将来的样子，不用等后端完成才判断设计。

制作方式：

- 依据 `DESIGN.md` 做一套统一的 Miro 风格，不再并列探索其他品牌或另生成设计系统；
- 用轻量 HTML/CSS/JavaScript 与固定模拟数据制作可在本地浏览器运行的 Demo，不先搭正式应用脚手架；
- 主要按钮可点击，页面和状态可切换；不是截图集、配色板或营销落地页；
- 不连接真实模型、SQLite、MCP 或系统快捷键，不收集真实 API Key；模拟行为明确标注。

最少覆盖：

| 界面 | 要看见和试用的内容 |
|---|---|
| 备忘录主窗口 | 列表与搜索、当前版本编辑、原话来源时间线、历史查看；创建、删除与取消的基础操作 |
| 捕捉小窗与短回执 | 输入与主动附带来源；提交后小窗消失，再展示新建、续写或暂不判断的动作与目标；可撤销或另存为新记忆 |
| 精简设置与菜单栏示意 | BYOM 配置与连接测试状态、MCP 总开关、导出入口；菜单栏可用/暂停状态 |

用少量固定场景覆盖空库、搜索无结果、AI 等待配置/处理中/失败，以及长中文内容和较窄 Mac 窗口。保持同一套组件，不为每种状态另做一页；不加入聊天首页、待办或统计仪表盘。

通过条件：

- 用户实际试用“捕捉 → 回执 → 纠正 → 打开记忆 → 搜索”，并明确表示本版 Demo 通过；仅选定 Miro 不算完成验收；
- 主窗口、捕捉、回执和设置的布局、字体层级、按钮、卡片及关键状态已确认；约定窗口尺寸下无非预期文字截断、重叠或不可操作区域；
- 保存同一版 Demo 源文件，并在下方记录确认日期、入口、版本及验收窗口尺寸，不另写验收报告；
- 未通过就继续改 Demo。视觉通过只允许进入阶段 1，不代表真实保存、AI、MCP 或原生窗口能力已完成。

**Demo 确认记录：**尚未通过；规划入口为 `design-demo/index.html`（尚未创建）。通过后补充确认日期、对应提交、验收窗口尺寸及用户确认的必要差异。若 Demo 与原始设计规范不一致，先确认差异，不自行选择一套覆盖另一套。

### 阶段 1：技术风险样机

前置条件：阶段 0 已获用户明确通过。

目的：验证最可能推翻技术选型的事项，同时检查通过的视觉在 Mac 应用窗口中的可实现性。

工作：

- 初始化 Tauri 2 + React + Rust，在目标 macOS 版本编译和运行空应用；
- 把通过的 Demo 中主窗口和捕捉小窗的最小界面放入 Tauri，验证字体、布局与窗口约束，不重做视觉；
- 从 Rust 创建 SQLite，验证 bundled SQLite、FTS5 trigram、迁移和中文查询；
- 验证菜单栏、全局快捷键、隐藏/唤起小窗；
- 验证中文输入、长文本滚动、窗口焦点和多显示器；
- 用 `rmcp` 做只有一个假工具的 stdio 程序，连通优先支持的 Agent；
- 验证远程和本地 OpenAI-compatible 端点各一次。

退出条件：

- React 界面与 MCP 都能调用同一 Rust core 并读写同一结构的数据库；
- macOS 快捷键能可靠唤起并聚焦输入框；
- 中英文搜索样例通过；
- 至少目标 Agent 可调用 stdio MCP；
- 目标 macOS 版本没有不可接受的输入法、焦点或 WebView 问题。

如果 Tauri 的 macOS WebView、窗口焦点或系统能力在这一阶段失败，再重新比较 Electron 或 SwiftUI；不在正式业务开发中同时维护两条技术路线。技术限制若迫使界面改变，先回到 Demo 确认受影响部分，再继续实现。

### 阶段 2：核心数据层

目的：建立无需 AI 也能可靠工作的本地备忘录。

工作：

- 定义 capture、memory、memory_version、版本来源和配置边界；
- 完成 migration、事务、WAL、busy timeout 和异常恢复；
- 实现原话创建、记忆 CRUD、用户编辑生成新版本、删除与撤销；
- 实现 Markdown 导出和一致性数据库备份；
- 建立统一错误类型和脱敏日志。

退出条件：

- 强制终止 AI 或应用都不会丢失已提交原话；
- AI 代码没有修改原话的接口；
- 每个当前版本都能追溯到历史版本和来源；
- 新旧数据库迁移测试、备份恢复测试通过。

### 阶段 3：按 Demo 实现 macOS 备忘录基础界面

目的：把通过的主窗口 Demo 接到真实本地数据，让没有模型的用户也能完整记录和查找。

工作：

- 按通过的 Demo 完成列表、搜索、当前版本编辑、原话时间线、历史和设置界面，复用共用组件与样式；
- 以确认过的分栏、窗口尺寸、空状态和无结果状态为基准，替换模拟读写与搜索，不另起一套界面；
- 完成 FTS5 trigram 索引、标题权重、来源/时间/项目过滤；
- 小于三个字符的查询采用受限普通匹配；
- 对索引提供删除后重建能力。

退出条件：

- 未配置模型时，创建、编辑、历史、搜索、导出全部可用；
- 中文短语、英文词和 URL 搜索案例通过；
- 搜索不产生任何模型请求；
- 索引损坏或删除后能从事实表重建；
- 在与 Demo 相同的样例内容和窗口尺寸下对照实测，无未经确认的布局、组件或状态差异。

### 阶段 4：macOS 快捷捕捉

目的：完成产品最重要的两秒捕捉体验。

工作：

- 菜单栏常驻、开机启动选项和暂停/可用状态；
- 全局快捷键、小型无干扰捕捉窗、回车立即保存；
- 只读取前台应用名称；URL/文件路径必须由用户主动附带；
- 捕捉提交后立即关闭小窗，AI 状态通过回执事件更新；
- 处理重复快捷键、失焦、多显示器、全屏应用和应用重启；
- 小窗、菜单栏和回执沿用通过的 Demo；将浏览器模拟替换为真实原生窗口与保存事件，不把模拟 AI 结果带进正式应用。

退出条件：

- 从快捷键到原话事务提交，在目标机器上达到 PRD 的 1 秒要求；
- 未接入 AI 时，保存和关窗独立完成；真实模型断网或超时不阻塞保存的场景在阶段 5 复验；
- 未经用户点击，数据库中没有 URL 或文件路径；
- 连续快速捕捉不会覆盖、丢失或重复记录；
- 捕捉与保存状态的外观、来源选择和收起行为对照 Demo 通过；三种真实 AI 落位结果在阶段 5 验收。

### 阶段 5：Memory Agent

目的：形成 AI-native 的新建、续写和暂不判断闭环。

工作：

- BYOM 设置、连接测试、Key 本机文件保存与日志脱敏；
- 捕捉提交后创建持久 pending 状态；
- FTS/当前项目/最近记忆生成有限候选；
- 一次模型调用返回动作、目标、版本正文、标题和检索词；
- 严格结构校验后在一个事务中应用动作；
- 实现回执、撤销、改为新记忆和失败后重试；
- 应用启动时恢复被中断的 pending/processing；
- 用真实 AI 状态驱动 Demo 已确认的回执、纠正与错误界面。

退出条件：

- 三种动作均有可见回执和确定的数据结果；
- 非法输出、超时、限流、端点离线时只改变处理状态，不损伤原话；
- 撤销能恢复 AI 动作前的当前版本；
- 相同 job 不会因为重启被应用两次；
- Key 不出现在日志、SQLite 内容库、Markdown 和备份元数据中；
- 新建、续写、暂不判断及失败状态均与通过的 Demo 对照通过，且结果来自真实处理，不保留 Demo 模拟逻辑作为运行时回退。

### 阶段 6：MCP 与 macOS 外部测试版

目的：完成跨 Agent 入口并交付第一轮真实用户验证。

工作：

- 构建 `memivy-mcp` stdio 入口；
- 实现且只实现 `memory_capture`、`memory_search`；
- 强制 MCP 总开关、结果数量限制和 Agent/项目/会话来源；
- 提供清晰的客户端配置路径与连通诊断；
- 将 Demo 中的 MCP 开关和状态接到真实服务，并对照通过的设置界面验收；
- 完成签名、notarization、DMG 和卸载/数据保留说明；
- 邀请 PRD 中定义的 10–15 名 Mac 用户连续试用。

退出条件：

- 关闭 MCP 后所有工具调用被拒绝；
- capture 先保存原文，search 只返回少量当前记忆和必要来源；
- MCP 不存在修改、合并、删除或读取全库接口；
- 至少两种目标 Agent 的真实会话通过；
- 收集有效续接率、落位纠正率、无结果搜索和失败任务数据。

到这里才算 PRD 所定义的 Mac MVP 完成。

## 5. 核心测试资产

### 5.1 视觉与交互对照

- 保留阶段 0 通过的 Demo 版本；后续调整先在 Demo 展示并重新确认受影响部分，再更新基准，不能为了迁就已写出的代码反向修改基准；
- 正式 UI 使用与 Demo 相同的示例内容和窗口尺寸逐项对照布局、字体、组件、关键状态和操作顺序；
- 浏览器检查负责基本视觉，Mac 应用实测负责字体渲染、输入法、焦点与原生窗口行为；
- 发现差异时先修正式实现；确需改变基准时，回到 Demo 确认并更新本计划中的确认记录。截图只作证据，不代替可点击 Demo。

### 5.2 固定搜索集

建立一组中英文混合记忆，覆盖：

- 中文连续短语与错位子串；
- 英文大小写、URL、项目名；
- 两字符查询；
- 当前版本与旧原话内容不同；
- 时间、项目、来源过滤；
- 无结果和大量相近结果。

该集合用于 macOS 的 SQLite 构建，防止 SQLite 或 tokenizer 升级后行为漂移。

### 5.3 Memory Agent 合同集

首版准备 40–60 个经过人工判定的案例，至少包括：

- 明显新想法；
- 对旧记忆的补充；
- 修正或质疑旧判断；
- 信息不足，应暂不判断；
- 多个相似候选，不能强行续写；
- 模型返回不存在的 memory ID；
- JSON 截断、空结果、超时和限流。

这不是模型排行榜。它只验证当前提示词、候选生成与写入规则是否满足 PRD。

### 5.4 不可妥协的故障测试

- 原话事务提交后立即杀进程；
- 模型调用中断、返回坏 JSON、重复返回；
- AI 应用事务执行到一半失败；
- UI 与 MCP 同时写入；
- migration 中断后重启；
- 索引删除后重建；
- 活动数据库进行应用内备份并恢复；
- 日志和导出中扫描 API Key。

## 6. 发布与观察

### macOS 外部测试

先观察四周，不急着扩平台。重点不是捕捉总量，而是：

- 记忆后来是否真的被用户或 Agent 采用；
- AI 续错位置的比例；
- 暂不判断是否长期堆积；
- 用户到底通过搜索、浏览还是 Agent 找回；
- 无 embedding 导致的真实漏搜案例；
- 用户是否自然要求手机与电脑共享同一份记忆。

只有手机访问需求被真实验证，才另立移动端与同步项目；当前不预先设计。

### 发布边界

- 只维护 macOS 的签名、安装、升级和卸载检查表；
- GitHub Release 不等于发布完成，必须在干净 Mac 安装；
- 升级必须保留数据库与本机配置；
- 官网、安装文档和发布页只列出 macOS。

## 7. 粗略工作量

视觉阶段以用户明确通过为结束，不用固定日期代替验收。以下技术开发时间仅用于安排顺序，按一名熟悉 Rust/TypeScript 的开发者估算，不含前置视觉迭代和跨设备同步：

| 里程碑 | 粗略时间 |
|---|---:|
| 视觉设计与可点击 Demo | 先完成，以用户明确通过为准 |
| 风险样机 | 1 周 |
| 数据层与无 AI 备忘录 | 2–3 周 |
| macOS 捕捉与 Memory Agent | 3–4 周 |
| MCP、导出、Mac 发布收口 | 1–2 周 |

Demo 通过后，Mac 外部测试版的技术开发约 7–10 周。它不是包含视觉阶段的总工期承诺，阶段 1 的实测结果会改变估算。

## 8. 开发期间不提前做的事

- 不因“以后可能同步”先加 CRDT 或 change log；
- 不因“以后可能语义搜索”先加 embedding 表和模型下载；
- 不同时实现 stdio 与 HTTP MCP；
- 不为 Windows、iOS、Android 构建空壳或预留适配层；
- 不构建、测试、发布或维护 Linux 版本；
- 不先做富文本、标签、图谱、插件或自动更新；
- 不把 API Key 放进内容数据库或导出文件。

## 9. 暂缓范围

Windows、iOS、Android 没有开发排期，也不是 Mac MVP 的验收条件。未来若恢复其中任一平台，单独立项并重新确认捕捉方式、MCP、同步和发布要求；不默认当前实现可以零成本移植。Linux 不支持。
