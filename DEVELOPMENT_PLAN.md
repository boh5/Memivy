# Memivy 开发计划

当前源码说明（2026-09-13）：用户要求彻底移除旧交互样机，旧入口、旧 Store、旧 MCP 和专用验证脚本已删除。下文阶段 1 的命令、文件与验收结论仅作历史记录，不是当前启动或验证指南；当前命令见 AGENTS.md 的 Setup and validation。

状态：2026-09-06 阶段 3 首页与记忆库已实现并完成下方工程验证，待用户试用验收；阶段 1 第二版用户验收与阶段 2 数据验证记录保留；Demo v0.1 未改
依据：[PRD.md](PRD.md)、[TECH_STACK.md](TECH_STACK.md) 与 [DESIGN.md](DESIGN.md)，更新日期 2026-09-06

国际化：长期约定见 [TECH_STACK.md](TECH_STACK.md#界面国际化2026-09-12)，实施与验证记录见下文「I18N 实施（2026-09-12）」。

## 1. 计划目标

在已经通过的阶段 1 技术基础上，按新方向推进后续工作：

> **产品文档同步 → 专项交互研究 → 阶段 1 第二版原生交互实验与用户验收（已完成）→ 确认正式产品交互基准 → 正式产品开发。**

后续授权补充（2026-09-06）：用户要求先研究阶段 2，经现有代码、SQLite 官方资料、Mem/Joplin 与公开用户反馈核对后，确认“好，做吧”。据此先完成不依赖新版布局的核心数据层，并采纳“删除整条记忆及专属原话、历史进入回收站”的规则；原生交互试用与阶段 3 UI 的 Demo 确认边界保留。

此前文档同步任务已结束。用户在交互研究后明确授权改造阶段 1 代码，让悬浮助手和新流程可以亲手使用。原阶段 1 结论与未测边界继续保留；新增实验与原验收分开记录，不重做已通过的整套验证。

正式产品交付以下闭环：

> 记一下 → 原话落盘 → AI 整理并回执 → 找回记忆、问一问 → 接着想 → 确认结论存回记忆 → 下次在 Memivy 或其他 Agent 中继续使用。

本计划只交付 macOS。Windows、iOS、Android 暂缓，不提前构建空壳、兼容接口或发布流程；Linux 不支持。

## 2. 开发原则

1. **按阶段授权推进**：阶段 1 第二版已通过用户手工验收；阶段 2 已单独获得授权，只交付核心数据层与验证；阶段 3 已按下方授权采用当前 dev 源码为基准；后续阶段仍按各自授权推进；
2. **三份依据各司其职**：PRD 决定功能；官方 `DESIGN.md` 决定 Miro 视觉语言；通过的 Demo 固定 Memivy 的布局、组件外观与状态表现。Demo 不增加 PRD 之外的功能，`DESIGN.md` 原文保持不变；
3. **通过后不重新设计**：正式 UI 复用或对照 Demo 实现；需要改变已确认的视觉或交互时，先调整 Demo 并重新确认受影响部分，不在功能开发时自行换版；
4. 原话落盘优先于 AI；一份 Rust 业务核心、一套数据库规则，UI 和 MCP 不复制记忆逻辑；
5. Demo 的模拟行为不等于技术验证；阶段 1 已通过只证明记录中的技术范围，不代表问答、讨论或正式记忆功能已经完成；
6. Embedding 仅按 2026-09-10 已授权检索增强范围实施；不提前实现同步、任务系统、插件或额外平台适配；富文本仅限 2026-09-08 已授权的 Milkdown 正文编辑范围。

产品取舍以用户喜欢和实际使用为依据，可以借鉴成熟做法；已有竞品提供同类能力不构成取消该能力的理由。开发计划不增加“必须独有”或额外商业验证审批门槛。

## 3. 建议仓库结构

```text
memivy/
├── design-demo/             可点击视觉基准；阶段 0 创建，纳入版本管理
│   └── index.html           Demo 固定入口，必要样式和脚本放在同目录
├── src/                     React + TypeScript 界面
├── src-tauri/               Tauri host 与 macOS 系统接线
├── crates/
│   ├── memivy-core/         业务规则、数据、AI、检索、会话、导出
│   └── memivy-mcp/          macOS stdio 入口，只做协议适配
├── migrations/              SQLite migration
├── tests/
│   ├── fixtures/            搜索与 AI 合同案例
│   └── e2e/                 关键用户路径
├── PRD.md
├── TECH_STACK.md
├── DESIGN.md                GetDesign 官方 Miro 原文
├── AGENTS.md
└── DEVELOPMENT_PLAN.md
```

以上是目标结构，不代表全部能力已实现。当前已有 `design-demo/` 与阶段 1 的 `src/`、`src-tauri/` 和两个 core/MCP crate；后续按需扩展，不重新初始化。候选素材和临时截图仍放在忽略的 `research/`，长期使用的 Demo 保留在 `design-demo/`。

`crates/` 只保留 core 和 MCP 两个 Rust crate，另由 `src-tauri/` 承担原生 host。core 内部按模块分层，不为每个名词拆 crate，不引入服务仓库。

**开发服务 CPU 修复（2026-09-08）：**实测运行二十多分钟的 Vite 占用约 402%～420% CPU；进程采样显示四个 libuv 工作线程持续执行文件 `stat`。本机缺少可选 `fsevents`，同环境探针确认监听器退回 100ms/300ms 轮询；原配置未排除根目录 `target/`（当时 117,327 个文件、约 13 GB）。新增 `vite.config.ts` 显式关闭轮询，排除 `target/`、`src-tauri/` 和 `research/`，并把依赖扫描入口限定为 `index.html`。此配置同时用于正式与样机 dev，Rust 重载仍由 Tauri 负责。

实际验证：`npm run build` 与 `tests/vite-watch.test.mjs` 通过；隔离测试强制不使用 `fsevents`，确认产物目录不被监听，源码修改仍触发事件并使 Vite 转换缓存失效。真实仓库探针监听 26 个目录、147 个条目，5 秒空闲 CPU 约 0.01%，结束后已关闭。macOS 原生监听在 shell 沙箱中返回 `EMFILE`，正常权限重跑通过。采样保存在忽略的 `research/dev-cpu/2026-09-08-vite-sample.txt`。收尾核查时原 Vite/应用进程已退出，未发现新的 Memivy dev 进程；下次启动会载入新增配置。上述探针结果不代表已完成整套原生应用验收。

## 4. 阶段与验收门槛

### 阶段 0：交互研究与可点击 Demo（待后续推进）

目的：研究新方向的使用方式，再让用户看见、点到并确认完整闭环。本次只记录待办，不开展交互定稿或 Demo 修改。

先研究 [PRD.md](PRD.md) 第 12 节的问题，重点是：

- 快捷窗口如何区分记录和提问、保留草稿，并在小窗与主窗口之间继续同一话题；
- 首页、近期话题、会话续接和记忆库的关系，以及空库和少量记录时的首次体验；
- 回答来源、历史变化与新建议的表达；
- 结论核对、修改、保存去向与撤销如何用少量操作完成；
- 未配置、处理中、无证据、取消、失败与重试的完整交互。

研究可以参考成熟产品，先说明采用理由与使用代价，不为追求新奇增加操作。具体方案与页面布局未确认；下表是未来 Demo 需覆盖的能力，不是已经确定的控件设计。

制作方式：

- 依据 `DESIGN.md` 做一套统一的 Miro 风格，不再并列探索其他品牌或另生成设计系统；
- 用轻量 HTML/CSS/JavaScript 与固定模拟数据制作可在本地浏览器运行的 Demo，不先搭正式应用脚手架；
- 主要按钮可点击，页面和状态可切换；不是截图集、配色板或营销落地页；
- 不连接真实模型、SQLite、MCP 或系统快捷键，不收集真实 API Key；模拟行为明确标注。

最少覆盖：

| 界面 | 要看见和试用的内容 |
|---|---|
| 首页与近期话题 | 记录、提问和继续讨论的入口；能从少量已有内容开始使用，不伪造个人洞察 |
| 快捷窗口与回执 | 明确记录/提问意图，主动附带来源；原话保存与整理动作可见、可纠正；小窗与主窗口的上下文衔接 |
| 记忆问答与讨论 | 有来源的回答、继续追问、历史与建议区分；来源查看、取消、失败重试与恢复会话 |
| 结论保存 | 核对和编辑拟保存内容、知道去向、明确确认后更新记忆；回执、纠正与撤销 |
| 记忆库 | 列表与关键词搜索、当前版本编辑、原始输入与来源、历史；创建、删除与取消 |
| 精简设置与菜单栏示意 | BYOM 配置与连接测试状态、MCP 总开关；单篇导出放在记忆页；菜单栏可用/暂停状态 |

用固定场景覆盖首次使用、少量记忆、关键词无结果、问答证据不足、历史变化、普通讨论未保存、确认保存与撤销、AI 等待配置/处理中/取消/失败，以及长中文内容和较窄 Mac 窗口。保持同一套组件，首页与讨论的组织方式由交互研究决定；不加入待办、统计仪表盘或后台定时洞察。

通过条件：

- 用户实际试用“记录 → 回执 → 找回/提问 → 继续讨论 → 确认保存 → 下次继续”，并核对浏览编辑、来源、纠正与撤销，明确表示本版 Demo 通过；仅认可视觉不算交互已确认；
- 首页、快捷窗口、讨论、记忆库、回执和设置的布局及关键状态已确认；约定窗口尺寸下无非预期文字截断、重叠或不可操作区域；
- 保存同一版 Demo 源文件，并在下方记录确认日期、入口、版本及验收窗口尺寸，不另写验收报告；
- 未通过就继续迭代受影响的 Demo；阶段 1 已通过，不需重做。新版 Demo 通过后按用户授权进入后续实施，不代表真实问答、保存或原生能力已完成。

**Demo 确认记录：**Demo v0.1 已制作，入口为 [design-demo/index.html](design-demo/index.html)。2026-09-05 用户反馈：“这个原型看起来还可以，但是有些功能可能和实际功能可能对不上，只是说视觉参考还是可以。”据此记录为：本版可作为视觉参考，功能与关键交互尚未确认；阶段 0 尚未整体验收通过。视觉参考认可日期：2026-09-05；整体验收日期：—；对应提交：—（未提交）；用户验收窗口尺寸：待确认。

后续沿用本版的视觉方向和已选品牌，功能范围与行为以更新后的 `PRD.md` 为准，实现边界以 `TECH_STACK.md` 为准。v0.1 未包含新问答、讨论与结论保存闭环，不把它的模拟按钮和旧布局当作新交互规格。`DESIGN.md` 原文继续保持不变。

**产品方向变更记录（2026-09-06）：**用户确认“记一下、问一问、接着想”、近期话题首页、可浏览编辑的记忆库，以及结论经确认后进入长期记忆。随后要求：“先把相关的文档全部改掉，Demo 先不改……特别是交互上，我们后面还重点研究一下。”本次同步 PRD、技术方案、开发计划和协作规则；Demo v0.1、品牌、应用代码均保留原状。新版 Demo 尚未制作、编号或验收，阶段 2 仍未获授权。下方 v0.1 制作与阶段 1 验证记录均为历史证据，不能当作新方向的实现结果。

**v0.1 制作、研究与验证记录（2026-09-05）：**

- 采用 Miro 白底、黑色胶囊主按钮、黄色品牌与浅色语义块；列表与正文两栏，当前版本在上、原话来源时间线在下，历史按需查看。中文使用现有规范中的系统字体回退；`DESIGN.md` 保持未修改，未创建生产应用脚手架。
- 官方界面与文档研究落实为：[flomo 快捷记录](https://help.flomoapp.com/basic/quick-input.html) 的直接输入、[mymind Quick Note](https://mymind.com/the-new-quick-note) 的留白与轻量展开、[Bear 排版选项](https://bear.app/faq/typography-options/) 的行宽与行距、[Mem 历史版本](https://help.mem.ai/features/version-history) 的明确历史状态、[Capacities 搜索](https://docs.capacities.io/reference/search) 的命中片段层级。结合 Memivy 保留原话、可撤销回执的要求取舍，未照搬热力图、收藏墙、聊天首页或对象管理系统；研究材料与临时证据留在忽略的 `research/`。
- 已在浏览器 1280×720 验证：捕捉关闭与回执、续接后撤销仍保留原话、另存新记忆、删除取消与恢复、编辑形成 v4 与历史 v1 只读、中文多词及特殊字符搜索、AI 失败重试与暂不判断、设置模拟连接测试及 MCP 开关。Node.js 语法检查与 `research/phase-0-regressions.cjs` 的 7 组回归已通过。另已检查 1440×900 主窗口、820×720 长中文与设置、640×720 折叠布局与空库；无页面横向溢出，设置标题与完成按钮保持可见，Enter 保存 / Shift+Enter 换行和返回列表可操作。
- 上述均为 Demo 模拟行为；原生中文输入法、系统快捷键、SQLite、真实模型和 MCP 均未经验证。阶段 0 仍以用户实际试用并明确通过为准。

### 阶段 1：技术风险样机

前置条件：通常要求阶段 0 已获用户明确通过。本轮例外授权记录：2026-09-05，用户在调研与三项决策汇报后确认“好推进做吧，注意只做阶段一哈”。允许沿用 Demo v0.1 视觉先做技术样机；不倒填阶段 0 已通过，不进入阶段 2 或完整业务开发。正式业务 UI 前仍须校正 Demo 与 PRD 的功能交互。

本轮范围：隔离的 `Memivy Phase 1` 应用和测试数据库、最小捕捉与搜索、原生窗口验证、一个最小 `memory_capture` MCP 工具、模型协议探测。暂不实现正式记忆 CRUD/版本/撤销、AI 自动落位、导出备份、开机启动、签名发布。

确认的行为与计时口径：主窗口关闭继续菜单栏常驻；彻底退出后，已开启的 MCP 可保存原话并返回等待整理，AI 消费待处理记录在阶段 5 实现；关闭 MCP 总开关仍拒绝请求。分别测量“快捷键到可输入”和“提交到事务完成”，各自以一秒为门槛，排除用户打字时间与模型处理时间。

目的：验证最可能推翻技术选型的事项，同时检查通过的视觉在 Mac 应用窗口中的可实现性。

工作：

- 初始化 Tauri 2 + React + Rust，在目标 macOS 版本编译和运行空应用；
- 把通过的 Demo 中主窗口和捕捉小窗的最小界面放入 Tauri，验证字体、布局与窗口约束，不重做视觉；
- 从 Rust 创建 SQLite，验证 bundled SQLite、FTS5 trigram、迁移和中文查询；
- 验证菜单栏、全局快捷键、隐藏/唤起小窗；
- 验证中文输入、长文本滚动、窗口焦点和多显示器；
- 用 `rmcp` 做仅含最小 `memory_capture` 的 stdio 程序，写入隔离测试数据库，验证真实 Agent 调用和界面跨入口读取；
- 验证远程和本地 OpenAI-compatible 端点各一次。

退出条件：

- React 界面与 MCP 都能调用同一 Rust core，访问同一个隔离数据库，并发写入不覆盖、不重复已确认的请求；
- macOS 快捷键能可靠唤起并聚焦输入框；
- 中英文搜索样例通过；
- 至少目标 Agent 可调用 stdio MCP；
- 目标 macOS 版本没有不可接受的输入法、焦点或 WebView 问题。

如果 Tauri 的 macOS WebView、窗口焦点或系统能力在这一阶段失败，再重新比较 Electron 或 SwiftUI；不在正式业务开发中同时维护两条技术路线。技术限制若迫使界面改变，先回到 Demo 确认受影响部分，再继续实现。

**阶段 1 验证记录（2026-09-05 至 09-06，用户已验收通过）：**

2026-09-06 用户确认：“不要搞那么复杂，不要过度设计了，我觉得可以通过了。”据此阶段 1 验收通过并结束本轮工作。下列未测项保留为证据边界，不再作为本阶段收尾阻塞，也不记作测试通过；不继续追加验证或进入阶段 2。

| 项目 | 已取得的证据与边界 |
|---|---|
| 范围与隔离 | `Memivy Phase 1.app` 使用独立 identifier 与测试目录；真实原话始终为 pending，无正式记忆版本、删除、撤销或 AI 整理。Demo 原文件与 `DESIGN.md` 未修改。 |
| 真实构建 | macOS 26.6.2 / Apple M4 Pro、Node 24.12.0、Rust 1.98.1；Tauri 2.11.5 + React 19.2.8 编译并打包本地 `.app`，已启动真实 WKWebView。未做签名、公证、分发或其他系统版本验收。 |
| SQLite 与故障 | bundled SQLite 3.53.2 / FTS5 trigram / WAL / FULL；8 项核心测试覆盖原话逐字保留与禁止改写、重试去重、迁移、中英文特殊字符、开关失败关闭、并发写入与模型故障。另以实际进程验证 24 个并发写入、确认保存后 SIGKILL、重开及 integrity_check。 |
| MCP 与跨入口 | rmcp 3.2.0 的 stdio initialize/list/call 与 EOF 退出通过；仅暴露 `memory_capture`，默认关闭、开关即时生效、重复 UUID 不增加记录。真实 Codex CLI 已调用该工具一次，返回 pending 回执，随后在原生 UI 读到同一个 capture ID。MCP 明确要求用户保存意图，但服务端无法独立证明模型是否正确理解同意；客户端仍需按写入工具管理授权。 |
| 搜索样本 | 当前机器 debug 构建、1 万条合成原话、每词 20 次：长中文、两字中文、混合中文、URL 和无结果查询均通过，P95 分别约 17.03 / 15.26 / 20.41 / 1.55 / 0.61 ms。短词扫描全部合格记录后限制返回条数；没有通过截断候选集伪造速度。非长期或真实用户数据基准。 |
| 本地模型 | Ollama 0.32.15 + `qwen3:0.6b`，同一 Rust OpenAI-compatible schema 探测通过，首次含加载约 3.76 秒。仅证明协议路径，未评估 AI 整理质量，不构成默认模型选择。测试服务仅监听 127.0.0.1:11435，模型位于临时目录。 |
| 原生基础 | 已验证长文本滚动与逐字保存、Shift+Enter 换行、Enter 保存、写入失败保留原话及重试只增一条、关闭主窗口常驻、退出重启数据恢复。已修复诊断持锁等待主线程的潜在死锁与主窗口入口返回目标混用，2 项返回目标回归测试通过；用户确认 Esc 返回主窗口正常。 |
| 中文输入与 Esc | 原生逐键输入 `ni` + 空格实际生成“你”；组合态回车不会提前保存，明确提交才从 5 条变为 6 条，Rust 查询核对原话（含输入中已有空格）一致。新面板中再次验证 Esc 先取消候选而不关闭、选词后回车保存“你”且只增一条。Esc 已从 textarea 移到窗口级处理，输入框失焦后仍可退出。仅覆盖当前机器的当前中文输入法，未确认输入法品牌或跨输入法兼容。 |
| 鼠标关闭 | 初期调整窗口焦点未解决偶发双击；随后本机带单调时间戳的事件记录复现 pointerup 先于 pointerdown，与 [WebKit 219670](https://bugs.webkit.org/show_bug.cgi?id=219670) 的 IME/轻点问题相符。收起按钮处理主键释放，并与 click 去重；包含乱序的 5 次原生坐标点击均一次收起，新 NSPanel 上又通过一次。只用于保留草稿的可逆收起操作。 |
| 快捷键与外部全屏 | 用户选择将冲突的 ⌥ Space 改为 ⌃⌥ M，普通 NSWindow 版跨应用实际唤起、直接打字及 Esc 返回连续 3 次正常。外部访达全屏暴露普通 NSWindow 焦点失败，仅加 fullScreenAuxiliary 仍不足；改用固定提交的非激活 NSPanel 后，同一唤起路径在访达全屏取得原生 key-window 与输入框焦点，选词、保存并返回访达通过。该全屏测试使用诊断区 10 秒延迟入口，不是物理快捷键；面板版最终快捷键复测遇到系统锁屏，仍待补齐。当前只检测到 1 块显示器，多屏未验收。 |
| 计时边界 | 最新 NSPanel 的外部全屏单次样本：唤起到 DOM 输入框及原生 key-window 就绪 9.41 ms，Rust 事务提交 1.44 ms，UI 到 Rust 提交确认约 2 ms；来源为访达，入口为诊断验证。历史主窗口按钮样本 44.65 ms；旧 ⌥ Space 冲突样本 2645.83 ms 未通过，不能混入正常热键统计。尚无最终面板版物理快捷键延迟分布，不用上述样本冒充完整性能验收。 |
| 应用图标 | 按用户反馈由选定 SVG 导出铺满画布的不透明底，保留叶子 m 与黄色，由 macOS 26 应用圆角遮罩；已在访达实际验证黄色底贴合系统外框。网页用品牌源文件保持不变。依据 [Apple 图标规范](https://developer.apple.com/design/human-interface-guidelines/app-icons/)。尚未验证旧版 macOS 图标呈现。 |
| 未测项与收尾状态 | 当前单屏，双显示器定位未测；原生收尾遇到锁屏，最终面板版实际快捷键回归未完成，访达仍留在测试时的全屏状态。远程模型私密配置未提供，远程探测未做。用户已知悉这些边界并确认阶段 1 通过，不要求继续补测；阶段 2 未获授权。 |

原始调研、协议回执、进程测试结果和基准输出保存在忽略的 `research/`，运行命令以 `AGENTS.md` 为准。用户密钥不进入仓库、内容库或这些测试报告。

最终检查：最新面板版的前端 TypeScript/Vite 与原生 app 构建、Rust fmt/clippy（warnings 视为失败）、10 项 Rust 测试（8 项核心与模型、2 项窗口返回目标）通过；此前实际进程/stdio 脚本通过，本轮未修改核心或 MCP，未重复该脚本。文档本地链接和 `git diff --check` 通过。临时 Ollama 服务已卸载模型并停止，模型缓存保留于 `/private/tmp/memivy-ollama-models` 供后续复测；样机进程留开，原生界面收尾等待系统解锁。未提交或推送代码。

### 阶段 1 第二版：桌面助手与记忆讨论实验（2026-09-06）

授权：交互研究后，用户要求把现有阶段 1 改成可以亲手体验的形态，并提出桌面常驻悬浮助手；随后明确确认“好，那你就做吧，做出来我看看”。本次直接在原工程上做原生实验，Demo v0.1、品牌与官方 DESIGN.md 不变，原阶段 1 验收记录保留。

已实现的实验范围：

- 桌面叶子 m 常驻入口，点击后同一个 NSPanel 原地展开输入区；回答继续在面板内显示，支持拖动、固定输入、收起与隐藏，保留 ⌃⌥ M；
- 同一输入区明确选择“记一下 / 问一问”；主窗口提供近期话题、会话续接、记忆库和来源原文；展开到主窗口传递话题、输入模式、草稿和所选记忆；
- 原话真实写入 SQLite；模型先生成检索词，再基于有限原话证据回答；引用短编号由核心映射为真实不可改写记录 ID，拒绝越界引用；
- 会话、问题和未确认建议不进入记忆搜索；结论展示拟保存内容和名称，用户可修改后确认“存为一条新记忆”；回执支持撤销，撤销后不再检索或引用该记忆，原确认文本与会话出处仍保留；
- 问答支持处理中、失败、停止、重试和应用退出后的中断状态；原话保存、关键词搜索不依赖模型；设置保留 MCP 总开关；
- v2 不实现正式 memory/version 编辑、AI 自动整理、续写现有记忆、语音、提醒或同步。浏览器只做明确标注的布局预览，写操作须使用 Mac 样机。

**用户验收（2026-09-06）：**模型配置路径与点击/输入焦点问题修复后，用户确认：“好了，上面你给我的检查和测试的点，我都测了，通过了。”据此记录阶段 1 第二版在已给出的检查清单范围内验收通过：原话保存与搜索、桌面小窗与输入操作、配置模型与带来源问答、继续讨论与确认保存/撤销、停止/重新提问与重启保留，以及本轮点击和粘贴回归。以下自动化记录与当时未测项保留为历史证据，不将用户的整体通过反馈扩写成每一种输入法、跨屏或全屏组合的逐项测试报告；不再要求重做已通过的样机验收。阶段 2 的正式数据层仍未接入界面，此确认不表示正式 UI 或后续阶段已启动。

运行与验证（用户验收前的过程记录）：

| 项目 | 证据与边界 |
| --- | --- |
| 构建与自动化 | `npm run build`、`cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings`、`cargo test --workspace --offline` 通过；共 16 项 Rust 测试。新增会话隔离、确认文本与去重、撤销后原话保留、取消/中断后的迟到回答、越界引用、旧数据库迁移覆盖。已重新打包 debug `.app`；新数据库结构的 MCP/跨进程兼容检查使用现有 `verify_phase1.py`。 |
| 真实模型 | 在合成数据上验证检索、两轮讨论、确认新记录和撤销；第一问不预选来源，实际通过检索找到原话。0.6B 模型仅能证明协议，回答质量弱；本次以本机 qwen3:4b 验证交互链路，不作为正式默认模型或质量验收。 |
| 本机运行条件 | Ollama 服务监听 `127.0.0.1:11435`，模型在 `/private/tmp/memivy-ollama-models/`。默认应用模型配置位于 `~/Library/Application Support/com.memivy.phase1/model.json`，权限 0600、无 API Key；已存在的配置不覆盖。服务需要保持运行，样机不自动安装或启动模型服务。 |
| 数据隔离 | 单独模型烟测使用临时数据库；临时联调配置在 `/private/tmp/memivy-v2-runtime/`。正常双击 app 使用既有独立 Phase 1 默认目录；未迁移或改写旧 `research/runtime/` 测试数据。 |
| 界面检查 | 浏览器已检查首页和记忆库。解锁后已打开真实 `tauri://localhost` 样机，验证入口展开、收起按钮和 Esc；原生面板可呈现输入区。用户反馈图标难以拖动，已将整枚图标与面板标题区接入拖动，禁用图片默认拖拽，以 5 px 阈值区分点击与移动，使用系统物理坐标移动 NSPanel。自动化对非激活面板的拖拽及键盘定位不可靠（曾操作到主窗口），因此拖动、实际键入、中文选词、置顶、全屏和焦点返回仍需手动验收，不能以代码或构建成功代替。 |
| 草稿衔接 | 修正浮窗收起后主窗口仍持有旧草稿的问题；通过原生无障碍接口填入合成中文草稿，验证收起后主窗口收到同一内容、再次展开仍保留，测试后已清空。此验证不代表物理键盘或中文输入法测试通过。 |
| 模型配置路径修复 | 2026-09-06 用户在 dev 中反馈保存配置被“仓库以外”限制阻断。核对发现用户主目录存在 `.git`，旧检查错误覆盖标准应用数据目录。现仅对标准 Phase 1 应用目录豁免 HOME 层级的 `.git`；先解析路径，仍拒绝实际项目、worktree、指向项目的软链接及相对/父级路径。新增 4 项回归覆盖首次保存、读回和 0600 权限；全部 6 项 host 测试、Clippy 与原生程序编译通过。使用临时合成配置，未改写用户配置，未代测用户端点连接。 |
| 点击与输入焦点修复 | 2026-09-06 用户反馈按钮难以点击、「接着想」无法获取光标或粘贴。线程采样未发现主线程死锁；隔离原生副本中观察到窗口/页面切换后焦点落到 HTML 容器。移除直接调用 `resignKeyWindow`（Apple 明确将其定义为系统通知回调），使用 `orderOut` 释放小窗焦点后再激活主窗口；收起图标不再成为 key window；主窗口接受首次点击。React 在页面提交后先恢复 WebKit first responder、再聚焦输入框，设置与来源面板打开期间不抢焦点。原生 dev 副本实测通过：保存合成记录、记忆库导航、记忆卡片「接着想」后点击输入并键入/粘贴中文、小窗展开回主窗口、Esc 收起后立即继续键入、打开/关闭设置；未调用模型，临时库与用户数据分离。前端构建、6 项 host 测试、Clippy 通过。自动化直接中文键入与部分剪贴板检测不可靠，中文粘贴以实际输入值验证；中文输入法选词、跨屏/其他应用全屏焦点本轮未重测，不作为已通过。 |
| 当前状态 | 模型配置与焦点修复后的样机检查清单已由用户手工测试并确认通过；阶段 1 第二版验收完成。原生 dev 检查、前端构建、Clippy 与 6 项 host 单元测试的实际结果见上方记录。 |

再次启动本地测试模型可运行：`OLLAMA_HOST=127.0.0.1:11435 OLLAMA_MODELS=/private/tmp/memivy-ollama-models OLLAMA_CONTEXT_LENGTH=8192 OLLAMA_NO_CLOUD=true ollama serve`。模型质量或速度不合适时，在“模型与设置”连接自己的端点。

### 阶段 2：核心数据层

目的：建立可靠的本地记忆与会话数据层，先落实长期记忆与普通讨论的边界。

状态：2026-09-06 已完成核心接口、迁移与数据验证；未接入正式 UI 或 MCP。阶段 1 第二版交互的用户验收单独记录于上一节。

工作：

- 定义 capture、memory、memory_version、版本来源、本地会话/消息/引用与独立配置边界；
- 完成 migration、事务、WAL、busy timeout 和异常恢复；
- 实现原话创建、记忆 CRUD、用户编辑生成新版本、删除与撤销；
- 实现会话持久化与继续读取，但不自动将消息写为 capture 或纳入记忆索引；
- 实现确认结论保存的基础事务、来源关联和重试去重；来源指向稳定版本，删除会话不连带删除已保存结论；
- 实现 Markdown 导出和一致性数据库备份；
- 建立统一错误类型和脱敏日志。

退出条件：

- 强制终止 AI 或应用都不会丢失已提交原话；
- AI 代码没有修改原话的接口；
- 每个当前版本都能追溯到历史版本和来源；
- 普通讨论不进入记忆搜索或 MCP；确认后保存的内容与原消息角色、来源可核对；
- 回答来源被编辑或删除时，不错误指向新版本或伪造可用原文；
- 新旧数据库迁移测试、备份恢复测试通过。

**本轮实现与实测记录（2026-09-06）：**

| 范围 | 实际结果 |
|---|---|
| 正式数据模型 | `memivy_core::memory::MemoryStore` 保存原话、当前记忆、不可覆盖版本、版本来源、回执及每条记忆的变更、会话/消息/引用和确认保存意图。默认正式目录为 `~/Library/Application Support/com.memivy.app/`；测试全部使用临时合成数据，未迁移样机或创建用户正式库。 |
| 写入与撤销 | 原话先独立提交；版本、来源、回执同事务保存。UUID + 请求摘要保证重试一致性；版本校验拒绝过期写入。恢复/撤销追加完整版本，纠正归属的撤销同时恢复两边，任一边已被编辑则整体拒绝，不留下半次变更。 |
| 删除与来源 | 整条记忆和专属原话进回收站，普通搜索与来源解析不可用；共享来源保留，单独删除原话不随记忆恢复。显式清空擦除正文和全文索引项，来源 ID 保留不可用状态；撤销整理保留原话。 |
| 会话边界 | 问题、建议、草稿不入记忆索引。确认文字、名称、目标和原消息角色独立保留；目标失效返回 `needs_review`，不换目标。删除会话后，确认保存和相同请求重试仍正确。引用固定版本；取消/中断/来源失效阻止迟到回答保存为完成。 |
| 搜索基础 | 一个 FTS5 trigram 索引随事实表写入/擦除维护；短词使用字面匹配。中文、英文、特殊字符、标题与原话组合、旧版本和回收站排除测试通过。排序权重、来源/时间/项目过滤和索引重建入口仍在阶段 3。 |
| 迁移与恢复 | 正式 v1 → v2 保留已有原话，DDL 中途失败完整回滚；拒绝 Phase 1、无关和较新数据库。SQLite backup API 生成一致性备份，恢复到新空目录；版本、回收站、会话和重试记录可恢复。Markdown 同一读快照导出完整有效历史与来源，会话单独标识；备份/导出均不包含独立配置密钥。 |
| 自动检查 | `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings`、`cargo test --workspace --offline` 通过。共 35 项测试：19 项新数据层测试、原有 14 项 core 测试及 2 项 host 测试；`npm run build` 通过。 |
| 真实进程 | `cargo build -p memivy-core --example memory_probe --offline` 后运行 `python3 scripts/verify_memory_store.py` 通过：24 个独立进程保存；8 次同请求并发重试只产生一条；原话及版本/回执提交后实际 SIGKILL 仍恢复；另一个进程持续写入期间备份一致；最终 47 条合成原话、1 个版本，SQLite 3.53.2，integrity 与 foreign-key 检查通过。 |
| 范围边界 | 新接口不调用模型。未切换现有 UI/MCP、未修改 Demo、品牌或 DESIGN.md；未做本轮原生交互、真实模型质量或物理断电测试，未生成新原生包、提交或推送。正式 UI 与 MCP 接入、完整检索排序及 AI 整理由后续阶段完成。 |

**同日 Review 修复与提交前验证：**

- 修复永久删除的隐藏副本：删除原话时同步擦除关联的已撤销版本及 FTS 正文，支持显式清空已撤销记忆；备份正文检查、失败回滚、共享内容保留及重复清空通过。
- 修复撤销纠错后的操作中断：新回执指向恢复后的归属和新版本；新建/已有来源与目标的四种组合均连续纠错、撤销三轮，重试去重和后续编辑保护通过。
- 修复并发首次打开误报 `Schema`：初始化元数据在同一读快照读取，写锁内再次核对。100 轮、每轮 4 个连接的回归测试通过；真实进程验证新增 8 个空库、每库 4 个进程同时打开，正常锁竞争可重试，未再误报格式错误。
- 提交前 `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings`、`cargo test --workspace --offline`（44 项：24 项正式数据层、14 项原有 core、6 项 host）、`npm run build` 通过；`python3 scripts/verify_memory_store.py` 的首次打开、并发写入、去重、SIGKILL 和在线备份验证，以及 `python3 scripts/verify_phase1.py` 的原有进程/stdio MCP 验证均通过。测试仅使用临时合成数据；本次数据修复未改变已验收的原生交互。

### 阶段 3：按新版 Demo 实现首页与记忆库基础界面

目的：把确认的主窗口基准接到真实本地数据，让没有模型的用户也能完整记录和查找。

**本轮授权与基准（2026-09-06）：**用户先要求研究现有 dev、行业实践和竞品，并明确不另写方案文件；随后选择“采用紧凑列表＋宽正文”，澄清“是我们 dev 环境的代码，我可能没有 build 吧”，最后确认“好，实现吧”。据此沿用当前 dev 源码的首页、Miro 视觉、品牌与基本组件，仅将记忆库调整为确认的布局并接入正式 MemoryStore。此为明确的阶段 3 开发授权，不倒填旧 HTML Demo 整体验收通过，也不增加一次 Demo 审批。首轮未包括阶段 4 快捷入口、阶段 5 正式 AI 闭环和阶段 6 MCP 接入；随后用户要求恢复已有问答与讨论，范围修正见下文。

工作：

- 按上述当前 dev 基准实现首页的本地近期话题、记录入口、记忆库、关键词搜索、当前版本编辑、来源、历史与设置，复用组件；
- 以确认过的布局、窗口尺寸、空状态和无结果状态为基准，接入真实本地读写；依据后续用户纠正，恢复已可用的问答与讨论体验，直接接到正式数据与配置的模型，不运行模拟回答；
- 完成 FTS5 trigram 索引、标题权重、来源/时间/项目过滤；
- 小于三个字符的查询采用受限普通匹配；
- 对索引提供删除后重建能力。

退出条件：

- 未配置模型时，创建、编辑、历史、搜索、导出全部可用；
- 中文短语、英文词和 URL 搜索案例通过；
- 关键词搜索不产生任何模型请求；
- 索引损坏或删除后能从事实表重建；
- 在当前 dev 的 1140×790 主窗口基准下实测，并核对 760×560 最小窗口的列表/正文切换；使用相同品牌与组件，不自行改变首页布局。


**本轮实现与实测记录（2026-09-06）：**

| 项目 | 实际结果与边界 |
|---|---|
| 正式入口与隔离 | 默认 `npm run dev:app` / `Memivy.app` 使用正式 `MemoryStore` 与 `com.memivy.app/memivy.db`；旧样机通过 `npm run dev:prototype` 保留，MCP 仍用 Phase 1 Store，无自动迁移。浏览器仅显示标明身份的固定布局示例，写入返回不可用提示。 |
| 首页与记忆库 | 沿用当前 dev 的首页与最近记忆卡片；正式库为空时显示真实空状态。记忆库采用紧凑分页列表＋宽正文，包含来源、项目、更新时间过滤、命中原文片段、当前内容、原话与来源、版本历史及回收站。已有正式话题可分页只读；未伪造话题、AI 回答或整理回执。 |
| 版本与草稿 | 原话保存后返回确认；手工编辑创建版本并显示可撤销回执。旧版本恢复会新建版本，陈旧编辑拒绝覆盖并保留草稿；首次版本撤销返回原话。草稿写入独立表，跨导航排队、重试，退出前等待落盘；永久擦除也清理对应草稿，并拒绝迟到写入。 |
| 搜索与数据检查 | 新增 `memory_library` 8 项回归全部通过：标题排序、限量前过滤、分页、中文短词/URL/字面特殊字符、真实来源片段、版本并发保护、草稿不进入搜索/MCP/导出、删除状态隔离、缺失索引重建。最后的独立删除来源边界修正后重跑 8 项通过。 |
| 全部工程检查 | `npm run build`、`cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings` 通过。`cargo test --workspace --offline` 共 52 项通过（含正式数据 24 项、库查询 8 项、样机/模型/host 相关检查）。过程中修复了双 `generate_context!` 导致 dev/test 重复嵌入 plist 的问题，正式与样机现共用一次 context 生成。 |
| 真实进程检查 | 构建 core 两个 probe 与 MCP 后，`verify_phase1.py`、`verify_memory_store.py` 均通过：跨进程并发/幂等、确认落盘后 SIGKILL、版本/回执保留、在线备份、SQLite 完整性和 MCP stdio。使用临时合成数据，无真实模型请求。 |
| 原生与视觉 | 已构建并启动未签名 debug `target/debug/bundle/macos/Memivy.app`。在显式临时正式目录中实际完成中文原话保存、手工编辑、原话逐字核对、来源关键词找回、版本历史展示、首次编辑撤销、⌘N/⌘K/⌘S、退出重启草稿恢复、原生目录选择导出。1140×790 原生布局及 760×560 浏览器列表/正文/返回通过视觉核对；编辑框在草稿就绪后获取焦点。 |
| 导出与设置 | 原生导出产生 memories/captures/conversations Markdown 与 manifest，逐项核对包含保存的正文及原话，未提交草稿不在导出中。设置提供索引重建与独立模型配置/固定文字连接测试；配置损坏不阻断记录和搜索，重新填写可显式替换不可读配置。未代测用户模型端点或改写用户凭据。 |
| 证据边界 | 原生中文粘贴以实际输入值核对；部分自动化剪贴板调用报超时但文本已输入，不按工具返回单独判定。未逐项实测物理中文输入法候选词、全屏/多屏或大规模长时性能；回收站恢复、永久擦除、陈旧版本与索引重建的核心行为由回归测试覆盖，未将它们全部标记为原生点击实测。原型此前验收保持，不重新追加原型体验验收。 |

**导出交互修正（2026-09-06）：**用户明确要求“就导出一篇……想导出的时候就导一下”。据此移除设置里的整库导出与对应原生命令，改为每篇记忆折叠“更多”菜单中的“导出 Markdown”，保存为单个 Markdown 文件，仅包含该篇已保存的标题与当前正文；未整理原话也可单篇导出。正在编辑时需先保存，再导出。上表整库导出是首轮历史实测，不再代表当前 UI。单篇导出新增 2 项回归，连同原记忆库 8 项共 10 项通过，覆盖仅一篇正文、原话保真、原子替换、旧版本与回收站拒绝导出；原生独立测试副本已验证“更多”展开、原生保存窗口与中文文件名；实际文件逐字等于该篇已保存标题和当前正文，权限为 0600，未包含其他文章、原话历史或讨论；设置页已无导出入口。

**已有讨论功能回退修正（2026-09-06）：**用户指出“问一问”被禁用、记忆里的直接讨论入口消失，明确要求保留此前可用的体验。原先将正式问答全面推迟的边界在此范围内被用户修正：恢复首页提问、从记忆“接着想”、近期话题续接、可打开的版本引用、取消/重试、经确认保存新记忆及撤销；接到正式 MemoryStore，不把旧样机数据库混入正式搜索。导出作为附属操作收进“更多”，主操作仍是编辑与接着想。正式自动整理、桌面助手与 MCP 接入仍未扩入。新增 3 项讨论回归通过（固定本地协议测试端点）：版本依据、取消/删除/非法引用拒绝迟到完成、讨论不自动写记忆、明确确认后保存、草稿与选定依据重启保留、近期与更早消息分页。在独立原生测试副本和临时正式数据中，实际完成首页问一问、从记忆接着想、两轮回复、打开版本依据、停止、恢复问题后重试、审核并修改结论后确认保存、回执与撤销。提问不增加原话；确认保存才新增，撤销后保留确认原话。前端构建、Rust fmt/Clippy 和全工作区 57 项测试通过，正常 Memivy debug 应用包已重新构建。原生协议验证使用固定本地测试端点，未验证用户模型的实际回答质量；中文粘贴与输入操作通过，未逐一测试中文输入法组合状态。自动审批因可能丢失未保存状态拒绝退出用户当前应用，因此保留其窗口，另建独立 bundle identifier 完成测试；当前已打开的旧窗口尚未重启，不将新构建等同于该窗口已更新。

本轮未提交或推送，未新增方案文件。以上是工程实测结果，尚未记录用户对阶段 3 的最终验收；阶段 4/5/6 不因此获得授权。

**弹窗与页面标题修正（2026-09-06）：**根据用户反馈，正式共用弹窗补齐遮罩点击与显式 Esc 关闭；嵌套弹窗停止事件冒泡，只关闭最上层，正文内留白与从内部开始的拖选不触发遮罩关闭，保留已有保存中保护。去掉重复的顶部“我的记忆 / …”整栏，沿用各页面自身标题；模型配置状态合并到左下角设置入口。参考 [W3C 弹窗交互](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/) 和[面包屑适用范围](https://www.w3.org/WAI/ARIA/apg/patterns/breadcrumb/)。浏览器实际验证输入框内 Esc、嵌套 Esc、焦点恢复和 760×560 首页/记忆正文；独立原生测试副本实际验证设置遮罩/Esc、内部留白不关闭、嵌套确认框遮罩/Esc 仅关闭顶层、引用遮罩关闭、结论正文输入框 Esc 关闭，1140×790 讨论布局已目视核对。测试未提交设置、索引或结论写入；未逐一测试中文输入法组合态和拖选手势。前端构建与原生 debug 构建通过，本轮不涉及 Rust 数据行为变更。

**讨论排版与首次点击修正（2026-09-07）：**移除讨论消息复用旧卡片样式造成的正文贴边，统一标题、消息与输入框的左右留白，区分提问和回答，并调整段落、引用、输入区间距及窄窗口样式。在隔离原生应用中复现输入框聚焦后首次点击无效，临时事件记录确认收到 `pointerup → mouseup → pointerdown → mousedown` 而没有 `click`，与 [WebKit 219670](https://bugs.webkit.org/show_bug.cgi?id=219670) 的输入法相关事件乱序一致。正式界面共用层仅对聚焦编辑器后、同一按钮/菜单/链接的短时同位置逆序手势恢复一次点击；普通点击、键盘、拖动、修饰键和失效控件保持原行为，并防止迟到原生 click 重复执行。新增 `npm run test:ui`，12 项回归通过，覆盖恢复、普通/迟到点击、连续手势、键盘、拖离、失效控件和窗口失焦。原生实测首页“记一下/问一问”、侧栏话题切换、记忆库搜索结果、更多菜单、设置关闭均可一次点击生效；设置复选框原有单次点击正常，未提交设置；未发送的讨论草稿切换后仍保留。常规窗口与接近最小尺寸的讨论排版已目视检查，最后一轮滚动复查因 Mac 锁屏中断；未逐一覆盖物理输入法组合态。临时诊断代码已从正式入口移除，`npm run build`、正常 `Memivy.app` debug 构建与 `git diff --check` 通过。测试仅用隔离合成数据，本轮未更改 Rust 数据逻辑。

**审查问题修复（2026-09-07）：**修复四处跨页面问题：撤销回执绑定目标记忆；草稿按记录串行写入并同步当前页面，发送完成只清空匹配请求的草稿，迟到回调不能覆盖新输入；单条提交只等待自己的草稿，退出时仍检查全部未保存内容；加载历史以当前可见消息为锚点保持位置，仅新消息/回答状态变化触发底部滚动。新增 12 项队列与受控组件生命周期/IPC 回归，连同原有点击测试共 24 项通过，覆盖新草稿在迟到回调前后及清空 IPC 期间的保护、失败重试、跨记录撤销和历史定位。隔离原生副本实际完成 A 编辑→切 B 无误带回执→回 A 撤销，退出后核对 A 恢复、B 未变；无效标题草稿存在时首页捕捉成功，返回仍保留失败草稿，修正后可正常落盘退出；讨论草稿跨页面及退出保留。25 轮讨论加载第 1–5 轮后，第 6 轮保持原屏幕位置；常规与窄窗口已目视核对。原生测试使用临时合成数据，无模型请求；迟到请求的精确时序由受控回归覆盖。前端构建、正常 Memivy debug 应用包构建和 `git diff --check` 通过。本次不涉及 Rust 数据逻辑变更。

### 阶段 4：macOS 快捷入口与捕捉

目的：沿用已验收的原生基础和当前 dev 视觉，将快捷入口接入正式 MemoryStore。

**授权与交互确认（2026-09-07）：**用户要求先研究行业实践与竞品、汇报关键决策，随后表示“好，同意。你可以开始做了吗？”，授权本阶段实现。沿用当前源码的 Miro 视觉与品牌，不重做旧 Demo。确认 ⌘Enter 提交、Enter 换行；原话保存后普通窗口收起，固定窗口保留并清空；收起时回答继续生成，不自动弹回。借鉴 [Drafts 捕捉窗口](https://docs.getdrafts.com/docs/extensions/capture-window) 的低打扰捕捉、[Raycast Quick AI](https://manual.raycast.com/ai/quick-ai) 的对话展开，以及 [Apple 菜单栏规范](https://developer.apple.com/design/human-interface-guidelines/the-menu-bar)；避开 [VoiceOver 的 ⌃⌥M](https://support.apple.com/en-gb/guide/voiceover/mchlp2748/mac)。具体规则已更新至 PRD 5.1。

已实现：

- 正式 NSPanel、叶子 m、单色菜单栏图标、可配置全局快捷键（默认 ⌃⌘M）、固定/拖动/隐藏/暂停，以及读取真实系统状态的可选登录启动；
- 正式 capture/question 独立草稿，逐字原话和可移除应用来源，用户主动附带链接/路径；保存失败保留、请求幂等、跨窗口比较后写入与冲突核对；
- 小窗复用正式问答、引用、取消/重试、继续讨论和确认结论保存；后台任务归 host 管理，收起不取消；
- 小窗到主窗口交接同一话题、引用及草稿，接收窗口准备完成才收起；过期的收起/交接事件不能关闭后来打开的面板；
- 主窗口关闭前保存草稿再隐藏，退出时等待所有窗口确认；通过 [AppKit applicationShouldTerminate](https://developer.apple.com/documentation/appkit/nsapplicationdelegate/applicationshouldterminate(_:)) 接住系统退出并延后答复，失败或未完成弹层会保留窗口，未响应不会强制退出；
- 工作区边界与断开显示器后的位置约束；菜单/窗口操作统一主线程，借用既有 NSPanel 和依赖，无新后台服务。

本阶段继续排除自动 AI 整理和正式 MCP；样机、旧 Demo、官方 DESIGN.md 和品牌原件保持原样。原话保存后显示本地成功反馈，不将其伪称为 AI 整理回执。

实际验证（2026-09-07）：

- 前端构建、29 项 UI/草稿测试、62 项 Rust 工作区测试、fmt 和 clippy（warnings denied）通过；新增跨窗口草稿、来源精确保留、重复提交、叶子辅助功能激活、拖动与按键/IME 防误交测试。文档本地链接与 diff 空白检查通过。
- unsigned debug `Memivy.app` 已实际构建并运行；测试库 `/private/tmp/memivy-phase4-qa/`，受控本机模型桩，仅发送合成测试文本。先发现默认实例启动参数未传递，改用 macOS `open --env` 并核验空库后才进行写入；没有在真实资料库中创建测试记录。
- 原生实测：中文多行原话、Enter 换行与 ⌘Enter 提交；普通保存收起、固定保存保留且输入恢复；退出后草稿恢复；显式来源链接随草稿交接；当前回答、引用及续问草稿一起展开主窗口。引用弹层内容与实际原话一致，Esc 只关闭弹层。
- 受控模型延迟 12 秒：发送后收起，回答完成且不自动弹窗；再次打开仍在原话题，数据库没有新增长期记忆。
- 用户实际按 ⌃⌘M 后反馈“能正常打开”，完成物理全局热键核验；自动化按键没有触发系统热键，未拿自动化结果替代物理验证。
- 原生入口请求到可输入的两个样本为 307ms、277ms；一次从提交动作（含草稿 flush）到原话事务完成 4ms。入口样本来自主窗口按钮，不是物理热键的独立耗时测量；只属于本机本轮样本，非长期性能分位数。
- 最终构建复验：关闭主窗口后仍有叶子入口；叶子原生点击能恢复同一话题，取消固定后的展开也保留续问草稿。暂停/隐藏及恢复操作实际写入设置并反映在界面。修正保存后重新获得输入焦点、样式加载顺序、叶子辅助功能点击，以及交接过程中失焦的竞争。
- 退出复验发现标准 ⌘Q 会绕过 Tauri 的 ExitRequested，已增加 AppKit delegate 的延后退出答复。最终原生实测：结论确认弹层打开时 ⌘Q 不退出，Esc 关闭后可见取消退出提示；输入新的续问草稿后立即 ⌘Q 能退出，随后从 SQLite 核对全文已保存，长期原话仍为 3 条。新 delegate 保留 Tao 原有方法与布局，clippy 和应用包构建通过。测试模型桩已停止，当前打开的新版已回到正式资料库，未写入测试内容。
- 登录项查询实际返回应用包不可用；当前构建不在应用程序目录，未开启登录项或执行真实重新登录。多显示器/显示器拔出、外部全屏应用、物理中文输入法组词，以及菜单栏图标的完整点击流程还未作为真机通过项记录。

尚未记录用户对阶段 4 整体的最终验收；物理热键反馈仅确认该单项，不扩展为整体验收或后续阶段授权。

**开关卡顿修复（2026-09-07）：**用户反馈快捷键唤醒和点叉收起很卡，授权定位并修复。原生分段计时发现 `snapshot` 每次同步查询 `SMAppService.status`，单次 122–131ms；一次打开、收起各重复查询 3 次，全局热键分支还额外构造快照。已从窗口状态中拆出登录项查询，仅设置页打开/回到前台时在工作线程按需读取；原生开关分支改为直接读取所需窗口字段，设置页拒绝迟到状态覆盖新操作。收起前仍等待草稿落盘。

- 同一隔离库、同一已有话题，修复前 2 次打开到原生输入就绪为 431/417ms，点叉处理（含草稿 flush）389/387ms；修复后 4 次打开为 69/22/23/29ms，点叉为 8/19/12/14ms。窗口状态快照从约 125ms 降至 0–2ms；开关过程中不再查询登录项，进入设置后单独查询 127ms 并返回真实状态。
- 上述打开样本通过主窗口快捷入口按钮调用同一原生打开函数，未当作物理热键端到端耗时；收起数据截止到原生处理完成，不包含下一帧显示扫描。样本为本机 debug 包，不代表长期性能分位数。
- 原生复验通过：连续开关、输入后立即点叉再打开，讨论草稿全文和焦点恢复；记一下模式保留多行原话及显式来源 URL；原有话题、回答仍在。数据库核对长期原话仍为 3 条，未产生测试记忆写入正式资料库。
- 本轮 31 项前端测试、3 项桌面 Rust 测试、fmt、clippy（warnings denied）和原生应用包构建通过。新增慢登录项查询不阻塞其余设置、迟到响应不能覆盖登录项操作、窗口状态刷新不触发登录项查询的回归覆盖。已在正式资料库打开修复版，请用户复核物理热键与点叉手感；尚未把待反馈项记为通过。

**阶段 4 审查修复（2026-09-07）：**修复连续空白交接到主窗口后沿用旧来源的问题：空白默认值随当前来源更新，已经开始编辑、恢复或正在写入的草稿仍保留自身来源和显式 URI，成功保存后下一次空白输入采用当前来源。窗口布局改为先计算完整目标矩形，再提交缩放与移动；不再在 Tao 异步缩放后立即读取旧尺寸。展开、收起、回执与默认叶子均复用目标尺寸计算，保留工作区边界与锚点规则。

- 新增 3 项来源生命周期和 2 项窗口几何回归，34 项前端测试、5 项桌面 Rust 测试、类型检查、fmt、clippy（warnings denied）与 debug 原生应用包构建通过。几何测试覆盖旧叶子/回执/输入/讨论尺寸、回执消失后锚点恢复、不同缩放比例和负屏幕坐标；不将计算测试视为跨屏真机验证。
- 原生测试使用全新隔离库 `/private/tmp/memivy-review-fixes-qa/`。连续空白交接后输入并保存两条合成记录，SQLite 核对来源依次为 Google Chrome、Memivy，草稿已清空；原生展开、交接与收起后的 72×76 叶子已目视核对。未调用模型，未向正式资料库写入测试内容；测试结束后已重新打开使用正式资料库的修复版。跨显示器拖动/拔出仍未实测，未扩大阶段整体验收结论。

### 阶段 5：AI 整理、记忆问答、继续讨论与结论保存

目的：完成用户在 Memivy 内“记一下、问一问、接着想、确认保存”的完整闭环。

**调研后决策确认（2026-09-07）：**用户同意前两项建议，并明确系统仍在开发阶段，无需考虑旧数据：

- 自动续接采用补充和局部更新，保留未涉及内容，不自动做整篇压缩重写；明确改变的判断可以更新，犹豫和质疑保留不确定性。
- 结论存入已有记忆时默认补充；需要融合成新的当前理解时，先展示融合结果，由用户一次确认后保存。
- 不安排旧数据兼容、历史记录批量整理或升级迁移交互。

**实现授权（2026-09-07）：**完成剩余问题核对后，用户要求开始写代码，阶段 5 开始实现。沿用当前正式 UI、布局和共用组件，以此前已接通的问答、讨论与新建结论保存为基础；正式 MCP 仍属于阶段 6。实际验证结果在完成后记录，不将授权视为验收。

先接入捕捉后的整理：

- BYOM 设置、连接测试、Key 本机文件保存与日志脱敏；
- 捕捉提交后创建持久 pending 状态；
- FTS/当前项目/最近记忆生成有限候选；
- 一次模型调用返回动作、目标、新建正文或续接的补充/局部修改、标题和检索词；核心基于所用版本保留未涉及内容并生成新版本；
- 严格结构校验后在一个事务中应用动作；
- 实现回执、撤销、改为新记忆和失败后重试；
- 应用启动时恢复被中断的 pending/processing；
- 用真实 AI 状态驱动 Demo 已确认的回执、纠正与错误界面。

再接入问答、讨论与结论保存：

- 同一 BYOM 配置下，结合问题与有限会话上下文生成检索词，由共享 core 检索有限记忆并生成带来源的回答；
- 区分回忆与新建议，引用绑定实际使用的版本/原始输入，处理无证据、旧判断变化与来源不可用；
- 接入首页、快捷窗口和单条记忆的提问与继续讨论，支持本地会话续接；
- 实现处理状态、取消、超时、失败重试、中断恢复与迟到结果隔离；
- 按用户确认的内容和去向保存结论，复用原话落盘、版本、回执、纠正与撤销，防止普通对话自动进入长期记忆；
- 补齐已有记忆作为结论保存目标：默认补充原正文；融合需先展示结果再确认，准确保存确认文本与最终版本正文；
- 结合固定案例核对召回与回答质量，记录 FTS 的真实漏搜，不预先增加 embedding；
- 所有界面与操作按新版已确认 Demo 对照，移除模拟路径。

退出条件：

- 三种动作均有可见回执和确定的数据结果；
- 自动续接保留未涉及正文和原意，不将犹豫或质疑改成已确认事实；已有记忆的结论补充不覆盖旧正文，融合后的版本与用户确认结果一致；
- 非法输出、超时、限流、端点离线时只改变处理状态，不损伤原话；
- 撤销能恢复 AI 动作前的当前版本；
- 相同 job 不会因为重启被应用两次；
- 关键词搜索仍无模型请求；问答只读取有限相关内容，回忆性陈述与引用相符，证据不足时明确说明；
- 多轮追问可接续上下文，取消或失败不制造成功回答、不写记忆；
- 结论明确确认前不创建 capture，确认保存可追溯、去重、纠正和撤销；目标已改变时不静默覆盖；
- 在应用内部完成“记录 → 问答 → 讨论 → 确认保存 → 再次使用”，不依赖外部 Agent 才能成立；
- Key 不出现在日志、SQLite 内容库、Markdown 和备份元数据中；
- 整理、问答、讨论、结论保存及失败状态均与新版 Demo 对照通过，结果来自真实处理，不保留 Demo 模拟逻辑作为运行时回退。

**阶段 5 实现与实际验证（2026-09-07）：**

- 已接通正式 MemoryStore 自动整理：捕捉与 pending 任务原子落盘；单个后台消费者用有限候选执行一次模型调用；new/append/defer、局部更新、派生关键词、重试、回执、纠正与撤销均由 Rust 核心处理。未配置模型不阻塞捕捉和搜索，确认的讨论结论不再自动整理。
- 问答按多个独立关键词合并排名后限量，回忆逐段关联引用，保存实际证据版本及节选位置，区分当前/历史记录和新建议。补齐已有记忆作为结论目标、默认补充、融合预览与准确保存；过时目标保留确认原话并返回可纠正状态。
- Rust 工作区测试 **74 项通过**；UI 行为测试 **37 项通过**。新增覆盖任务抢占/恢复/去重、手动归属与模型竞争、非法局部修改、版本变化/删除、关键词重建与清理、无证据/无引用、长文实际命中片段、历史版本、结论追加/融合/撤销、过期预览与目标加载失败。`cargo fmt --all -- --check`、全目标 Clippy（warnings denied）、前端构建及原生 debug bundle 已执行。
- `scripts/verify_organization_recovery.py` 实际强杀已认领任务的进程，重启后沿用同一 attempt，仅生成一个版本与回执，原话逐字保留；重新执行不重复应用。`scripts/verify_memory_store.py` 的并发写入、原话/版本强杀恢复、在线备份与完整性检查继续通过。
- 固定合成整理集位于 `crates/memivy-core/tests/fixtures/organization_cases.json`：16 个 new、16 个 append、8 个 defer。当前本机配置的 `gpt-5.6-luna` 首轮 **36/40**，其中两次 HTTP 500，两次因缺失指代而错误续接。收紧“候选次序不能解释第二个/刚才”等规则后，全组重跑 **40/40** 通过动作、目标和事务校验；本轮中位耗时约 **4.67 秒**，最大 **8.63 秒**。人工核对了变化、犹豫、否定、人物区分与提示注入案例的正文，未发现将假设变成确定事实；这不是对任意用户资料或其他模型的准确率保证。
- 真实问答链使用独立合成库：当前判断、过去变化、收费追问、新建议、无个人经历证据共 5 轮，以及一次融合预览。早期一次服务端失败；另一轮观察到组合长词造成漏搜，改为独立主题词/关注点后重跑，前 4 轮均找到相关版本，最后一轮无证据时不编造经历。所有问答与预览均未写入记忆。原始结果保存在忽略目录 `research/phase5-eval/`，可通过 `intelligence_probe`、`discussion_probe` 复核。
- 原生实测采用 `com.memivy.phase5qa` 独立构建和 `/private/tmp/memivy-phase5-native` 合成库，明确环境变量后确认空库才输入测试文字。已看到保存后 processing、新建/补充回执、前后正文、撤销后的原话、按原话另存、从记忆讨论、逐段引用及当轮节选、结论选择已有目标、融合预览、手动修改后准确保存，并在重启后找回记忆与话题。确认前仅有 2 条捕捉；确认后恰好增加 1 条结论来源，最终版本与审核文字逐字相同。测试库检查未含配置中的 Key。后续真实端点失败时，原生界面显示保留原话和重新整理入口，点击后回到 processing，最终返回 defer，未新增重复捕捉。默认追加已有记忆也在原生端复核，结果等于此前全文加上审核的结论；保存与撤销均返回真实回执。最终测试窗口已关闭。
- 原生启动曾受 shell 沙箱阻断；早期仅改 plist 的副本被 CUA 自动启动到默认数据上下文，未在该窗口输入或执行内容动作，核查默认库没有整理任务。之后改用独立 Tauri identifier 和明确临时数据环境，并验证空库后继续。正常运行窗口需要重启到本阶段构建；不声称旧进程已加载新代码。
- 验证边界：正式 MCP、签名发布、跨设备、embedding 均未实施；本阶段复用了阶段 4 的快捷窗口与交接，未将本轮验证描述为再次完成全套物理快捷键、登录启动和跨屏测试。真实端点可能限流/离线或返回服务端错误，失败时需用户重试；保留原话不等于保证模型每次整理成功。当前记录是开发验证，不代替用户验收。


**阶段 5 Review 修复与复验（2026-09-07）：**用户要求“修复这些问题”，本轮修复审查中确认的 7 处缺陷：

- 保存目标冲突时，完整审核融合稿与确认标题/去向一起持久保存（schema 5 增加 `merged_body`）；从原话页可查看、审核并明确另存，重启不丢失。保留稿不进入普通检索，擦除原话时一并清理。取消了冲突回执中跳过完整稿核对的直接另存入口。
- 整理回执绑定记录与刷新版本；切换、读取失败和组件卸载后，旧按钮与迟到回调不能操作或打开上一条记录。
- 多查询合并保留各次命中的原话集合；长文按查询覆盖与标题权重选取连续片段，并保留原文 Unicode 偏移。新增端到端回归验证“主题命中当前版、收费命中原话”以及长文靠后的收费事实实际进入模型证据，查看引用返回同一片段。
- 后台失败状态写入遇到数据库锁时保留待写终态并退避重试，不再丢弃第二次 Busy；锁释放后可正常重试整理。候选同时携带项目来源，筛选和事务提交均阻止已标记不同项目的自动续接。
- 真实评估仅接受全新且不存在的目录，原子创建，不能跳过旧结果并改写模型名称；保存模型、配置开关、端点摘要、固定输入与实现代码摘要。进程实测用不可调用的假模型和已有成绩目录，确认直接拒绝，原 summary 不变，也未写入新案例。
- 当前 Rust 全工作区/全目标 **81 项通过**，UI 行为 **41 项通过**；前端构建、Rust fmt、Clippy（warnings denied）和默认原生 debug bundle 通过。Phase 5 强杀/恢复/幂等以及 Phase 2 并发写入、备份、完整性进程检查继续通过。
- 使用独立 `com.memivy.phase5fixqa` 构建和 `/private/tmp/memivy-phase5-fixes-native` 合成库，在原生窗口查看完整保留稿，修改中文及换行后确认另存，再撤销并重启。数据库核对最终正文逐字等于审核输入；捕捉总数仍为 2，短结论与并发修改的目标正文都未改变；重启后原审核稿仍可展开读取。测试窗口已退出，正常用户进程未重启。
- 使用当前本机 `gpt-5.6-luna` 在全新目录重跑 40 条固定案例：**38/40** 通过动作、目标与核心写入校验，中位耗时 **4.72 秒**。另 2 条（new-03、new-06）动作正确但模型返回空标题，被核心拒绝写入；未将旧的 40/40 成绩算作本轮结果，也未把失败解释为成功。证据保存在忽略目录 `research/phase5-fixes/`，其中不包含配置中的 Key。真实模型输出仍存在波动，这份开发验证不代替用户验收。

### 阶段 6：MCP 与 macOS 外部测试版

**授权与范围（2026-09-07）：**用户先要求充分研究并讨论决策，明确“支持 MCP”而不设客户端白名单、不支持旧 Mac、目前没有签名条件；审阅简要方案后以“好，实现它”授权实施。沿用当前正式 UI 与共享核心，仅增加两项 stdio 工具及必要设置。开发包面向 Apple Silicon / macOS 26，采用 ad-hoc 签名，Developer ID 和 notarization 明确延期；不为延期增加云端服务、自动更新或旧数据兼容流程。

**状态：**开发实现与下列本机验证已完成；另一台干净 Mac 和 10–15 人连续试用尚未执行，不据此认定整个 Mac MVP 已通过外部验收。

交付行为：

- 默认 `memivy-mcp` 使用正式 MemoryStore，与原生 UI 共用事务、UUID 幂等、来源和整理队列；历史原型单独使用 `memivy-mcp-prototype`，数据仍隔离。
- 只公开 `memory_capture` / `memory_search`。保存只接受 Agent 来源的明确授权原文，Agent 名必填，项目/URI 缺省为空；先落盘再回执，应用负责后续 AI 整理。
- 搜索不调用模型；非空关键词、默认 5 条、最多 8 条，无分页。复用 FTS5/短词字面搜索、先过滤再限量，结果绑定实际命中的原话或当前版本；不返回未保存会话、草稿、已删除记录或旧版本正文。
- 总开关默认关闭。每个数据调用都受跨进程锁与即时开关检查约束；关闭成功后不再接受新数据调用。协议发现与不读记忆的诊断可用；输入帧限制 1 MiB，stdout 专用于协议，stderr 不包含原话、路径或 Key。
- 设置提供真实开关、同包绝对路径配置复制、连接说明和本地 stdio 检查。明确外部 Agent 可能将结果交给它的模型；区分开关、本机检查与真实外部调用。
- 独立数据库变更监听不依赖模型配置；外部写入自动刷新已有 UI，退出期间的待整理任务在应用重启后继续。未提交草稿不会因刷新被覆盖。
- `npm run build:beta` 生成同版本 app、DMG、SHA-256；DMG 含 Applications 链接与安装/手动替换/卸载/数据保留说明。包内 MCP 不依赖开发机的 Rust、Node 或 Python。

实际验证（2026-09-07）：

| 层面 | 已完成证据与边界 |
|---|---|
| 构建与回归 | 前端 TypeScript/Vite、43 项 Node UI 回归、Rust fmt、Clippy（warnings 视为错误）、全工作区 86 项 Rust 测试通过。新增 6 项核心 MCP 回归与 2 项设置交互回归；原有测试仍通过。 |
| 正式协议与进程 | `verify_mcp.py` 对实际打包 sidecar 通过：2025-11-25 initialize 与 2026-07-28 discover/请求元数据、两个工具、默认关闭/动态关闭、原文与来源、重试去重/冲突、非法参数与未知工具、12 个应用/MCP 并发写入、确认后 SIGKILL、应用未运行时 pending、帧限额、EOF 与 SQLite 完整性。 |
| 真实 Agent 样本 | 本机 Codex CLI 与 Claude Code 分别在独立合成库完成一次明确保存及检索；核对工具调用记录和数据库，均仅保存一次原文，未编造 project/URI。这是标准 MCP 的兼容性样本，不形成客户端白名单，也不承诺任意客户端会主动调用。 |
| 原生设置和生命周期 | 独立 QA bundle 实测开关、配置复制、关闭时本地检查仍可用、检查不宣称 Agent 已连接；无模型时外部记录自动出现，刷新保留正在输入的中文草稿。退出后通过 MCP 保存并检索，重启后草稿仍在；三个 pending 任务通过本机固定协议端点整理为当前记忆，显示回执、版本与 Agent 原话来源。固定端点用于工程闭环，不代表模型质量测试。 |
| 真实包 | release app 与包内 MCP 均为 arm64，Info.plist 最低系统 26.0；`codesign --verify --deep --strict` 通过。最终 DMG 只读挂载及校验通过，包含应用、Applications 链接与说明。从 DMG 复制到隔离安装目录的原始包实际启动、中文保存、同包 MCP 诊断与配置路径通过，安装副本的 MCP 再跑协议检查通过。 |
| 替换与数据保留 | 退出安装副本后执行同版本手动替换，重启仍看到同一条合成记忆；退出并移除该测试应用包后，独立数据目录的原文和完整性仍在。此项不是跨版本数据库升级测试。测试结束退出测试实例、停止固定模型端点并卸载测试 DMG。 |
| 现有数据流程 | 阶段 2 并发/备份/崩溃脚本、阶段 5 整理任务崩溃恢复脚本通过。历史阶段 1 测试脚本仅调整显式原型可执行名，其既有进程/stdio 回归也通过，不重新定义其历史验收。 |

**全量改动 Review 与修复（2026-09-07）：**用户要求检查所有改动并修复问题。本轮发现并修复：后台数据库刷新会关闭已打开的整理变化对比，并干扰同一记录正在执行的保存/撤销；MCP 开关写入期间可关闭设置，且旧状态读取或写入报错可能让界面与已落盘开关不一致；自定义 Cargo 输出目录时，打包脚本可能复制默认目录中的旧应用或旧 MCP。整理回执现在按记录身份保留交互状态，设置等待操作完成并重新读取实际开关，构建从 Cargo 实际产物与目标目录定位输出。MCP 服务版本随 Cargo 包版本生成，打包前拒绝应用与 MCP 版本不一致。

修复验证：新增失败复现与回归后，52 项 Node 前端/构建测试、86 项 Rust 测试、TypeScript/Vite、fmt 和 Clippy 通过。原生隔离 QA 实测文件锁等待期间 Escape 不关闭设置、失败后显示关闭状态、重试成功；打开变化对比期间通过同包 MCP 写入，新记录自动刷新且对比保持打开。重新生成 release app 与 DMG，包内 MCP 协议检查通过；只读挂载最终 DMG，确认应用与 MCP 二进制哈希均与本轮构建一致，签名校验与 DMG SHA-256 通过后卸载。测试未调用真实模型；结束后关闭隔离库 MCP 并退出 QA 应用。

构建和测试证据保存在忽略目录 `research/phase6-*`。默认 Cargo 目录下，应用为 `target/aarch64-apple-darwin/release/bundle/macos/Memivy.app`；DMG 与 SHA-256 固定输出到 `target/release/bundle/dmg/`。本轮不修改 Demo、DESIGN.md 或品牌，不提交或推送代码。

尚未完成的外部验证：另一台干净 Mac 的下载隔离属性/Gatekeeper 首次打开、无开发运行时环境、真实跨版本替换、10–15 名用户的连续试用及效果统计。当前无 Developer ID 和 Apple 公证，不把本机 ad-hoc 签名校验描述为 Gatekeeper 放行或正式公开发布。没有向测试用户发送邀请。

后续外测仍需收集：应用内及跨 Agent 的有效续接、回答可用性、结论保存、落位纠正、无结果和失败原因；样本兼顾使用与不使用外部 Agent 的用户。完成这些真实验证后，才能评价 PRD 定义的 Mac MVP 是否通过外部验收。

### 阶段 7：五项优化与性能验证（2026-09-08）

**授权：**用户审阅五项方案并确认整库恢复、自动保留当前库副本后，要求“特别注意性能问题……开始做吧……做完你先自己 review 一下”。本阶段沿用当前正式 UI，覆盖内部整理 Function Calling、普通 RAG 提示词、携带时间的新旧资料检索、详情相关记忆、手动备份恢复；不加入 embedding、独立时间检索、额外回答审核模型或合并恢复。

**实现：**核心按 model / organization / discussion / retrieval / related / transfer 分工，UI 与 MCP 不另写数据库规则。整理只接受一个合法内部函数调用，AI 新建标题非空，继续原话先落盘、回执及撤销。问答自动检索相关历史，保留不同版本并去掉相同片段，排除撤销/删除的不可用内容；普通列表与 MCP 搜索契约不变。相关记忆最多三条、直接进入已有讨论。备份保持流式 SQLite 一致性复制；恢复在原生草稿确认后的重启启动阶段执行，固定数据目录、跨进程锁、恢复前副本及中断回退。

**实际验证与自查：**

- 本轮真实配置的合成整理首次 **36/40**；修正独立内容不应暂缓、共同词不等于同一事实及缺失指代规则后，第二组 **40/40** 通过动作、目标及核心应用检查。第二组 16 次新建均由 AI 给出有效标题，原话与去重检查通过。配置和 Key 未写入研究输出。固定材料上的表现不保证任意真实内容准确率。
- 普通 RAG 的 12 条固定合成案例全部完成并保持无自动记忆写入；逐条对照冻结材料做了 Codex 自查。`qa-09` 保留收费未定与订阅假设，未再补出旧收费方式。自查不是用户语义判定，也不新增运行时核查模型。
- 一万条合成记忆及对应原话、版本、回执与 FTS 索引的 debug 测试，每类执行 20 次：普通 FTS 查询 P95 **5.7ms**，两个中文短词查询 **113.9ms**，相关记忆 **5.9ms**。初版相关查询超过 250ms，EXPLAIN 确认连接顺序使每个命中扫描全库；修为从来源索引出发并按需补查询后通过。SQL 总预算为 250ms，不将其称为任意数据规模下的响应承诺。
- 原生 debug 应用已实际完成：详情下方相关记忆显示与选择、进入含两份依据的现有讨论、原生保存备份、选择并校验备份、确认后的重启恢复。恢复后回到备份的 6 条记忆；副本保存了后增记忆和讨论草稿，两库完整性检查通过。发现并修正退出确认可能重复消费的问题；最终改用 Tauri 请求重启并保持恢复请求直到完成。最终包含 MCP 的 app 已再次实际确认自动重启、子进程持续运行及恢复成功界面。测试使用隔离库，不进行正式用户库恢复。
- 实际 stdio / 跨进程测试：恢复前并发写入的 **45** 条合成原话均进入自动副本；恢复等待期间新 MCP 初始化和原连接数据调用均被阻止；恢复后原 MCP 连接直接读取同一路径的新库。见 `scripts/verify_restore.py`。最终 app 内的 MCP 二进制也通过 `verify_mcp.py --binary` 全套实际 stdio、并发写入和完整性验证。
- 新增覆盖 Function Calling 参数/单调用、旧版本召回与撤销隔离、相关结果过期与刷新合并、显式确认恢复、损坏与过新备份、替换前后中断回退、退出重复确认的回归。全 workspace Rust 回归、相关新增测试、Clippy `-D warnings`、前端构建及包含正式 MCP 的原生 app 构建实际通过。UI 原有文件监听测试在沙箱内遇到 EMFILE，正常 macOS 权限下完整 UI 回归 **60/60** 通过；不改动已有的 Vite 修复。

原始输出保存在忽略目录 `research/five-improvements/`（模型逐项结果、源码指纹、性能、进程验证和 self-review）。保留此前性能修复与历史验收记录。尚未在另一台 Mac 验证，也未模拟真实磁盘耗尽或硬件断电；中断覆盖为受控文件状态/进程验证。本阶段完成开发自查，等待用户查看，不标记为用户验收或公开发布。

### Markdown 正文编辑补充（2026-09-08）

用户在认可白底、黄色点缀、宽正文、Bear 式阅读排版和轻量选区工具栏后，明确指定 Milkdown 并要求实现。保留当前页面结构及 Miro 品牌；这次授权覆盖正文和结论审核，不扩展块工作区、附件、协同或存储迁移。

- 已实现：Milkdown 7.22.1 按需加载，正文／Markdown 源码切换，选区加粗、斜体、删除线、代码和链接；正文、历史与结构化回答使用统一 Markdown 排版，来源引用按钮独立保留。快速记录、提问和原话仍使用原来的输入／字面展示。
- 保存继续走现有草稿、版本、回执、并发检查。文档更新同步进入草稿队列，避免延迟监听遗漏最后一次输入；打开和切换模式不主动格式化已保存字符串。额外封住 ⌘S 绕过版本冲突的入口。HTML 不执行，图片保留语法但只显示文字占位。
- 工程检查：`npm run build` 和隔离的 `Memivy Markdown QA.app` debug 构建通过；原 60 项加 2 项保存回归共 62 项前端测试全部通过，另加 1 项真实 Markdown SSR 渲染回归通过（标题、查询高亮、列表、代码、表格、HTML/链接及图片边界）。沙箱内既有 Vite 监听测试报 EMFILE，沙箱外单项和完整 62 项重跑通过；不是应用回归。
- 实测：Chrome 中检查正文与 580px 结论弹窗的排版、浮动工具栏；原生独立空库检查中文 Markdown 标题／段落／引用／列表、正文／源码切换、工具栏斜体、⌘S 保存、末次输入立即保存、关闭编辑及重启应用后恢复草稿、撤销版本；浏览器检查外部内容更新能同步到编辑区。只读核对测试 SQLite 中版本正文、草稿和原话，确认 Markdown 落盘、保存清理草稿、原话不被覆盖。测试资源在忽略的 `research/editor-qa/`。
- 验证边界：原生中文正文通过 AX 设置源码并转入 Milkdown；自动键入通道未正确输入中文，不能据此宣称物理拼音输入法组合态已验证。结论保存合同有自动回归，未调用真实模型重跑融合生成。本轮不改 Rust 核心，不将开发检查视为用户视觉验收或正式发布验收。

**交互修正（同日）：**用户认为排版可以，但格式操作不足，明确取消正文／Markdown 双模式，并要求参考本机 Mem。实际只读查看本机 Mem 选区格式栏（H1/H2、字体、列表、引用、清除格式等），并核对 [Mem 官方编辑说明](https://help.mem.ai/features/editor) 的输入转换与格式栏说明；未改动 Mem 笔记。

改为纯所见即所得，保留 Markdown 输入规则。编辑区常驻一排轻量工具，选区浮动栏复用同一组控件：普通文本、三级标题、列表、引用、代码块，以及加粗、斜体、删除线、行内代码、链接和清除文字格式。段落转换只作用于当前选区；选区不必覆盖整行，光标在标题内即可改为普通文本。移除源码切换和源码故障回退；加载失败保留草稿并提供重试。新增格式操作回归覆盖标题转正文及撤销、选中文字标记与清除、块类型互转、局部列表拆出、只读和组合输入状态阻止修改。

本轮检查：69 项前端测试、生产前端构建和原生 debug 构建通过。生产资源预览实测光标所在标题切为普通文本、输入 `# ` 立即转标题、选区浮动栏同步段落格式；原生新版已打开，核对常驻与选区控件、无源码切换、第一轮用户草稿完整恢复。原生仅选择已有文字查看工具，不改写用户试用草稿；具体转换动作在合成浏览器样例和 ProseMirror 回归中验证。临时开发样例出现缓存 React 实例混用，视觉复核改用生产打包资源；不将失效的开发样例当作验证通过。中文物理输入法组合态仍保留上一轮验证边界。

**最终收简（同日）：**用户明确要求去掉常驻工具栏，按 Mem 的选区浮动方式编辑。已删除常驻控件及其选区状态同步、sticky 样式，只保留选中文字时出现的一行工具和 Markdown 输入自动转换。段落／标题、字体、列表等能力继续在选区工具内使用；默认编辑区域保持干净，配色、字号、圆角沿用 Memivy。上一轮常驻工具栏记录仅代表中间实现，不再是当前产品行为。 本轮 12 项相关回归、前端与原生 debug 构建、`git diff --check` 通过；原生实测进入编辑无常驻工具、选中文字出现单行格式栏、取消选中立即隐藏，并确认用户试用草稿完整保留。Markdown 输入规则与保存逻辑未改动。

**直接按钮修正（同日）：**用户再次指出不应保留下拉框。移除段落类型 select 和全部相关样式，选区只显示一行 H1、H2、字体、清除格式、代码、列表、引用及链接按钮。H1/H2 再次点击恢复普通文本；清除格式同时恢复普通段落并清除选区文字标记。重新读取 Mem 曾被自动审批以私人内容风险拦截；用户随后明确授权“我允许你去看我的 Mem”，已实际查看本机 Mem 的选区浮层，确认 H1/H2 与格式按钮直接横排、没有下拉框，没有修改 Mem 笔记。13 项相关格式及保存测试通过，前端生产构建与原生 debug 测试包构建通过。在隔离数据的原生测试应用中确认单行直接按钮、H2 激活状态、再次点击恢复普通文本，以及取消选区后浮层隐藏；测试修改已撤销，用户现有草稿已保留。此为本次实际验证记录，不代表用户验收。

**完整格式栏与 H3（同日）：**用户提供 Mem 浮层截图，明确要求功能完整一致，并额外增加 H3。按截图顺序与三组分隔补齐 16 个直接按钮：H1/H2/H3、加粗、斜体、下划线、删除线、清除格式、行内代码；编号、项目、待办列表；引用、代码块、链接、表格。继续使用 Memivy 白底、细边框和黄色激活态，无段落下拉框、无常驻格式栏。下划线使用安全的 `<u>` Markdown 扩展，阅读与编辑共用；待办复选框参与现有草稿/版本保存，回车新增项默认未完成；启用 Milkdown 表格单元格及行列组件，插入表格保留选中文字。补充 `[] `、`1 `、`| ` 输入转换与代码块回车退出，不增加独立任务系统。

- 本轮检查：完整前端回归 75 项中 74 项在沙箱通过，既有 Vite watcher 测试遇到 EMFILE，在正常本机环境单项重跑通过；随后增加待办回车回归，格式/渲染 14 项重跑通过。前端生产构建及原生 debug 测试包构建通过。
- 浏览器实际验证：H3 和下划线保存后重新编辑仍保留；待办勾选保存后状态保留；`[] ` 自动转换；表格 2×2 插入不删除选中文字，Tab 切换单元格输入中文/英文后保存重开结构与内容保持。
- 原生实际验证：16 个按钮按指定顺序单行完整显示；H3 转换、待办勾选、表格插入均生效；验证动作已撤销，先前用户试用草稿保留。表格行列组件已接入，尚未逐项手动验证行列拖动；物理中文输入法组合态的既有验证限制仍保留。本记录不代表用户验收。

**审查修复（同日）：**用户要求 Review 并修复实际问题，不做过度设计。本轮复现并修复三项：跨普通段落、引用和列表选区时清除格式不完整；块转换与文字标记清除分成多次撤销；点击编辑区外部后选区浮层残留。按选区逐块解除引用/列表，保留选区及未选内容；一次按钮操作合并为一次文档事务，避免中间草稿和分步撤销；浮层显式响应焦点进入/离开并清理监听。未增加功能或依赖。新增混合结构回归验证正文不丢、选区不变、只发布一次文档更新和一次撤销完整恢复；相关格式、渲染、立即保存与结论保存共 21 项通过，前端生产与原生 debug 测试包构建通过。浏览器和隔离原生应用均实测混合内容清除、一次撤销及失焦隐藏；原生验证改动已撤销，用户最新试用草稿保留。此前表格拖动和物理中文输入法的验证边界不变。

### 主窗口导航与统一检索（2026-09-08）

用户确认合并首页与记忆库、将搜索与提问统一为 AI/RAG 入口，同时保留左侧栏，并明确要求修改代码、统一视觉与性能自查。按已确认的分步计划，本轮先交付布局供实际体验；置顶与回顾、轻量专题／集合在布局确认后继续，不显示未实现的占位菜单。

- 已实现：顶部常驻 AI 提问框和黄色“记一下”，左栏保留全部记忆／最近讨论／回收站／快捷入口／设置与数据；紧凑列表与宽正文共用原有排版。Enter 提问、Shift+Enter 换行、⌘K 聚焦，⌘N 打开记录弹窗，⌘Enter 保存；关闭保留草稿，保存后原阅读记录不跳走。回答、引用、继续讨论和结论审核复用既有组件。
- 架构与性能：提取 `WorkspaceSidebar`、`MemoryList`、`CaptureDialog`，复用草稿队列与原生 generation 衔接协议；没有新增依赖、Rust 数据路径或数据库表。每页 40 条，选中记录或切换已有讨论不刷新列表，过期查询响应被忽略；键入不调用模型。最终主入口 JS 468.05 kB（gzip 143.65 kB），Milkdown 保持按需拆包；这是构建体积与调用次数证据，不代表大规模真实用户延迟已测。
- 最佳实践参考：[Mem Search](https://help.mem.ai/features/search)、[Mem Chat](https://help.mem.ai/features/chat)、[React lazy](https://react.dev/reference/react/lazy)、[WAI-ARIA Dialog](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/)。按用户选择统一搜索与问答，弹窗沿用已有原生 dialog、关闭草稿与焦点恢复机制。
- 工程检查：完整 `npm run test:ui` 86/86 通过；`npm run build`、隔离 `Memivy Navigation QA.app` 原生 debug 构建、`git diff --check` 通过。包括新入口 IME/快捷键、禁止键入触发模型、重复提交、失败重试与草稿、记录后焦点、分页调用次数、过期响应和快捷问题衔接回归。首次沙箱 Vite watcher 报 EMFILE，正常本机完整套件重跑通过。
- 实测：生产资源浏览器与隔离 macOS 原生应用检查白／黄布局、列表／正文、最近讨论和回收站；原生约 1109×768 与 823×618 窗口检查。真实 UI 操作验证关闭记录弹窗后草稿恢复、保存一次并显示回执、保持原选中记忆；顶栏 Enter 提问、回答引用打开正确当前版本、模型失败提示及停止回答；快捷记录展开主窗口后原文与来源完整保留。
- 数据核对：独立合成库原 7 条 captures，操作后 8 条；本次原话只写入 1 次，三个测试问题均未进入 captures，快捷记录草稿仍在。证据与固定响应测试服务位于忽略的 `research/navigation-qa/`，没有使用用户真实记忆或修改此前编辑器试用草稿。
- 验证边界：原生 RAG UI 使用本机固定响应模型服务，验证真实 IPC／检索／引用／取消与失败链路，不代表真实模型回答质量。中文通过 AX 设置与回归测试验证，物理拼音组合输入未重测。原生窗口关闭／恢复草稿已测；用户视觉验收与后续侧栏功能仍待反馈。

### 原生紧凑顶栏与展开提问（2026-09-09）

用户确认输入框下方展开轻量面板，随后明确授权实现。参考本机 Mem 实测、[VS Code Command Center](https://code.visualstudio.com/updates/v1_69#_command-center)、[Microsoft 标题栏搜索规范](https://learn.microsoft.com/en-us/windows/apps/design/basics/titlebar-design#search) 与 [Tauri 窗口定制](https://tauri.app/learn/window-customization/)。本次仅实现 macOS；Windows 只讨论布局规则，不加入平台开发或验收范围。

- 共享 `WorkspaceTopBar` 和 52px 顶栏，移除原有正文上方独立搜索区；保留白黄设计、侧栏、阅读字体与行距。原生主窗口采用 Overlay、隐藏重复标题、保留系统红黄绿按钮，只有空白区域可拖动；独立主窗口 capability 只允许拖动和双击缩放，不扩大数据权限。
- 点击或 ⌘K 展开非模态面板，支持多行问题；Esc 收起并恢复入口焦点，Tab/点击正文直接回到内容。草稿与提交中的表单始终挂载，收起不清空、不取消写入；键入不调用模型。专题范围、草稿摘要在入口可见，回答继续复用原讨论/来源流程和快捷入口交接。
- 工程检查：完整 UI 108 项通过，新增面板收起/重开、草稿与待完成提交、IME Escape、焦点恢复/Tab 回正文回归；既有专题范围、失败重试和快捷交接检查继续通过。首次沙箱文件监听报 EMFILE，正常本机环境重跑完整套件通过。TypeScript、Vite 生产构建及隔离原生 debug 包构建通过。没有新增依赖或 Rust 数据层变更。原始日志在忽略目录 `research/topbar-qa/`。

- 原生与视觉检查：独立 `Memivy Topbar QA.app` 使用既有合成库的 SQLite 一致性副本，浏览器和原生常规窗口、拖至约 760×560 的窄窗口均目视核对。实际验证搜索首次点击聚焦、中文多行草稿、Esc 收起/入口焦点、⌘K 重开、退出重启后草稿恢复，以及全屏进入/退出。最终红黄绿按钮与输入框垂直对齐。
- 实际专题提问通过本机固定响应服务完成：面板收起、讨论框获得焦点、专题范围与引用可见；服务记录仅提交后的一次规划和一次回答请求。原话数量前后均为 8，问题草稿在成功提交后清除。没有使用真实模型评估回答质量。工具曾自动重启 QA 到默认数据视图；该视图只读即退出，所有输入/提交均在确认合成库后进行。
- 默认 `target/debug/bundle/macos/Memivy.app` 已重新构建；旧 Dev/打包进程需退出并重新启动才能应用原生窗口配置，单纯前端热更新不够。中文通过 AX 多行输入和组件组合态回归验证，未完成物理拼音选词验证；最小化/关闭后恢复、多屏和原生拖动位移未完整验收。本轮仍是实现及开发验证，不代表用户视觉验收。

**本次顶栏审查修复（2026-09-09）：**复现搜索面板内 ⌘N 打开记录弹窗后，关闭弹窗会尝试恢复到已隐藏输入框，最终焦点丢到页面；改为打开记录/未配置模型的设置弹窗前交接到可见的搜索入口。原生与浏览器实际验证记录弹窗关闭后入口获得焦点；隔离原生还验证模型不可用进入设置、Esc 返回入口、重开后合成问题完整保留。搜索入口的无障碍名称补齐当前专题范围和未发送草稿状态，原生 AX 树核对通过。完整 UI **111 项**、TypeScript/Vite 构建、隔离 QA 与默认 Mac debug 包构建、diff 格式检查通过；没有新增依赖或数据规则。只读核对合成库原话仍为 8 条。预览 Vite 曾继续返回旧转换缓存，已重启本任务的服务并核对修复后的实际资源；证据在 `research/topbar-review/`。此次未重测物理拼音、多屏、最小化/关闭后恢复与真实模型质量，保留既有验证边界。

### 置顶、回顾与专题完整交付（2026-09-08）

用户随后明确要求“把这几步全部做完啊”，取消上一记录的中途布局确认门槛。本轮实现所有剩余项，没有再次停在方案或静态入口。

- 已实现：正文置顶／取消并在左栏直达；每组三条的主动回顾，筛选与“接着想”；专题创建／编辑／移除及撤销、多专题成员手动管理；一次 AI 查询规划返回候选，逐条确认加入；限定专题的顶栏问答和持续讨论。导航滚动区容纳置顶、专题与最近讨论，底部管理入口保持原位，沿用白黄配色和现有正文排版。
- 数据与结构：共享 MemoryStore `navigation` 模块、schema 6 元数据表和索引，不改原话或正文版本；成员关系与置顶随既有整库备份恢复。专题范围保存在会话中，候选排序前限定、证据绑定及回答提交再检查；已移除专题绝不回退全库。没有新依赖、embedding、通知、后台自动归类或 MCP 工具。
- 研究依据：[Mem 置顶](https://help.mem.ai/features/pinning)、[Mem 集合](https://help.mem.ai/features/collections) 的多集合与显式整理入口，以及 [flomo 主动回顾](https://help.flomoapp.com/advance/lucky.html)。按本产品范围只采用手动发起、低干扰行为。
- 检查：完整 UI 回归 90 项通过；核心与原生 host 的工作区 Rust 套件 106 项通过；新增导航专项含一万条记录性能实验 6 项通过；Clippy 全 workspace/all-targets 零警告、格式、前端与原生 debug 构建通过。全套结果与后续定向回归在 `research/collections-qa/`、先行 Rust 完整结果在 `research/navigation-qa/core-full.log`。
- 性能：一万条合成记录、debug 核心、每项 20 次，本机 P95 置顶 14.34 ms、专题列表 14.71 ms、三条回顾 14.16 ms、限定专题的 FTS 检索 24.38 ms；不包含模型响应时间。前端主入口约 479.92 kB（gzip 146.85 kB），正文编辑器仍拆包加载。
- 原生已测：约 1109×768 与 823×578 窗口检查侧栏独立滚动、专题列表／正文与讨论；重启隔离包后置顶、专题成员和会话范围恢复。创建“木桥产品”专题及说明、AI 推荐后只加入一条、置顶直达、手动移出再加入、两轮专题问答、三条回顾切下一组、移除专题并撤销。只读 SQLite 核对两轮引用全部属于专题成员及其历史，原始 captures 仍为固定样例的 7 条；专题、成员与置顶均正确落盘。测试位于独立合成库，没有修改用户真实记忆。
- 交付：生产前端资源、独立原生测试包及默认 `target/debug/bundle/macos/Memivy.app` debug 包已构建；文档本地链接与 `git diff --check` 通过。浏览器仅为布局预览，真实操作使用 Mac 应用。
- 验证边界：AI 推荐与问答使用本机固定响应服务，证明真实 UI／IPC／数据／范围链路，不证明真实模型推荐或回答质量；物理拼音组合输入仍未重测。以上是开发验证，尚非用户视觉验收或发布验收。

- 按钮样式修正（2026-09-08）：专题工具栏与推荐条目复用记忆页紧凑按钮，专题弹窗恢复共享黑色主按钮；MoreMenu 菜单项样式不再依赖外部工具栏祖先，统一字号、留白与危险操作色。前端构建及 90 项 UI 回归通过（沙箱文件监听 EMFILE 后在正常主机环境重跑通过）；独立原生合成库中目视核对专题菜单、编辑弹窗与记忆工具栏，无真实记忆写入。结果位于 `research/style-qa/`。


### 整理后专题推荐（2026-09-08）

用户明确要求设计并实现“AI 整理完成后推荐加入专题”。已在最新成功整理回执下加入轻量推荐区，最多三个已有专题及简短理由，逐条确认加入、显示已加入并支持撤销；可忽略，空结果不展示，失败可重试。新增内容复用浅黄色回执和共享紧凑按钮；回执区域按窗口高度扩展，仍保留滚动上限。

- 共享核心负责一次有界模型推荐及事务内确认：排除已有成员，拒绝虚构／重复 ID，校验回执状态、目标版本、专题 revision 与删除状态；模型请求自身不写入记忆或成员关系。没有新表、依赖、embedding、自动加入或后台定时任务。
- 前端按回执共用会话级请求缓存（最多 64 项），两处回执展示与刷新不会重复调用。没有可推荐专题时核心直接返回，不调用模型。
- 验证：完整 UI 96 项、Rust workspace 109 项通过；最终回执专项 3 项通过，覆盖幂等加入、撤销／删除／新版本／专题变更拒绝和无候选不调用模型；Clippy workspace/all-targets 与格式检查通过。结果在 `research/organization-collections-qa/`。
- 原生实测：隔离合成库记录一条想法，真实整理任务完成后自动出现专题推荐；点击加入更新左栏计数，撤销后恢复。只读数据库核对原始记录为原有七条加本次一条、成员关系恢复原值；本地模型请求日志为一次整理加一次推荐。使用固定本机响应服务验证 UI／IPC／模型协议／持久化链路，不代表真实模型的推荐准确率。 最终高度修正后再记一条合成记录，原生目视确认理由与加入按钮完整显示，忽略后推荐区收起；默认 `target/debug/bundle/macos/Memivy.app` 已重新构建。


### 就地反馈与安静阅读（2026-09-08）

用户认可减少打扰的优化方案，并明确要求实现供体验。本轮移除主窗口顶部捕捉回执与正文常驻成功提示：保存使用底部约四秒自动收起的短提示；悬停／聚焦暂停计时，同一时间只显示一条。整理中／未完成状态回到对应记忆；普通新建不再额外提示，补充旧记忆提供短暂的查看入口。整理回执、变化和撤销保留在版本历史。

- 专题推荐移到标题下的轻量标签，点击加入，理由按需展开；加入后显示普通专题标签，底部提供撤销。列表用本地状态显示推荐数量。忽略按回执持久保存，重启不再显示；新输入有独立回执，已更新正文不会显示旧版本建议。
- 结构：schema 7 `collection_feedback` 保存推荐结果和忽略状态，纳入 SQLite 备份并在永久擦除时清理。整理完成后异步推荐，推荐请求串行并复用缓存，不阻塞下一条保存；列表按最多 100 个 key 批量读本地状态，不从列表发起模型调用。没有新增依赖或定时通知。
- 检查：完整 UI 100 项通过，最终旧版本推荐隐藏专项通过；Rust workspace 111 项通过，包含持久忽略／永久删除的最终导航专项 10 项通过（额外的一万条性能实验本轮未重跑）；Clippy workspace/all-targets、格式、前端、MCP 与默认原生 debug 包构建通过。日志位于 `research/context-feedback-qa/`。
- 原生实测：独立合成库中记录 → 底部短提示自动消失 → 列表推荐数量 → 标题下推荐标签 → 展开理由 → 加入转成已有标签 → 提示撤销 → 忽略 → 版本历史找到变化和撤销 → 重启后忽略保持。约 1109×768 与 850×595 窗口目视检查。SQLite 核对原话为原有七条加本次一条、成员数撤销后恢复为一条、忽略状态已落盘；没有写入用户真实记忆。
- 验证边界：原生使用本机固定模型响应验证真实 UI／IPC／持久化流程，不代表真实模型推荐准确率；中文输入法组合与其他应用全屏场景本轮未重测。当前为开发包供用户体验，不代表用户验收。

### 全部改动审查（2026-09-09）

用户要求 Review 所有已修改和新增文件。本轮审查导航、专题推荐、反馈、编辑器兜底、数据与文档，并修复已确认问题：

- 专题范围：快捷窗口未绑定讨论的新问题回到全库，已有专题讨论继续保留范围；专题正文的相关记忆在候选和目标限量前限定成员，避免带入专题外来源后无法继续讨论。
- 本地反馈：已缓存专题推荐先从本地读取，移除模型配置不再错误阻断；轻提示在鼠标离开但键盘焦点仍位于内部时继续暂停，按钮间移焦与操作后新提示均正确保留。
- 批量状态：每批最多 100 条共用一个只读事务，读取最新任务，复用本地反馈校验，避免逐条打开数据库及读取完整回执历史。同一万条合成原话实验（无整理任务，debug 核心）100 条状态读取 P95 从 183.93 ms 降到 2.39 ms；这是批量本地读取证据，不是模型延迟或真实用户全量数据性能。
- 编辑器：失败时保留正文预览与重试，不再只有报错和空白；日志仅区分资源／初始化阶段，不输出可能含正文的异常内容。Dev 应用原始加载报错未稳定复现，不能将兜底或独立测试通过等同于已查明其根因。
- 检查：完整 UI 105 项通过；审查前完整 Rust workspace 基线 112 项通过，修复后相关记忆／AI 整理／导航定向回归通过，最终导航含缓存校验、范围与一万条性能实验共 13 项通过。Clippy workspace/all-targets、格式和前端构建通过。UI 首次在沙箱触发文件监听 EMFILE，正常本机环境重跑通过；轻提示测试改用可控时钟避免并发测试调度造成误报。证据在忽略的 `research/full-review/`。
- 原生实测：新标识 `Memivy Review QA` 与独立合成库、无模型配置下，已有推荐正常显示，确认加入与撤销均更新成员及计数；Milkdown 正文打开、中文草稿编辑、收起再打开均正常，目视检查既有白黄排版。只读核对原话仍为 8 条、成员关系撤销后仍为 1 条，合成编辑草稿已落盘；未修改用户真实记忆。独立 QA 与默认 `target/debug/bundle/macos/Memivy.app` debug 包均重新构建成功。此处中文用 AX 输入，未重测物理输入法；未进行真实模型质量评估或发布验收。


### 单篇正文整理与图标工具栏（2026-09-09）

- 授权：用户在 Mem/竞品研究及原位审核方案确认后明确要求实现，并要求职责分离与性能控制。范围为单篇当前正文或编辑草稿；不扩展全库处理、自动总结、选区整理、模型 Agent 循环或 MCP。
- 交付：右上角六个普通动作改为图标及悬停/焦点提示；整理期间临时显示审核操作。正文原位生成、结果/改动切换、共享 Milkdown 编辑、再调整、取消、一次确认保存，以及可见撤销回执和审核来源标记。临时预览不会进入原编辑草稿、记忆检索或原话。
- 数据：核心快照和接受事务同时核对当前版本与草稿；请求指纹支持回执重放。接受时为尚未保存的来源草稿保留历史版本，随后保存审核稿并原子消费该草稿；撤销恢复整理前内容。schema 8 增加不可修改的 review_kind 注记，沿用现有版本表及备份路径，没有版本表重建。故障注入确认版本、回执、索引及草稿消费一起回滚。
- 模型：移除统一 max_tokens=2500；默认使用服务商配置，高级设置可指定 token 上限及 max_tokens/max_completion_tokens 参数名。结构化调用保持 90 秒/64 KiB 边界；正文整理和融合预览采用 180 秒/1 MiB 响应边界。完整正文仍受核心 128 KiB 合同约束。截断响应单独提示，不接纳为候选；补充调整失败时保留此前完整审核稿。
- 验证：TypeScript/Vite 构建、Rust fmt、Clippy warnings denied、全工作区 Rust 回归 **121 项通过**；UI 回归 **117 项通过**。正文整理专项 **5 项**涵盖草稿/版本冲突、精确保存、幂等、撤销、不可变审核来源与事务失败；模型专项覆盖超过 64 KiB 的完整响应、截断拒绝、默认省略上限与两个可选参数名；UI 专项涵盖切换后的迟到结果、双击确认、版本更新、最后一次输入立即快捷键保存和差异文本完整性。文件监听测试在沙箱内 EMFILE，单独沙箱外验证及完整 UI 回归通过。
- 原生：使用独立标识 `com.memivy.cleanup-20260909`、隔离 SQLite 和 loopback 合成模型。实际查看工具栏、键盘焦点提示和改动界面，复核黄色确认按钮及改动分块，完成生成、再调整、接受、撤销；验证编辑草稿取消保留、接受后撤销找回草稿、截断不替换有效候选。隔离库最终 1 段原话、6 个版本、2 个审核来源注记，integrity_check=ok；备份恢复到新目录后 schema 8 与审核标记保留。初次沙箱启动退出及单实例干扰已通过独立 QA 构建解决，未对用户现有窗口执行测试操作。
- 性能：有限行匹配避免平方复杂度。100 次本机差异计算中，约 78 KB/3000 行输入对的 P95 **1.77 ms**；约 109 KB/4200 行触发合并完整删增块，P95 **0.28 ms**。这是差异计算耗时，不含 Milkdown 渲染或模型耗时。
- 边界：合成响应证明交互及协议行为，不证明真实模型的整理质量。尚未针对多家真实 BYOM 服务商做质量/超长文兼容矩阵，也未将这次自测记作用户视觉验收或发布签名、公证。临时预览在取消、离开条目或退出进程时丢弃，原编辑草稿及已保存版本保留。未提交或推送。

**后续全量改动评审与修复（2026-09-09）：**检查包含新增文件在内的全部未提交改动，复现并修复四处问题：保存成功但 IPC 回应丢失时，允许重试原始确认请求并重放回执，后续编辑仍受版本冲突保护；来源草稿不同于已保存正文时，允许审核结果恢复到已保存正文，同时保留来源草稿历史；详情后台刷新暂时失败时保留正在编辑或审核的界面及文本，并显示错误；“更多”展开时隐藏重叠的工具提示。对应故障用例先复现失败，再验证修复，未扩展功能范围。

- 回归：正文整理核心 **6 项通过**，UI 整理相关 **10 项通过**；完整 Rust 工作区 **122 项通过**（含实际模型超时），完整 UI **121 项通过**。TypeScript/Vite、fmt、Clippy warnings denied 和 diff whitespace 检查通过；独立 QA 包与默认 `target/debug/bundle/macos/Memivy.app` 均重新构建成功。UI 文件监听测试在沙箱内遇到 EMFILE，完整套件在沙箱外通过。
- 原生复核：独立 QA 包、隔离数据库及本地合成响应下，确认“更多”首个菜单项无遮挡；将未保存草稿整理回已保存正文，接受成功并保留两个新历史版本，随后撤销恢复来源草稿。只读核验最终 **1 段原话、9 个版本、integrity_check=ok**。保存回应丢失和详情读取失败由 UI/IPC 测试夹具验证，未声称原生 IPC 故障实测。证据保存在忽略的 `research/cleanup-review/`。真实模型质量与服务商兼容矩阵仍未验证，未提交或推送。

### 检索增强三阶段施工授权（2026-09-10）

用户审阅 [`research/retrieval-2026-09-09/three-phase-plan.md`](research/retrieval-2026-09-09/three-phase-plan.md) 后授权完成以下三项。每项完成后运行该阶段测试，启动独立 Review-Fix 循环，并在复核后单独提交一个 commit；禁止自动 push。现有 UI 与视觉基准继续保留。本授权仅表示可以施工，不表示任何阶段已验收。

- [x] 阶段 1：Memory 立即可用、原话/历史降级为归档恢复材料，并统一 UI、问答、整理、专题、相关记忆和 MCP 使用的唯一搜索接口。
- [x] 阶段 2：设置内按需下载 HF 托管的 Qwen3-Embedding-0.6B Q8_0，接入本地 sqlite-vec 和混合检索；支持增量入库、失败回退与重建。
- [x] 阶段 3：仅为问答和录入整理加入有界 Agent 工具循环，复用核心搜索/阅读能力；其余 AI 功能保持固定流程。

本次授权在上述范围内覆盖历史记录中的“暂不 embedding／只依赖原话历史／不做工具循环”限制；历史阶段的实现、验收和验证记录保留。三阶段实现及独立复审记录如下，尚不代表用户验收。

实际验证（2026-09-10）：
- `cargo test --workspace --offline` 全部通过（含真实 90 秒模型超时）；`cargo clippy --workspace --all-targets --offline -- -D warnings`、格式检查及 `npm run build` 通过。UI 全套 123 项通过；最后状态文案修改后重跑相关 11 项通过。Vite 文件监听测试在正常权限下运行，沙箱内的 EMFILE 不作为产品缺陷。
- schema 8 升级、立即可用/归并/草稿/结论冲突等专项通过；真实进程 `verify_memory_store.py`、`verify_mcp.py` 通过，覆盖多进程写入、幂等、强杀持久性、备份恢复和 MCP 同一搜索结果。
- 一万条合成 Memory：FTS p95 14.8ms、两字查询 56.7ms、相关记忆 80.9ms；置顶 3.85ms、专题列表 4.07ms、回顾 27.97ms、专题检索 34.99ms。测试发现并修正了搜索关联顺序造成的短词超时，未放宽 500ms 预算。
- 独立标识的原生 QA 包和临时数据库：未配置 AI 保存后立即出现在记忆库，可编辑生成版本；输入归档完整保留，恢复预览后生成第三个版本；阅读页视觉保持原设计。结论冲突及迟到响应以真实组件/核心夹具验证，未声称已做外部模型或多窗口原生故障实测。
- 独立 Review-Fix 完成：修复无任务领取时暂停状态回滚、归并目标草稿保护、删除讨论遗留结论草稿；结论专项另修复原子消费草稿、双窗口冲突入口和迟到融合覆盖。最终复审无剩余阻断项，搜索性能修复后再次独立重跑搜索 2 项及当前 Memory 9 项通过。

阶段 1 的通过结论不包含本地模型语义质量、低内存设备和发布包外部验收。

阶段 2 实际验证（2026-09-10）：
- 设置接入默认关闭的本地语义检索，HF 断点下载、固定哈希校验、Metal 独立编码进程、schema 10 同库 sqlite-vec 索引与统一混合搜索；输入/历史继续只作归档。后台增量、重建、恢复、关闭与清理均遵循当前版本资格；前端没有第二份搜索或下载逻辑。
- 工作区 Rust 全套通过（含真实 90 秒超时）；最终核心 19 项、搜索 2 项、讨论 6 项回归通过，Clippy 全目标 warnings denied、格式检查、前端构建及 UI 126 项通过。真实进程 phase2/phase6 检查通过，搬移后 app 内的 MCP 二进制亦通过完整 stdio/多进程夹具。修正了上一阶段组织案例改为 keep/merge 后遗漏同步的展示夹具。
- 固定 120 条合成语料，中文、英文、其他语言、跨语言、中英混写、精确实体各 20 条：本地实际 Qwen Q8 混合 Recall@8/MRR@8 均为 1；每组热查询 p95 27–37ms。字面基线是原问题直接 AND 搜索，未包含生成模型改写，不把这组简单受控样例当作生产准确率。长文末尾事实、过期向量和范围排除另有回归；特殊标记、NFC、换行、NUL 明确失败边界已覆盖。
- Apple M4 Pro 发布优化构建，1 万 Memory／5 万片段 p95 182.1ms，10 万片段 309.4ms，均 0/25 降级。扫描夹具复制真实向量，只衡量查询成本，不衡量语义质量。修正临时聚合携带大 BLOB 的性能问题，保持 500ms SQL 预算；未优化的开发代码在 10 万片段仍可能触发字面回退，发布指标不能直接套到 debug 构建。
- 独立原生 QA 包与临时数据库，实际完成下载、暂停/继续、空库自动启用、无生成模型录入立即可用并增量索引、关闭及清理；清理后 Memory 保留且 integrity_check=ok。模型空闲 5 分钟退出，设置仍正确显示索引就绪；截图核对按钮间距和现有样式。完整 blob 发布中断后不再下载即可补快照。
- app/DMG 已按既有 ad-hoc 路线构建，复制到开发目录外后原生启动、建立索引，通过随包 MCP 的英文问题召回中文记忆；codesign --verify --deep --strict 通过。强杀隔离模型后立即字面回退且冷启动恢复语义查询通过。未做 Developer ID、公证或外部用户验收；源码构建需要 CMake。低内存/另一台设备峰值与真实个人语料质量仍需后续实测，不能由本机回归推断。
- 独立 Review–Fix–复审完成：修复空闲重复唤醒、32 KiB 问题边界、单条增量失败影响全库、pinned 覆盖语义窗口、重试游标竞争、缓存重复下载；最终独立重跑 19 项核心测试，无遗留阻断项。详细合成输出保留在忽略的 research/retrieval-2026-09-09/stage2-qa/。

阶段 3 实际验证（2026-09-10）：
- `model/tools.rs` 共享有界协议循环，`memory/agent.rs` 共享当前证据登记；Q&A 最多四轮、录入最多两轮只读取证后提交一次终结结果。原有统一搜索、范围、双方版本/草稿校验及回执撤销继续复用；schema 11 仅增加引用片段从属表。单篇整理、结论融合和两类专题推荐保持固定流程，MCP 工具不增加。
- 设置连接测试分别验证结构化 JSON、单次工具和多轮回传；未知配置保持基本流程，明确不支持工具则暂停自动整理。能力按配置指纹绑定，不因网络或鉴权失败误记为不支持。
- 完整 UI **128 项通过**；核心 Agent 集成 **7 项**、预算与证据边界所在核心单元 **23 项**、两个模型 probe 的 **3 项**通过。实际进程 phase2/phase6 检查通过，覆盖当前 schema 的并发写入、备份恢复和 MCP。完整 Rust 工作区/全目标 **159 项通过**（含实际 90 秒超时），Clippy warnings denied、fmt、前端生产构建通过；最终提示修正后相关 33 项再次通过。
- 原生独立 QA 包、隔离合成库和本地协议端点：未配置 AI 时长文立即可用；连接测试自动选择增强流程；问答搜索后补读 6000 字符位置，引用保留 `0..1200` 与 `6000..6128` 两段，预览分别展示。取消后问题保留、迟到回答未落盘。发现并修正 12000 字节旧限制误拒中文正文的问题，重试成功且正文与输入归档一致；最终一条 Memory、一条 capture、两个版本，integrity_check=ok。
- 独立 Review–Fix–复审：修复终结时间/字节预算未预留、首次未命中不能改查、重叠重复阅读误算进展，以及长中文返回边界；模型评估资产补齐不连续且未引用的阅读片段、完整实现/样例摘要和旧版本引用夹具。真实模型初轮漏检后补清短关键词语义与初始候选不是全库的取证提示；独立语义审核进一步发现“已决定”被改成“已执行”，补充动作阶段不可互换的约束并再次完整复测。增量复审未发现代码阻断项。
- 真实模型评估采用显式 `--agent`：`intelligence_probe` 旧 40 条加新增 20 条多语言归属案例，`discussion_probe` 12 条固定问答；只发送合成数据。路由、引用结构、语义审核分别记录，不把 JSON/工具协议通过当作准确率。问答复测 12/12 完成且引用覆盖正确，逐条复核未发现实质性事实错误；每题 2 次生成请求，p95 19.7 秒。长文补读与首次未命中改查由原生/核心协议案例另验，不把这 12 条都说成实际补读案例。整理最终 **57/60** 路由与核心应用通过（初轮 50/60）：2 条应归并却独立保留，1 条整段替换超过局部修改限制而被核心拒绝；原输入和目标均保留。独立语义审核另外记录 1 处轻微措辞偏差（“测试报告”写成“另一次测试报告”），不计为完全语义通过。每条 1–2 次生成请求，p95 12.1 秒。使用当时已配置的 `gpt-5.6-luna`，Embedding 关闭，测试的是基本搜索下的 Agent；这些固定开发标签尚非用户验证的准确率。完整输出、未通过案例、合成工具轨迹与检查日志在忽略的 `research/retrieval-2026-09-09/stage3-qa/`。

最终发布优化 app/DMG 构建成功，`codesign --verify --deep --strict` 与包内 MCP 实际 stdio 检查通过，仍为未公证的 ad-hoc 开发产物。以上是工程和受控 QA 证据，不代表用户验收、所有 BYOM 服务商兼容或真实个人语料质量。

### Dev 启动修复（2026-09-10）

- 2026-09-11 补修：用户实际使用的 `npm run tauri dev` 未经过上述 `dev:app` PATH 修复，仍在 `cargo metadata` 阶段报 ENOENT。现统一使用 `scripts/rust-env.mjs` 补齐 Cargo 安装路径，Tauri、prototype、sidecar 和 beta 构建入口共用；保留已有 PATH 优先级和自定义 CARGO_HOME，不修改用户 shell 配置。直接运行原命令已完成 Vite 启动、Cargo 编译和原生可执行文件启动，正常桌面环境复跑同样通过；2 项 PATH 回归测试通过。此次验证针对启动链路，不替代界面交互验收。

- 复现 `npm run dev:app` 在 PATH 不含 Rust 工具目录时直接退出：`spawnSync cargo ENOENT` 被 sidecar 构建脚本吞掉。启动命令现在在现有 PATH 后补充 `${CARGO_HOME:-$HOME/.cargo}/bin`，同时传递给 sidecar 构建和 Tauri；子进程无法启动时输出具体错误。
- 实测原先缺少 cargo 的环境可通过修正后的入口完成两个 sidecar 和原生 debug 编译，Vite 返回 HTTP 200，正常 macOS 环境中 Tauri/Vite/原生进程保持运行。直接调用构建脚本且未配置 PATH 时，明确报告 cargo ENOENT。脚本语法与 diff 空白检查通过。
- 沙箱首次原生启动受到 macOS 窗口服务限制，已停止并在正常桌面环境重启。UI 检查工具无法识别独立 debug 可执行文件，本轮未确认窗口视觉效果或完整交互，不作为产品验收。

### 统一自有模型缓存（2026-09-10 后续修正）

- 用户最终选择 Memivy 自有缓存目录。模型统一位于 `~/Library/Caches/com.memivy.app/models/`，开发版、安装版与测试资料库共用；不随资料库位置改变，不读取全局 HF 缓存配置。数据库、配置与派生索引仍留在各资料库。旧的按资料库模型路径由本次决定替代，历史 QA 记录保留。
- 核心状态、下载、清理和模型进程均使用同一用户缓存解析入口。测试 probe 复用共用模型，不再删除真实模型快照模拟中断；破坏性缓存测试只用小型临时夹具。设置页显示实际缓存路径。
- 已将本机旧测试缓存中的正确 Q8_0 文件复用到新目录，639150592 字节、SHA-256 与固定清单一致，未重复下载。真实模型 probe 使用隔离合成资料库，在新目录完成加载、建索引、3 条跨语言 hybrid 查询和编码边界检查；证据 `research/cache-model-probe.log`。测试资料库下未生成模型目录。
- 实测 14 项核心 embedding 测试、3 项设置 UI 测试、前端构建、core/embedding 全目标 Clippy warnings denied 通过。编译使用此前安装的 CMake 工具。此轮没有重新打包安装版，也未重复完整原生界面验收；安装版需要重新构建才包含本次路径变更。

### 输入期间界面闪烁修复（2026-09-10—11）

- 根因：草稿逐次写入 SQLite，原后台监听把所有 `PRAGMA data_version` 变化都广播为 `library-refresh`（750ms 周期）；列表每次刷新降低透明度、详情操作短暂禁用、相关记忆先清空并丢失勾选。向量/FTS 索引的内部写入也会触发同类整页刷新。
- schema 12 增加事务内界面数据修订计数，仅对界面可见的记忆、会话、回执、专题等表建立触发器；草稿和派生索引不推进计数。保留原有定向草稿同步与外部 MCP 更新。列表后台刷新保留正常透明度与已有结果，详情后台更新不切换工具栏禁用状态；相关记忆保留同一来源/版本/范围的内容和勾选，新结果到达后仅剔除失效选择。
- 核心回归先实际复现 `capture` 草稿写入错误触发刷新，再验证 6 类草稿的 CAS 保存/清除、FTS 重建、向量索引写入不触发刷新，真实记忆保存与置顶变化仍可见，未提交/回滚事务不可见。完整 core 测试通过；132 项 UI 测试通过（Vite watcher 在沙箱报 EMFILE，正常环境单独复跑通过），前端构建、全工作区全目标 Clippy warnings denied 和独立原生 QA app 构建通过。
- 原生 QA 使用 `com.memivy.flicker-qa` 和独立合成库：记一下、提问、Milkdown 正文输入英文及粘贴中文，3 份草稿落盘后修订值持续为 10；另一个进程保存新 Memory 后原生列表自动出现新记录，原编辑草稿保留。截图与 AX 检查未见原列表/按钮状态抖动；未验证真实中文输入法候选组合的完整过程。日志在忽略的 `research/flicker-*.log`。这是本次受控回归，不代表完整产品验收。

**架构根因复核及后续修复（2026-09-11，用户明确授权）：**上面的第一轮只排除了草稿和索引触发整库刷新，不能视为完整根治。进一步真实 DOM 复现确认 `Markdown.tsx` 每次渲染重新创建组件类型，原样正文也会卸载段落/标题并丢失选区；另复现后台刷新丢失分页、普通阅读读取失败清空正文、旧桌面状态覆盖新展开事件。现有模拟组件测试不足以证明 DOM 稳定性。

- 稳定 Markdown 组件类型与查询高亮上下文，输入预览下移到查询组件；保留 Milkdown 生命周期与组合输入保护。
- schema 13 增加有界事务变更元数据和资料库 epoch；核心统一提供按游标读取，主窗/快捷窗按实体订阅，重复唤醒不重复失效。TanStack Query 共享只读 IPC 缓存、去重、结构共享与过期请求丢弃，移除 App 的全局 revision 传播。
- 分页保留已加载数量与视口锚点，正文和回执在临时读取失败时保留；明确不可用使用结构化错误，移除旧缓存。外部正文新版本不直接替换正在阅读的版本。当前正文与归档载荷分开读取，计数与来源语义保留。
- 相关来源选择独立于推荐排名，讨论提交前验证来源仍可用；阅读历史讨论时不强制滚到底部。设置保留诊断信息，Embedding 操作与轮询统一排除乱序。原生快照带递增序号，并从几何/焦点快照路径移除数据库和配置 IO。
- 新增真实 React + DOM 选区/表格节点回归、资源缓存去重/实体失效/迟到回应、分页超过 100 条、读取失败与不可用、正文版本提示、原生状态乱序，以及跨连接提交/回滚、草稿排除、游标缺口和当前正文轻量读取测试。本授权不等于用户验收。
- 最终检查：完整 UI 回归 **142/142**、完整 workspace Rust **162 通过、2 项按既有标记忽略**；TypeScript/Vite、fmt check、全目标 Clippy `-D warnings`、含重新构建 MCP/Embedding sidecar 的 ad-hoc debug app 均通过。最后一处组合输入保护调整另跑输入/真实 DOM 回归 3/3，通过后重新构建 app。
- 原生隔离库 `research/runtime/stability-20260911/` 实测搜索输入英文与中文粘贴、Milkdown 英文输入、记录弹窗输入；外部 MemoryStore 进程新增时输入焦点与正文保留，已加载两页仍为 80 条；另一个进程修改当前正文后旧正文保留，点击新版本提示才更新。三份草稿均落盘。新版本等待期间相关查询的冲突噪声在随后修复，并由组件回归覆盖。日志和证据摘要存于该忽略目录。截图/AX 观察未见整页清空，不等同于持续逐帧证据；真实中文输入法候选组合全过程仍未验证。


**用户再次反馈后的系统性复核（2026-09-11）：**前一轮未覆盖逐次自动保存的反馈文案，不能视为全部闪烁已经解决。

- 检查正式应用全部输入/编辑界面、组件 key 与生命周期、全局 CSS 动画/透明度、资源刷新和跨窗口同步。确认搜索、新建、快捷捕捉/提问、讨论和正文编辑把每次草稿写入的 pending/ack 直接映射到说明文字，导致正常输入期间反复替换提示；现保持操作说明固定，正文使用“草稿自动保存在本机”描述机制，失败仍明确显示。useDraft 不再订阅仅影响瞬时保存文案的 saved 状态，保存队列/CAS/退出前 flush 保持不变。
- 确认跨窗口 draft-changed 只有空通知，另一窗口重读所有已访问草稿。现由 Rust 发出变化 key，前端只重读对应且正在挂载的草稿；重开编辑器仍从库读取。错误提示保留到实际写入成功，不再每次按键清除后又出现。
- 搜索展开期间只更新输入正文，收起后才更新顶部草稿摘要；桌面设置背景读取登录状态失败时保留已知控件状态。未发现其他逐按键重建组件的明确证据，未改动正常的提交进度和主动导航语义。
- 本轮完整前端回归 154/154；其中真实 React/DOM 使用实际 DraftQueue 加延迟 IPC 覆盖四种捕捉/提问呈现，逐次输入及写入完成后提示区域 0 个 DOM mutation、输入框/提示节点身份保持。另覆盖讨论/标题/正文提示、失败恢复、跨窗口定向读取、设置失败状态。TypeScript/Vite、Rust host check、fmt check 和 debug app 构建通过。日志 `/private/tmp/memivy-feedback-*.log`；原生视觉验证结果另记，不用这些结果替代物理输入法或持续逐帧验收。


- 本轮原生验证：以构建配置传入独立 identifier `com.memivy.feedback-qa`（仅修改 Info.plist 不足以隔离编译进程序的单实例标识），复用隔离合成资料库。实测搜索、新建、记忆标题/正文编辑、快捷捕捉和快捷提问连续英文输入，AX 变化集中在编辑文字和首次可提交按钮，底部说明保持，搜索顶部摘要在展开时保持，截图确认布局。未做真实中文输入法候选全过程或持续逐帧采样。没有停止用户正在运行的 dev。另跑 Rust 草稿跨窗口/CAS、刷新与 changefeed 相关 12 项测试全部通过。


### Dock 与悬浮图标修复（2026-09-11）

- 用户确认修复两个图标问题。Tauri dev 直接设置 NSImage，现由 npm 的共享 Tauri 入口仅为 dev 注入独立图标配置；`dev-icon.icns/png` 从既有品牌 SVG 导出，保留圆角并增加透明留白。build 仍使用既有满幅 `memivy.icns/app-icon.png`，文件内容保持不变，原始品牌素材未改。生成脚本同步维护两种输出。
- 悬浮按钮显式取消 WebKit 原生 appearance、清零 padding/border，图片改为 block，按钮不参与 flex 收缩；图片与阴影边界一致，保留键盘焦点轮廓和成功角标。
- 验证：TypeScript/Vite 构建、12 项快捷入口及 workspace 回归通过；独立资料库/端口的 `npm run tauri dev` 实际编译并运行成功。独立标识的 debug app 构建通过；原生收起窗口截图确认左侧白色底板消失、图片与阴影居中。dev Dock 验证到专用资源内容和实际启动，未取得 Dock 本体截图；未将资源预览描述为 Dock 截图验收。

### 本地语音输入（2026-09-11）

- 用户授权实现短语音输入及设置。主窗口记录/提问和桌面快捷入口复用 CaptureForm；本机 CPAL 采集、Rubato 16 kHz 转换、停顿分段，独立 llama.cpp/mtmd 进程同时使用 Metal 音频编码与解码。设置包含开关、下载/续传、加载/释放、预加载及快捷键；权重复用全局 HF 缓存约 1.02 GB，没有重下或复制模型。
- 实际通过：完整 Rust 回归 168 项（两项既有环境依赖用例 ignored）、最终语音 host 单测 5 项、前端回归 159 项、fmt、Clippy 全 targets 和 debug app 打包。Vite 文件监听测试在沙箱报 EMFILE，正常系统环境复跑通过；不是产品代码故障。新增测试覆盖静音边界、停顿和硬分段、原文保留、末段失败禁止提交、人工修改不被后续状态覆盖、私有暂存与配置损坏。
- 最终测试包以构建配置使用独立 identifier `com.memivy.voice-qa-20260911`（不只修改 Info.plist），复用原生隔离资料库 `/private/tmp/memivy-voice-qa/data`：实测三个输入区布局、实际麦克风收音、Metal GPU 加载、转写后中英文字修订及显式保存；保存后 session 清空、PCM 文件为 0。收起桌面面板和提问面板后录音均停止，草稿保留。测试外放合成英语再经麦克风收音有识别错词，不能替代真实用户准确率评估；未发送到外部生成模型。
- M4 Pro / 48 GB 开发构建：优化哈希依赖后，两次 Metal 完整加载（含 SHA 校验）4.03 / 3.13 秒；五秒静音经最终 stdio 管道转写 0.557 / 0.119 秒，均为空文字并正常结束。首次 Metal shader 准备另一次测得总加载 12.27 秒；CPU 完整加载 2.60 秒。以上不是低配 Mac 的性能保证，8 GB/M1 尚未实测。
- UI 后续优化：快捷输入按内容高度调整原生窗口，展开来源及同时显示录音状态均完整可见，收起恢复紧凑高度；停止键改为圆形底色与实心圆角方块，末段转写显示禁用的进度指示。已在隔离原生应用检查展开/收起和真实录音，恢复测试前草稿并移除测试录音；前端 160 项、host Clippy、前端构建及 debug app 打包通过。
- 语音快捷键原生录入和保存正常；自动化按键未触发系统全局热键，物理键盘唤起/停止仍待用户实按确认。麦克风拒绝授权、真实中文 IME 候选全过程及另一台 Mac 尚未实测。未宣称用户验收或正式签名发行。

- 后续统一缓存：用户确认语音与 embedding 共用系统缓存目录下固定 `com.memivy.app/models`，用 dirs 6.0 系统路径解析，Windows 对应 LocalAppData。本机两个语音 blob 校验 SHA-256 后硬链接到 App 缓存（同 inode，无重新下载或重复权重占用，保留 HF 来源）；隔离原生设置显示新路径，Metal 加载及释放成功。core 单测 28 项、语音前端 6 项、fmt、core Clippy、前端构建及 debug app 打包通过。未实测 Windows，也未将本次目录适配宣称为完整 Windows 支持。

- 全量 Review-Fix（2026-09-11）：覆盖全部 tracked diff 和未跟踪语音实现。补充迟到状态/卸载组件/启动后立即关闭的回归并先复现失败，再隔离状态读取与变更；完成转写但草稿冲突时禁止新录音静默清除；录音会话开始/清除/确认写盘失败保持可恢复状态，暂存清理失败不掩盖已成功的记忆保存。快捷键设置事务移到主线程避免锁等待反转；初次加载失败停止收音并保留已收到的分段；模型加载失败增加“校验并修复”入口，修正浏览器预览旧 HF 路径。
- 本轮验证：完整前端 166 项、Rust 171 项（2 项既有环境用例 ignored）、完整 Clippy、fmt、diff check、前端构建及独立 debug 包通过。原生隔离实例 `com.memivy.review-20260909`、`/private/tmp/memivy-voice-review/data` 实测快捷键注册/解绑/启停、Metal 加载、录音中关闭与重新打开草稿、手动编辑后保存及 PCM 清理；紧接着点击录音和关闭后 recording/starting/processing 均为 false、PCM 为 0；来源展开截图无溢出滚动条。退出该测试实例，未改动用户正在使用的资料库或共享模型文件。损坏模型修复入口使用状态夹具验证，未破坏真实共享权重；物理全局热键、Windows、真实 IME 候选验收仍保留原有边界。

## 5. 核心测试资产

**2026-09-07 实施范围：**用户要求“做一下 5. 核心测试资产”。本节将阶段 2–6 已有回归与新增固定材料连接成可重复执行的资产，不增加产品功能、数据库兼容流程或发布验收。原话安全、显式确认和版本冲突规则继续采用 PRD；正式 UI 的基准采用阶段 3–6 已确认的当前源代码，保留历史 Demo v0.1 的原有认可范围。

统一入口：`npm run test:core-assets`。它在忽略的 `research/core-tests/` 下创建全新运行目录，执行 Rust fmt、Clippy、全工作区/全目标测试、Node UI 回归、前端构建、实际进程/stdio 故障脚本，并准备原生视觉合成库。使用 Cargo 返回的实际可执行产物，支持自定义 target 目录；结果记录平台、代码/案例/二进制 SHA-256、各阶段退出码和日志。任一自动检查失败，命令返回非零。默认不读取本机模型配置、不调用外部模型。

真实模型评估：`npm run test:core-assets -- --models-only --model-config /absolute/path/to/private-model.json`。配置必须在仓库之外且为 `0600`，只发送版本控制中的合成材料；配置和 Key 不复制进结果。每次创建新目录，禁止覆盖旧成绩，保留错误案例和人工复核表，并扫描结果是否包含本次配置的 Key。`--models-only` 只运行模型资产，不代表离线回归通过。离线通过、模型结构通过、语义复核、原生交互和用户验收分别记录，不合并为一个准确率。

### 5.1 视觉与交互对照

- 固定操作与窗口规格在 [visual_contract.json](tests/assets/visual_contract.json)：主窗口 1140×790 与最小 760×560，覆盖首页、紧凑列表/宽正文、原话与版本、折叠菜单导出、讨论引用、审核保存、输入法、快捷面板、迟到回答与 MCP 刷新。
- [visual_fixture](crates/memivy-core/examples/visual_fixture.rs) 与搜索测试共用合成材料，只接受不存在的绝对目录，创建带当前版/旧版/原话、回收站、未保存草稿和固定讨论的正式 MemoryStore；MCP 关闭，无模型配置。统一入口生成 `visual-data/` 与初始全为 `not_run` 的 `visual-review.json`。
- 原生 QA 使用独立 bundle identifier 和显式 `MEMIVY_DATA_DIR`，启动后先核对固定合成内容再操作。每张截图记录窗口逻辑尺寸、截图像素、缩放、应用和源码摘要、案例 ID；截图只作证据。首次采集不自动成为用户认可的新基准，不能为了测试变绿更新认可记录。
- 浏览器预览负责基本布局和样式；当前预览不连接上述 SQLite 合成库，不将不同示例内容冒充逐像素同源对照。中文组词、焦点、物理快捷键、窗口交接与退出恢复在原生端逐项记录；本轮不重新定义已验收原型。
- [Tauri 当前测试文档](https://v2.tauri.app/develop/tests/webdriver/)已提供通过嵌入式服务驱动 macOS 的路径。本轮沿用现有原生 UI 检查工具，不引入新的运行时插件或 E2E 依赖；未执行的项目保留 `not_run`。

### 5.2 固定搜索集

[core_search.json](crates/memivy-core/tests/fixtures/core_search.json) 固定 7 条种子与 28 条查询，[core_search.rs](crates/memivy-core/tests/core_search.rs) 自动验证：

- 中文连续短语、错位子串、一/两字符、英文大小写和子串、URL、项目、引号、emoji 与字面符号；
- 当前正文与旧原话分别命中、跨当前正文/原话组合检索、旧版本独有正文不进入普通搜索；
- 时间下界包含/上界不包含、项目/来源过滤、无结果、回收站/草稿/普通会话排除；
- 相同查询通过正式记忆库和 MCP，索引重建前后结果集合一致，返回源可以实际解析；
- 另加入 55 个相近干扰项，核对标题优先、分页/有更多结果、MCP 数量上限和先过滤再限量，不绑定同分结果的随机 UUID 顺序。

使用项目 bundled SQLite，日志报告实际 SQLite 版本；不拿系统 Python SQLite 的 tokenizer 替代。三字符以下单独覆盖的依据是 [SQLite trigram 限制](https://www.sqlite.org/fts5.html#the_trigram_tokenizer)；[Joplin 的中日韩检索说明](https://joplinapp.org/help/apps/search/)也说明不能只用英文分词样本验证笔记检索。这里遵守 Memivy 已定的字面检索规则，不复制竞品的查询语言。

### 5.3 Memory Agent 合同集

- 复用 [organization_cases.json](crates/memivy-core/tests/fixtures/organization_cases.json) 的 **40 条语义案例**：16 new、16 append、8 defer；新想法、人物/项目不同、明确改变、犹豫/否定、因果不确定、小样本、缺少指代和提示注入均保留。
- [organization_review.json](crates/memivy-core/tests/fixtures/organization_review.json) 为每例补齐判定依据和必须保留的语义。标签是已有开发预期，**尚未声称经过用户逐条人工裁定**；有争议的标签先请用户裁定，不能看完模型输出后为提高分数改答案。
- [organization_failures.json](crates/memivy-core/tests/fixtures/organization_failures.json) 加入 **8 条独立故障合同**：未知目标、截断、空结果、429、500、连接中断、超限、正式 90 秒超时。由 [core_faults.rs](crates/memivy-core/tests/core_faults.rs) 经真实 loopback HTTP 和正式整理调用验证，不把这些确定性故障算进真实模型准确率。
- [intelligence_probe](crates/memivy-core/examples/intelligence_probe.rs) 保存候选、动作、目标、最终正文、原话保留、重放结果、耗时、固定输入/实现摘要及逐条复核表。动作或目标错误、核心拒绝写入、模型调用失败均计入失败，返回非零；正文含义、未涉及内容和不确定性需另行核对。

这 48 条合同不是模型排行榜，也不是任意用户资料上的准确率承诺。任何错误写入、原话丢失、无确认保存或凭空来源都阻止将相应验收认定为通过；未确认统一的模型质量百分比不擅自设成产品发布门槛。

### 5.4 记忆问答与结论保存案例

[core_discussion.json](crates/memivy-core/tests/fixtures/core_discussion.json) 提供 **12 条真实模型问答案例**与逐例语义要求；[discussion_probe](crates/memivy-core/examples/discussion_probe.rs) 在隔离库中执行：单条回答、空库/无相关证据、当前与历史变化、跨记录背景、不同人物观点冲突、连续追问、新建议、假设、提示注入、中英文与重新打开数据目录。

每轮保留实际回答、完整的已取回依据（含最终未引用的节选）、冻结引用节选、预期来源、独立关键词搜索对照和未写入记忆断言。来源 ID 覆盖是结构检查，不能证明每句话有依据。`review.json` 分别记录检索漏失、回答来源支持度和不确定性；错误必须归因于检索、证据选择、回答或服务故障，不能合并为“回答失败”。[NotebookLM 的来源与引用说明](https://support.google.com/notebooklm/answer/16164461?hl=en)用于参考用户核对来源的需求，不将出现引用号等同于事实正确。

历史夹具 `tests/assets/conclusion_contract.json` 将 **8 项保存/生命周期合同**映射到当时的可执行回归：确认前隔离、逐字保留修改稿、默认追加/审核融合、重试去重、并发冲突、完整稿重启保留、撤销后原话仍在、删除会话保留结论、固定版本与删除来源、取消后的迟到响应。测试模拟显式确认；原生真实点击和跨窗口交接仍通过 5.1 单独记录。该旧结论协议夹具已由 2026-09-12 第二 Memory Agent 的现行行为测试取代，本段历史证据不变。

### 5.5 不可妥协的故障测试

| 故障 | 可重复资产及判定 |
|---|---|
| 原话/版本确认后立即杀进程 | [verify_memory_store.py](scripts/verify_memory_store.py)：实际 SIGKILL 后检查原话、版本、回执与完整性。 |
| 模型坏响应、限流、超时、中断与重复结果 | `core_faults.rs` 的 8 条合同：无部分记忆写入，原话与关键词搜索仍可用；明确重试后只产生一个版本/回执。 |
| AI 事务执行到最后失败 | `organization_failure_after_receipt_and_index_updates_rolls_back_every_effect` 在更新整理任务终态前触发 SQL ABORT；检查版本、来源状态、回执、搜索词及索引回滚，同一 attempt 可恢复。 |
| 已认领整理任务强杀 | [verify_organization_recovery.py](scripts/verify_organization_recovery.py)：重启沿用 attempt，恰好应用一次。 |
| 取消后迟到响应、生成中退出 | `memory_discussion.rs` / `memory_data.rs` 的取消和恢复回归；[verify_core_faults.py](scripts/verify_core_faults.py) 强杀已持久化 pending 问答进程，重启变 interrupted，长期记忆计数不变。 |
| 确认保存与编辑/删除并发 | `memory_intelligence.rs` / `memory_data.rs` 的过期目标、删除来源、完整稿保留及并发编辑回归。 |
| UI 核心入口与 MCP 同时写入 | [verify_mcp.py](scripts/verify_mcp.py)：真实 stdio 与独立 MemoryStore 进程并发写入、幂等、开关、边界及 SIGKILL。原生 UI 刷新另验。 |
| migration 中断后重启 | `verify_core_faults.py` 以仓库迁移 SQL 在 SQLite 未提交事务中设置测试停点并 SIGKILL，确认无半套 schema 后交给 MemoryStore 重新初始化；已有 DDL 失败回归验证迁移整体回滚。停点仅在测试 harness，不是对实际 `MemoryStore::open` 中途插桩，也不等于断电模拟。 |
| 索引删除和重建 | 原有 `rebuilding_missing_index_recovers_facts_and_preserves_erasure` 加 28 条双路径固定搜索集。 |
| 活动库备份与恢复 | `verify_memory_store.py` 在并发写入期间调用备份 API，并恢复到全新目录；`memory_data.rs` 核对历史、会话、回收站和来源。 |
| Key 出现在日志/导出/备份 | `core_faults.rs` 将合成 Key 放入真实配置与错误 HTTP 正文，检查脱敏错误、单篇导出、SQLite 及备份；模型运行另扫描整组证据是否包含实际配置 Key。 |

故障设计参考 [SQLite 的崩溃与故障注入测试](https://www.sqlite.org/testing.html)，本项目检查的是业务事务和应用恢复，不声称复现了 SQLite 全套磁盘 I/O 故障测试。

**本轮执行记录（2026-09-07）：**

- 统一离线入口完整通过：**92 项 Rust 全工作区/全目标测试、56 项 Node UI/资产测试**、fmt、Clippy（warnings denied）、TypeScript/Vite 构建，以及 Phase 2/5/6 和新增 core fault 进程脚本。实际 bundled SQLite 为 **3.53.2**。28 条搜索在记忆库/MCP、重建前后共 112 组结果对照通过；8 条正式模型失败合同包含实际 90 秒超时。强杀迁移事务和 pending 问答、在线备份后恢复全新目录均通过。
- 当前本机 `gpt-5.6-luna` 在新目录完成一次 40 条整理：**37/40** 通过动作、目标、核心写入、原话保留和重放检查，中位耗时 **5.35 秒**。new-09 返回空标题，append-09 以记忆标题替代 `M1` 编号，append-14 对短正文的局部修改超过一半保护限额；三者均被核心拒绝写入，原话保留。没有重跑挑选较好成绩，也没有降低核心保护规则。已请用户选择是否将提示词/输出约束修复扩展到本轮；未收到扩展选择时，保持测试资产范围。
- 首轮 12 条真实问答全部完成，来源集合检查 **12/12**，每轮长期记忆计数保持不变；Codex 逐条核对答案及成功整理正文/8 个 defer 理由。随后补齐“已取回但未引用的依据”诊断，并以对应确定性回归及全新目录复验：最终 12 条问答仍全部通过结构检查，但 **qa-09 的结论把收费未定的现状推定为一次性产品**，提出“从一次性产品转向持续交付服务”的无据前提。该轮回忆段有来源，结论推演仍须修正；已单独标记，不能声称语义 12/12。`codex-review.json` 与初始人工复核表分开保存，**不将代理复核冒充用户人工裁定**，也不据此承诺其他模型或真实资料效果。
- 独立 `com.memivy.coreassetsqa` debug bundle 使用上述固定合成库，原生核对首页、中文关键词结果、正文、原话、旧版本、折叠菜单导出、讨论和引用；默认窗口与缩至最小尺寸后的页面均留存截图。审核结论改为两行中文，选择已有目标后显示默认补充；数据库确认保存前 7 条捕捉、保存后 8 条，正文逐字等于旧全文加审核文字。点击撤销后旧正文恢复，确认原话仍在。原生测试包已退出。截图是 CUA 缩放后图像，设备 backing scale 未暴露，未冒充原始像素金图。
- 本轮未重新执行完整中文组词、物理全局快捷键、全屏/跨屏、融合预览、生成中退出和原生 MCP 刷新的全部交互清单；相关核心/组件/进程回归通过不替代原生项目。现有阶段验收记录保留，不声称整套外部发布或用户视觉验收完成。
- 完整原始证据位于忽略目录 `research/core-tests/20260907T133759Z-b320c847/`（最终离线；较早一轮为 `20260907T132020Z-9958a22a`）、`research/core-tests/20260907T132018Z-1a078b04/`（真实模型，整体状态正确记为 failed）及 `research/core-assets-native/`（原生及 `discussion-final/` 最终问答诊断）。两项评估可执行程序拒绝已存在目录且不改写旧结果的实际进程检查也通过。中间一次诊断补充编译失败，临时命令误执行了旧可执行文件；该组已标记不可用于新诊断证明，保留审计说明并排除出上述最终成绩。最终构建与重跑使用当前可执行文件，已验证所有问答案例含新诊断字段。真实 Key 扫描通过。测试材料/脚本纳入版本控制范围，真实运行结果、合成数据库、截图和私密配置不纳入。

## 6. 发布与观察

### macOS 外部测试

首轮观察四周，重点跟踪：

- 第一次记录后是否就能找回、提问并获得有用回应；
- 记忆后来是否真的被用户或 Agent 采用；
- 继续讨论是否帮助思考，用户是否愿意保存结论，以及结论后来是否再次使用；
- 回答是否有正确来源，证据不足与新建议是否表达清楚；
- AI 续错位置的比例；
- 暂不判断是否长期堆积；
- 用户通过关键词搜索、浏览、应用内问答还是外部 Agent 找回；
- 无 embedding 导致的真实漏搜案例；
- 用户是否自然要求手机与电脑共享同一份记忆。

只有手机访问需求被真实验证，才另立移动端与同步项目；当前不预先设计。

### 发布边界

- 只维护 macOS 的签名、安装、升级和卸载检查表；
- GitHub Release 不等于发布完成，必须在干净 Mac 安装；
- 升级必须保留数据库与本机配置；
- 官网、安装文档和发布页只列出 macOS。

## 7. 粗略工作量

原有 7–10 周估算对应以捕捉、整理和检索为主的旧范围，不能继续作为新版总工期。阶段 1 已完成，不重复计入剩余工作；本次不为尚待研究的交互编造排期。

| 工作 | 当前状态与重新估算依据 |
|---|---|
| 产品文档同步 | 本次范围；方向与待研究项在现有文档内同步 |
| 交互研究与新版 Demo | 阶段 3 已指定当前 dev 基准；后续 AI/快捷入口交互按各阶段推进 |
| 阶段 1 技术样机 | 已验收通过，保留现有结果与证据边界 |
| 阶段 2 核心数据 | 已实现并验证，见阶段 2 记录 |
| 阶段 3 首页与记忆库 | 已获开发授权；实现及实测见阶段 3 记录，尚未记录用户最终验收 |
| 阶段 4 快捷入口 | 已授权并接入正式数据；工程与原生验证见阶段 4，尚待整体用户验收 |
| 阶段 5 AI 完整闭环 | 已授权并完成本轮实现与开发验证：自动整理、逐段引用问答、继续讨论、已有目标结论保存与审核融合；详见阶段 5 记录，待用户体验验收 |
| 阶段 6 MCP 与 Mac 发布收口 | 已授权并完成开发包与本机验证；正式签名延期，另一台 Mac 与长期外测待完成 |

在交互研究和新版 Demo 确认后更新剩余工作量，不把本文列出的未来工作当成已经开始开发。

## 8. 开发期间不提前做的事

- 不因“以后可能同步”先加 CRDT 或 change log；
- 不因“以后可能语义搜索”先加 embedding 表和模型下载；
- 不同时实现 stdio 与 HTTP MCP；
- 不为 Windows、iOS、Android 构建空壳或预留适配层；
- 不构建、测试、发布或维护 Linux 版本；
- 不扩展正文编辑之外的块工作区、标签、图谱、插件或自动更新；
- 不因新增问答拆独立聊天服务、通用 Agent 框架或全量用户画像系统；
- 不把 API Key 放进内容数据库或导出文件。

## 9. 暂缓范围

Windows、iOS、Android 没有开发排期，也不是 Mac MVP 的验收条件。未来若恢复其中任一平台，单独立项并重新确认捕捉方式、MCP、同步和发布要求；不默认当前实现可以零成本移植。Linux 不支持。


### 统一模型设置与 BYOM（2026-09-11）

- 用户认可 `design-demo/settings-models/index.html`，接受标准 ASR 首版范围和 Embedding 切换重建规则后，明确「接受，开始做吧」，授权本轮正式实现。该 Demo 作为本轮设置视觉基准，原有阶段验收记录保持不变。
- 已接入四类设置、三项模型能力、复用连接、候选测试后应用、可关闭自动整理、语义索引生命周期及共享本地文件管理。ASR 为内置 Qwen3 或 OpenAI-compatible transcription；不包含专用厂商适配及实时协议。
- 自动检查：全 workspace Rust 178 项通过、2 项按既有标记忽略；包含新增标准接口、配置 CAS/凭据隔离、不同维度索引和旧任务失效回归。完整前端 167/167 通过；沙箱内文件监听案例曾遇 EMFILE，正常权限重跑全套通过。Clippy 全目标 `-D warnings` 通过，新增录音绑定快照回归后，原生 host 20/20 与全目标 Clippy 再次通过。
- 原生 QA：独立 `Memivy Models QA.app`，资料库 `/private/tmp/memivy-byom-qa-20260911`，仅连接本机合成 HTTP 服务。完成新增连接、4 维 Embedding 测试/启用、1 条合成记忆增量索引（16 字节向量）、错误候选测试失败且活动模型/索引不变；ASR 复用连接测试/启用，实际录音分段上传本机服务、停止收尾、转写编辑及关闭重开草稿保留。API 返回文字为合成固定文字，不作为识别质量证据。`models.json` 实测 0600。
- 启动证据限制：第一次沙箱内直接启动退出，随后 UI 工具打开测试包未继承隔离变量而显示默认资料库；发现后立即退出，未在该实例进行配置/录音/记忆操作。改由 `open --env MEMIVY_DATA_DIR=...` 显式启动，并确认空白资料库后进行上述 QA。
- 未验证：真实商业服务账号及识别质量、物理中文输入法组合、低端硬件/长时间负载。没有更改真实模型凭据或删除共享权重；未提交、推送或进行用户验收。

- 最终交付检查：包含新编译 MCP、Embedding、Speech sidecar 的完整 debug app 构建通过，`codesign --verify --deep --strict` 通过。重启最终包后，四类设置原生布局及包内 MCP stdio 握手/两项工具清单检查通过；MCP 总开关仍关闭。测试应用停留在 AI 与模型页面，使用独立合成资料库；本机合成接口仅用于协议体验。此产物为 ad-hoc 开发包，未公证。

- 2026-09-11 用户纠正配置流程：取消连接选择与右下角连接管理，每项能力直接填写自己的 API 地址、模型名称、API Key。Demo 和正式设置同步调整；候选配置测试不写入持久配置，应用时独立保存，其他能力保持原配置。

- 简化流程验证：针对设置与 MCP 的 9 项 UI 回归通过，包含三项能力直接填写地址/Key、失败不保存；前端构建、全目标 Clippy、fmt 与完整原生包构建通过。原生隔离库中从问答页直接填写并测试 LLM，测试后磁盘配置未变化，启用后成功保存且 Embedding/ASR 绑定完全不变。Demo 和原生页均已确认没有连接选择与右下角管理链接。

- 2026-09-11 全量 Review 与修复：检查全部已跟踪和未跟踪修改；修复候选 LLM 测试覆盖活动能力缓存、ASR 失败录音修正 Key 后仍使用旧凭据、配置启用失败留下半套状态、重复选择运行方式清空模型字段、新录音残留旧错误、关闭自动整理影响已完成回执的幂等重放，以及仅更换 Embedding Key 误提示重建。补充配置文件大小边界，清理 Demo 已弃用的连接管理逻辑，并修复测试中修改地址导致按钮卡住。
- Review 自动验证：workspace Rust 186 项通过、2 项按既有标记忽略；最后并发处理调整后再跑相关核心 48 项，全部通过。最终完整 UI 171/171、前端构建、全目标 Clippy `-D warnings`、fmt 与 diff whitespace 检查通过。沙箱文件监听测试的 EMFILE 在正常权限重跑后通过。完整原生 app 与三项 sidecar 已重新构建，最终界面修正后再次打包。
- Review 原生验证：使用显式隔离资料库及本机合成服务，候选 LLM 测试后 `models.json` 和活动能力缓存哈希保持不变；重复点击运行方式、本地/服务往返切换均保留模型草稿；Embedding 更换模型后索引恢复 1/1 就绪，向量 4 维，LLM/ASR 配置不变。Demo 同样验证重复选择和往返切换保留字段。接口为合成返回，不代表真实服务兼容性或 ASR 识别质量。测试工具曾自动启动未继承隔离环境的已退出测试包，发现显示默认资料库后立即退出，未在那里进行任何设置或记忆操作；正式验证均在显式隔离启动并确认合成记忆后进行。


## I18N 实施（2026-09-12）

用户授权完成国际化实施方案，保持架构职责、性能和既有 UI/UX；允许 Luna Max 处理独立文案任务，完成后必须由独立 Astra 执行 Review-Fix。授权及工程检查不代表用户验收。

### 已实现

- i18next/react-i18next：中英文六个命名空间静态内嵌、官方 key 类型、完整句子/插值/复数、Intl 日期数字；语义通知在呈现时翻译。检查器验证资源与正式工作区固定中文残留，样例及用户原文明确豁免。
- Rust 独立语言偏好、系统语言列表匹配、原子保存、事件先于快照与 revision 迟到保护；每个 WebView 独立实例、同窗编辑器根共享实例。失败重试不覆盖原偏好，切换不刷新业务数据。
- 主界面、设置、快捷窗口、原生自有菜单、错误提示与编辑器文案迁移；Milkdown 原地更新工具栏、表格和链接 DOM，不因 label/语言变化重建。
- 新系统错误与整理原因使用语义码；旧原因、恢复结果及用户/AI内容保留原文。Agent/基本问答均固定提交时语言上下文，模型同轮输出派生标题；整理和润色保留目标语言。
- macOS 内嵌 en/zh-Hans 权限说明；系统标准控件和权限弹窗语言仍由 macOS 选择。

### 实际验证

- 完整 UI 回归 **181/181 通过**。沙箱运行仅 watcher 测试出现 EMFILE；相同测试及完整套件在沙箱外通过，未放宽断言。语言专项包括真实 i18n/React、跨窗事件次序、失败重试、独立工具栏/插件 DOM、生产复数 guard、原生恢复入口。
- 完整 Rust 工作区测试 **198 通过、2 项既有忽略**；格式检查及全工作区/全目标 Clippy 通过。另补有界 Agent 中英混合证据+英文输出回归通过；基本路径测试覆盖英文持久化与未确认不入库。合成协议测试不等于真实模型质量验收。
- 浏览器真实 Milkdown 验证：英文/中文来回切换保持同一编辑器 DOM、受控正文、选区和滚动；格式修改后的 undo 保留，已有表格与工具栏文字更新。
- 原生隔离包实际检查：英文启动、中文/英文切换、快捷窗口语言与标题、重启持久化、混合文字草稿和主窗口交接；760×560 窗口及默认窗口的中英文主要布局检查。中文通过粘贴输入，不能当作实体输入法候选状态验证。
- QA 使用唯一 identifier `com.memivy.i18n.qa`。初次资料库放在仓库内，被既有凭据路径保护拒绝读取模型设置；后移到 `/private/tmp/memivy-i18n-qa-20260912-library` 原生重测成功，模型卡片与设置入口正常，安全规则保留。无日常资料库或共享模型缓存修改。
- 最终 debug QA 包包含配套 `memivy-mcp`、`memivy-embedding`、`memivy-speech`；en/zh-Hans `InfoPlist.strings` 校验通过，ad-hoc 签名 strict/deep 校验通过。测试证据位于忽略的 `research/i18n-2026-09-12/`；不创建额外研究报告。

### 独立审阅与边界

Astra 首轮发现五组问题：本地语音标签、恢复结果中文、错误被二次转换、本地 embedding 错误出口、原生初始化恢复入口；另补回执占位和复数 guard。已修复，并独立复核相关 UI 39/39 通过。最后的仓库配置路径错误改用专用 `model_configuration_path`，保留安全规则并通过路径/错误测试。Astra 最终独立 Review 通过，所有已发现问题关闭，无剩余代码阻塞；独立追加 Agent 语言测试 1/1、I18N 和 diff 检查通过。

仍需用户原生验收：真实中文 IME 候选过程、真实麦克风/模型任务进行中切换、系统权限弹窗以及 macOS 单应用语言变化。未以合成测试或构建成功声称这些已验证。未提交或推送，未打发布标签或声明正式发布。

### 全量改动 Review 与修复（2026-09-12 后续）

- 检查全部已跟踪和未跟踪的 I18N 改动，修复两处问题：导出成功提示以翻译后的字符串触发通知，切换语言会重新发布已关闭的提示；讨论的回答中断、依据无效和来源失效原因被泛化，丢失具体恢复建议。现改为保留语义通知并使用专用讨论提示，历史回答正文不变。
- 新增回归覆盖关闭导出提示后切换语言、讨论失败的双语恢复建议、历史正文保留及切换不重新读取讨论。完整 UI **183/183 通过**；沙箱内 watcher 案例的 EMFILE 在正常权限重跑后通过。前端构建、I18N 资源检查及 diff 检查通过。本轮修复未修改 Rust 逻辑，没有重复全量 Rust 测试；上轮结果与证据边界仍按上文记录。
- 已重新构建包含匹配 sidecar 的原生 debug QA 包，strict/deep 签名检查通过。通过实际文件句柄确认使用 `/private/tmp/memivy-i18n-qa-20260912-library`；原生保存一条合成中英混合记录，导出到 `/private/tmp/memivy-i18n-review-export-20260912.md`，核对正文原样保留，并检查英文设置与正文布局。测试应用已退出，未提交或推送。

### 通用设置视觉调整（2026-09-12）

- 用户否定语言下拉框样式及其放在「快捷入口」的归类，并授权按修订方案优化。设置第一项调整为「通用」，语言、启动和快捷入口分组呈现；普通入口默认通用，缺少模型与语音能力入口保留直接定位。
- 语言改为有原生 radio 键盘语义的紧凑三选一，沿用灰白底、浅黄色选中态、现有圆角与字号；窄窗口移到说明下方。实测修复了旧表单 label 外边距对控件高度的干扰。官方 DESIGN.md 与主界面布局未修改。
- 设置、导航和 I18N 相关 30 项回归通过，前端构建、I18N 检查通过。浏览器实际检查中英文、方向键切换及 760×560 布局；隔离原生 QA 包检查通用默认入口、分组、中英文三选一切换。沿用原隔离资料库，未改日常偏好或启动项；未提交或推送。

### 全应用 UI 细节检查与修复（2026-09-12）

- 清除设置区块、字段说明、折叠区叠加的无意义边框，以间距分组；保留导航、内容标签及固定操作栏的必要分界。统一高级设置折叠箭头，输出参数改为两个可键盘操作的选项。
- 记忆筛选和保存结论复用同一 Select，统一灰白底、浅黄选中态、圆角、焦点和选中标记；使用浏览器顶层 popover 避免被滚动弹窗裁切，覆盖方向键、回车、Escape、失焦与外部点击。修复 macOS 点击后键盘焦点、label 内选择后重开的问题。
- 修复空缓存路径形成灰色空条、MCP 操作按钮分散到两端、专题复选框被通用 label 样式排成纵向，以及历史回执末尾的孤立分隔线。
- 完整 UI 回归 **184/184 通过**，包含真实共享 Select 的键盘、选值、Escape 不关闭父弹窗、受控更新及禁用测试；前端构建、I18N 检查、diff 检查和最终原生包 strict/deep 签名检查通过。Rust 逻辑未改，本轮未重复全工作区 Rust 测试。
- 浏览器检查设置各类别、展开的高级设置/数据维护和下拉列表；使用合成资料库 `/private/tmp/memivy-ui-polish-20260912-library` 与独立 identifier `com.memivy.ui.polish.qa` 实查正文、历史版本、编辑选区工具栏、讨论/引用、保存结论、专题创建/归属、回顾、回收站及记录输入。最终原生包复核了下拉菜单方向键与回车选值、设置区块和专题复选框对齐。没有访问日常记忆，没有删除共用模型或启用外部连接。
- 本轮为视觉和控件交互验证，未重新验证真实模型生成、麦克风、全部异常状态与实体中文 IME；不以构建或合成数据检查代替用户验收。未提交或推送。

## 2026-09-12 第二 Memory Agent 与统一输入：实施中

用户通过 active goal 授权完整实施 [plan-goal](docs/goals/second-memory-agent-plan-goal.md)，过程和逐项实际证据单独记录在 [progress](docs/goals/second-memory-agent-progress.md)。本段不改变之前任何阶段的历史验收。

正式讨论重构为自然流式工具 Agent：主动全局召回、自动记忆维护、稳定输入／attempt／操作账本、版本来源与整次撤销、长会话压缩、统一输入与续聊。现行合同已同步 PRD、TECH_STACK、AGENTS；不保留旧 RAG、三段答案、旧结论融合的运行备用路径。

已实际完成一轮 UI 185 项测试、执行核心 26 项及多套专项，当前配置模型通过流式多轮协议，真实模型合成语义及隔离原生应用均已运行。语义首轮发现无依据日期、纯记录标记等失败，独立审查发现长文覆盖、摘要并发与来源可用性缺口，正在修复并重跑。**这不是 A01—A17 全部完成或用户验收**；最新检查、物理输入缺口及最终审查状态以 progress 为准。

2026-09-12 本轮收尾补充：最终完整 UI 188 项通过，Rust workspace 237 项通过、0 失败、2 项既有显式性能测试 ignored，fmt／严格 Clippy 通过；末次跨语言边界又通过 4 项真实上下文测试。第三轮独立代码复审及小修复复审通过。真实模型 9 个场景中 8 个在覆盖范围内通过，长对话压缩的原话来源关联仍失败，已提交用户选择后续合同／模型验证方向。原生已观察多项实际闭环，但 Mac 锁屏和真实麦克风／物理快捷键／中文 IME 使 A15—A16 未完成。故本目标仍未完成，未提交或推送；详细证据与阻塞见 progress。

2026-09-12 后续补验：独立证据审计发现并补齐 A04 实际请求预算／材料增减／超预算目录、A11 部分成功回执 UI、A13 首条已提交时取消后重开三组证据。新增测试及 probe 经独立复审通过，未改生产逻辑；完整 Rust 241 项和 UI 189 项通过，fmt／严格 Clippy 通过。当前模型另完成材料 A→A+B→B 及 360 条大目录真实分页、正文读取、版本引用，独立语义审查通过。A09 来源关联和 Mac 锁屏／物理输入条件未解决，因此整体目标仍未完成。

2026-09-12 用户接受逐项来源合同建议后恢复：共享写入改为正文每项绑定实际原话，事务核对引用、继承未改完整行、归档真实来源；没有更换模型或保留旧写入格式。回应类型改为“仅确认／回答讨论”，与是否写记忆及维护暂停分开。来源、事务及最后回应类型增量独立复审通过。最终完整 Rust 251 项、UI 189 项通过，fmt／严格 Clippy／I18N／前端构建通过，更新后的独立 QA 原生包构建成功。十场景真实模型复验已运行，最终独立语义判定和证据见 progress；不能因引用结构校验或测试通过认定模型事实正确。Mac 已解锁且原生专题推荐／移除／来源回跳补验完成；真实麦克风、物理快捷键和中文 IME 仍待用户结果，目标尚未完成。

本轮最后收束：补齐回答引用、摘要范围、建议行动状态三处提示并独立复审后，定向五场景三项通过、两项未过。剩余关键问题是压缩后的写入多归档不支持事实的纯提问；另有 Markdown／编辑残句的输出质量问题，真实来源按钮本身可用。最新版 fmt／严格 Clippy／原生构建及严格签名验证通过，未把模型语义失败标记成通过。建议用户指定另一模型作相同合成场景对照，当前配置未改；详细证据及物理输入缺口仍见 progress。

2026-09-12 用户澄清工程责任边界：上下文、提示和执行逻辑必须正确，模型能力不作为实现方可保证项。已同步 plan-goal／AGENTS，撤销其他模型对照待决策；现有语义失败保留原始证据，按实际请求／工具参数／存储核对归为模型质量观察，不伪称通过。工程与自动验证完成；真实麦克风、物理快捷键和中文 IME 仍未验收。

2026-09-12 最终包原生补验：实际运行 `Memivy Agent QA Updated.app`，完成合成记录、自然回执、差异查看及整轮撤销。独立 SQLite 核对原话／来源／写入均一次、撤销后退出检索且原话版本回执保留；证据见 progress 的 updated-build-smoke。补齐最新版运行验证，物理麦克风／快捷键／中文 IME 仍待实际操作，不将模拟输入算作物理验收。


### 2026-09-12 用户语音反馈后的修复

用户实际发现录音空白却显示完成、主窗点语音立即中断。已复现同讨论隐藏输入停止可见输入录音，并修复实例录音所有权；同时修复空片段／空识别误报成功、低音量与短尾音被丢弃。独立复审又发现旧提交误清后来录音，改为提交绑定 session ID 后复审通过。

最终 Rust 256 项、UI 196 项、i18n、fmt、Clippy、debug 包及签名验证通过。真实 ASR 合成中文正常／低音量均有完整句段与尾句；低音量有一处错词，保留观察。原生主窗持续录音 30 秒、快捷窗 13 秒、交接回主窗再录 34 秒，没有立即中断或跳窗，停止不发送；但默认内置麦克风且当前合盖，未取得非零输入，完整麦克风收音→回填仍待打开屏幕继续实测。此前“工程已完成”的当前结论已撤回，历史结果保留。详细证据见 [progress](docs/goals/second-memory-agent-progress.md) 末节及其 JSON，不以代码通过代替语音验收。

后续定位纠正：打开屏幕后仍全零。macOS TCC 日志确认实际包缺少 Hardened Runtime 的 `audio-input` entitlement，系统因此拒绝麦克风访问；合盖并非已证明的唯一原因。主配置补齐最小签名权限，实际包确认 entitlement=true 并通过签名校验后，主窗／快捷窗真实麦克风中文、完整尾句、双向草稿交接与显式发送已运行成功。beta 打包增加实际签名权限检查，防止缺权限包进入 DMG。物理全局快捷键和中文 IME 验收仍未替代，详见 progress 最新章节。

2026-09-12 用户确认实体全局快捷键及中文 IME 两项没有问题。随后完成对话 UI 修复：More actions 菜单／提示改为顶层防裁切浮层，回到最新改为消息区域底部圆形箭头，移除重复角色标签。主窗与快捷窗原生交互、独立复审、完整 UI 201 项、TypeScript／i18n、debug 包及签名验证通过；详细验证边界与证据见 progress 末节。

2026-09-13 修复记忆选择框异步加载撑高；用户明确选择 AI 自动概括对话标题。标题在回答结束后生成，仅用有界用户原话，一次成功后固定，失败不影响回答，旧会话下次成功续聊按需补生成。原生主窗／快捷窗真实模型分别得到“十分钟认识月相练习”和“每周二十分钟折纸入门选择”，标题即时刷新；选择框有结果／无结果边界一致。独立复审、完整 UI 202 项、标题测试 7 项及执行／迁移／数据回归 58 项、TypeScript／i18n／fmt／Clippy与原生构建签名验证通过。详见 progress 末节。

2026-09-13 按用户确认精简语音完成状态：成功回填并保存草稿后自动清理临时录音，不显示成功卡／Remove；录音保留波形时长，失败才有重试／丢弃。修复空录音跨输入框阻挡，保留所有权、并发和失败恢复。独立复审、语音26项及完整UI209项通过，主窗／快捷窗实际录音确认状态消失、草稿保留、无自动发送；原未发送草稿保留。详细边界及证据见progress末节。

2026-09-13 全部未提交改动完成代码 review-fix-review：修复引用补读范围丢失、手动保存／撤销摘要与历史失效、大量手动回执撑满上下文、主窗交接来源／quick 标记泄漏，以及桌面偏好并发、失败卡住与迟到标题覆盖。完整 UI213项、Rust272项（2项既有ignored）、fmt／严格Clippy／TypeScript／I18N及QA原生构建签名通过。Mac 锁屏导致新版原生交接复验尚未进行，已请求解锁；原有QA草稿及消息保持不变，未提交或推送。具体复现和证据见[progress](docs/goals/second-memory-agent-progress.md)末节。


2026-09-13 解锁后的原生复验发现 CoreAudio 初始化阻塞，已增加 10 秒启动截止、立即停止、单 worker 与旧会话隔离，并允许超时后直接提交已有文字。独立 review-fix-review 通过；修复包主窗／快捷窗真实收音、尾句回填、状态自动收起及交接通过，原 11 份草稿正文／26 条消息不变。UI 215/215、native host 44/44、类型／i18n／fmt／Clippy／原生构建／严格签名通过；系统阻塞的修复边界由确定性回归验证，不宣称强制复现了系统故障。完整证据见 [Agent progress](docs/goals/second-memory-agent-progress.md) 末节；按用户要求 amend，不推送。

### 2026-09-13 UI 精简实施

- 按用户批准的 UI 精简方案完成侧栏、设置、阅读／讨论及编辑文案精简；保留左右设置布局、白黄品牌、输入草稿、来源／历史与撤销。模型概览提取为纯展示组件，读取和轮询仍由 Settings 管理；无新增依赖、IPC、定时器或存储逻辑。
- 独立代理 review 后修复小于 580px 的设置标题样式回归，补充保存快捷键的可访问信息，复审无剩余阻断问题。删除本次不再使用的文案与样式；只调整现有行为测试，没有添加检查“旧文案消失”的测试。
- UI 回归 214/215 在沙箱内通过；唯一文件监听测试遇到环境 EMFILE，在沙箱外单独运行通过。最终受影响的 UI／I18N 回归 38/38、`npm run i18n:check`、`npm run build`、diff 检查通过。构建仍提示主包较大，本轮没有引入拆包或性能架构改造；未重复未修改的 Rust 行为测试。
- 浏览器实查 1280×720、760×560 中英文设置，使用共享 ModelOverview 的隔离样例检查长模型名与已配置状态，540px 下复验隐藏标题及按钮不被挤压。样例与测试配置仅在忽略的 `research/ui-simplification/` 中。
- ad-hoc 原生测试包 `com.memivy.ui.qa` 使用 `/private/tmp/memivy-ui-qa-20260913` 合成资料库：760×560 设置、模型概览／语音详情、长标题阅读、中文粘贴草稿跨设置保留、⌘S 保存新版本、查看历史及撤销、快捷窗有／无来源均实际检查。首次测试配置漏带 capture 窗口导致启动失败，补齐原配置的窗口列表后验证完成；产品窗口配置未改。
- 本轮中文验证覆盖粘贴、编辑和草稿保留，未重新验证实体中文 IME 候选提交、真实麦克风／模型执行或系统登录启动；保留既有验收边界，不将本次视觉检查当作这些能力的新验收。

### 2026-09-13 桌面图标与快捷键解耦

- 按用户最新决定删除“暂停快捷入口”，设置页与菜单栏统一为“显示／隐藏桌面图标”。全局快捷键允许设置、修改和移除；新安装默认未设置，已有配置保留。图标显隐不改变快捷键，移除快捷键不改变图标或菜单入口；同步更新 PRD 与 UI 方案。
- 原生注册路径接受空值且不注册；更改失败保留设置，持久化失败回滚注册。桌面／语音回调核对当前配置，丢弃换键或移除后排队的旧事件。修复 WebKit 下录键按钮未获取焦点的问题；没有新增轮询或配置框架。
- 独立 review-fix-review 无剩余阻断问题；UI 216/216、native host 44/44、严格 Clippy、fmt、i18n、前端构建与原生包签名检查通过。
- 隔离原生资料库实际验证：隐藏图标保留快捷键值、录入 ⌃⌘M 后保存成功、移除后显示“设置快捷键”、重启仍保持未设置、图标开启时移除快捷键仍保持开启。760×560 下检查设置排版。自动化录键可用，但此次自动化全局按键尝试没有观察到唤起，不能作为实体全局快捷键触发的验收；真实语音录音未重复执行。

### 2026-09-13 通用默认值与即时快捷键设置

用户确认默认快捷窗口 ⌥M、录音 ⌥R，快捷键录入即保存；通用页增加“还原默认设置”，仅恢复语言跟随系统、开机启动开启、图标显示开启和两快捷键。明确关闭或清除的现有配置保留。正式 MCP 缺配置时默认开启，显式关闭、损坏配置仍关闭；通用还原不触及 MCP、模型或数据。

实现复用既有设置接口；快捷键恢复失败回滚原绑定和图标状态，回滚失败显示部分恢复提示。首次登录启动初始化及系统查询在后台执行，系统变更事件更新界面，旧读取不能覆盖新状态，无新增轮询。独立代理两轮发现并修复快捷键恢复失败、同值重试及启动状态竞态，最终复查无阻断问题。

验证：UI 全套 219 项通过，唯一文件监听用例在沙箱遇到 EMFILE，正常监听环境单独通过；后续新增登录事件竞态测试及通用／桌面／MCP 聚焦 14 项通过。原生 host 46 项、正式 MCP 7 项通过；前端构建、i18n、格式及 diff 检查通过，host/core clippy 无警告。760×560 原生 QA 中确认 ⌥M 录入即保存，移除绑定及关闭图标后还原得到 ⌥M／⌥R、图标开启和系统登录项开启；测试应用登录项随后关闭以免开机启动 QA。实际按键唤起与真实麦克风录音未在本次自动化中证实，不将注册成功当作该边界验收。

最终签名 QA 构建重启后确认手动关闭的登录启动仍关闭，两个快捷键和图标状态保持；外部连接页显示 MCP 已开启且不出现通用还原入口。最终 app 构建及 codesign 完整性检查通过。

### 2026-09-13 正式应用工程名清理

将正式宿主 Cargo 包名及默认二进制名从 `memivy-phase1` 改为 `memivy`，同步锁文件、beta 打包版本检查及现有测试夹具。独立旧样机的协议标识和历史验证记录保留。Cargo metadata 确认包／bin 均为 `memivy`，`cargo check -p memivy --offline`、9 项打包测试与 diff 检查通过。

`cargo build -p memivy --locked --offline` 通过，已生成 `target/debug/memivy`。

### 2026-09-13 全界面一致性复查

按用户反馈检查通用、模型总览／配置、数据、外部连接、列表、正式阅读／编辑、讨论、专题弹窗、回收站及桌面快捷窗口。集中修复设置的重复控件：两快捷键共用 ShortcutSetting，持久化与错误仍由原模块负责；录制／默认／空值／移除保持相同尺寸及列对齐。图标、MCP、预加载、快速响应使用统一开关样式；统一设置行、按钮高度、语言区间距和窄窗口导航，清除旧快捷键／语音开关 CSS 与失效文案。录制中 Tab 可正常移动焦点，Escape／失焦取消录制，保持 IME 和失败保值语义。

验证：UI 全套 224 项通过，文件监听用例沙箱 EMFILE 后在正常环境单独通过；新增语音 owner 集成用例及共享录制聚焦 5 项通过。前端构建、i18n、diff 检查和签名原生 QA app 构建通过。原生 760×560 检查共享快捷键的默认／录制／移除／重设、即时保存及 Tab 焦点；浏览器检查英文／中文与 760×560、500×720 响应式布局及底部还原入口。独立 review 无阻断问题，已采纳减少动态效果时统一关闭开关动画的建议。未改 Rust、存储、模型调用或编辑器生命周期；本轮不重复声称真实录音或物理全局快捷键验收。

### 2026-09-13 用户视角文案清理

检查中英文页面、弹窗、错误提示及原生菜单文案。去除面向用户的开发版／安装版说明、构建命令、内部管道／编码／预热等表达；模型存储改为说明空间占用、删除影响和重新下载，错误提示说明问题及可执行的下一步。保留发送内容、费用、删除与恢复的必要边界。移除模型存储中的内部缓存路径和语音后端标记；错误码与业务行为不变。

验证：文案和插值校验、前端构建通过；全套 UI 中 224 项通过，1 项旧文案断言更新后在模型/MCP 聚焦 12 项中通过，文件监听仍受沙箱 EMFILE 限制。浏览器展开模型管理区域，核对实际中文文案和布局。未新增仅断言禁用词的测试。

### 2026-09-13 移除查询指令设置入口

按用户要求移除语义检索配置中的高级查询指令、查询前缀输入和 Qwen 推荐按钮，清理对应中英文文案。内置模型的查询处理及已保存的外部模型配置不变。前端构建、i18n 校验、模型配置相关 7 项测试通过；浏览器核对外部模型配置的实际布局与入口移除，未重复原生或模型质量验收。

### 2026-09-13 查询指令全链路移除与残留清理

用户进一步要求连同调用移除，取代上一条仅移除入口的范围。删除本地与在线查询的任务指令拼接、Binding 字段、校验错误和前端夹具字段；在线连接测试只编码一次合成文本。查询保留长度边界与本地 Unicode 规范化，编码指纹同步更新，已有启用索引沿原机制重新准备。未增加迁移或旧字段兼容分支。

复查发现并移除无产品调用的旧完整语音设置分支、专用样式、失效设置文案、未使用的类型导入和状态返回值；现有语音下载／修复测试转为覆盖实际使用的设置分支，夹具补齐实际状态字段。未新增仅断言已删除符号不存在的测试。PRD 与技术说明同步。

验证：核心单元及模型测试 47 项通过，Rust host Clippy 通过；全前端未使用变量／参数检查、构建、i18n 和 diff 检查通过。UI 全套 225 项通过，文件监听项在沙箱报 EMFILE；浏览器检查现有语音配置布局。未运行真实模型检索质量对照或重新进行原生录音验收。

文件监听项在正常权限环境单独通过，UI 共 226 项均已取得通过结果。

### 2026-09-13 全部未提交改动 Review-Fix

检查全部 tracked diff 与新增文件，并独立复查原生／核心及修复结果。修复：只有显式修改快捷键才重试注册，图标及其他设置不受注册失败阻塞；Embedding worker 通信目录纳入编码指纹，避免新版复用尚未退出的旧编码进程；恢复语音按钮键盘焦点；通用还原后半段失败明确报告部分完成。清除无产品调用的旧 EmbeddingSettings 及独占文案，将其下载／继续／重试测试迁到实际 ModelCapability；对已启用索引的增量处理失败提供数量与重试，准备／暂停／关闭状态仍优先。修复 MCP 诊断测试对旧文案的无效断言，同步安装说明。未新增空壳兼容或仅检查删除符号的测试。

验证：全 workspace Rust 278 项、UI 232 项均取得通过结果（UI 全套 231 项，监听项受沙箱 EMFILE 限制，在正常权限环境单独通过）；workspace Clippy、fmt、前端未使用变量检查、构建、i18n 与 diff 检查通过。新回归以真实 Unix socket 验证旧 worker 不被复用，以实际模型设置组件验证部分失败及活动状态。独立最终复查无剩余阻断问题。

原生：重建隔离签名 QA app，在无效／系统保留快捷键导致启动注册失败时，验证桌面图标关闭、开启均落盘成功；测试后恢复原快捷键与显示状态，QA 登录启动仍关闭。浏览器 Tab 到语音按钮确认焦点环可见。此检查不代表实际模型检索质量或物理全局快捷键触发验收；这些边界本轮未重跑。

### 2026-09-13 快捷窗口留白与来源入口修复

用户截图指出前轮漏审：来源入口收起时右对齐、展开后跳到左侧，旧最小窗口高度在底部留下空白。来源区改为固定左对齐，输入区缩减顶部间距，输入窗口从 260 高度按实际内容伸缩；固定／图标设置不再重置已测量高度。沿用原 ResizeObserver 和原生窗口边界，无新增状态或轮询。

验证：500px 浏览器实测来源入口展开前后 x 坐标均为 25px；重建隔离原生 QA 包，实查中文输入页、来源展开／收起、固定／取消固定及小窗收起重开，底部按内容收缩，链接输入框未裁切。中文粘贴草稿与来源链接在重开后保留，未提交测试输入，测试后清空本轮草稿并恢复 QA 桌面偏好。独立代码复查无阻断问题；前端构建、fmt、9 项桌面原生单元、19 项输入／导航回归、签名及 diff 检查通过。本次不替代实体中文 IME 或全局快捷键验收。

用户随后发现展开瞬间闪窗，上一轮静态画面未覆盖该问题。Tao 的尺寸、位置 setter 分别异步排队，现改为主线程一次 AppKit frame 更新，跳过相同尺寸／位置；内容伸缩保持顶部，仅触及工作区时避让，叶子打开／收起保留原锚点。取消固定状态变化触发的重复测量，没有增加延时、隐藏重显或轮询。独立复核通过，10 项桌面回归（含 1x／2x、负坐标和底边限制）、Clippy、fmt、前端／原生构建及签名通过。新原生包实际操作普通／固定状态下反复展开收起、保留来源输入和整个小窗重开；未取得逐帧录屏或混合 DPI 多屏实机证据，不将操作后截图视为瞬态无闪动的完整证明。

### 2026-09-13 全应用展开与弹窗复查

按用户要求检查设置四分类、列表／筛选、专题弹窗、阅读／编辑／历史、讨论与来源、主窗口输入和桌面快捷入口。修复共享 Select 打开时选中项藏在长列表外、键盘导航可能带动外层滚动；仅调整菜单自身滚动位置。修复输入弹窗在进入原生对话框顶层前抢焦点的问题，打开后聚焦搜索／名称输入或指定的取消按钮，恢复焦点不滚动背景。统一筛选展开箭头方向，并将本地模型状态样式与旧样机同名规则分离，消除被拉成全宽胶囊的错误样式。未新增依赖、轮询或延时补丁。

验证：浏览器复现并修复长列表选中项不可见、弹窗焦点错误；检查 760／1280 宽度。隔离原生 760×560 检查设置展开、阅读／历史／选区工具栏、取消恢复版本、讨论来源弹窗；主窗口及桌面普通／讨论模式的记忆选择框均聚焦搜索，短窗口内搜索和结果可见；最终包核对紧凑模型状态及来源展开边界。UI 233 项均取得通过结果（全套 232 项通过，文件监听项受沙箱 EMFILE 限制，正常权限环境单独通过）；构建、i18n、签名、diff 检查与独立复查通过。无记忆提交或版本恢复；本轮不重复真实模型、实体 IME、全局快捷键或逐帧瞬态验收。

### 2026-09-13 再次复核全部未提交改动

复查全部 tracked diff、新增组件／测试与文档，独立代理分别检查原生／核心、共享 UI 与文案／残留。修复桌面配置损坏或读取失败时被误当首次安装的问题：只有缺文件才初始化开机启动，读取失败保留错误和原文件；补充真实损坏文件、目录读取失败及缺文件回归。同步技术文档中遗漏的 MCP 缺配置默认开启说明。其余未发现新的确定缺陷。

验证：全 workspace Rust 280 项、UI 233 项通过，Clippy、fmt、未使用变量检查、i18n、前端／原生构建、签名及 diff 检查通过。隔离原生包使用损坏的桌面配置启动，通用页显示读取错误；启动后原文件仍保留，登录项未开启（此 QA 包的系统状态为 not_found）。测试后恢复隔离配置。未改用户资料库；真实模型、实体录音／IME、全局快捷键和逐帧闪窗本轮未重跑。


## 2026-09-13：移除旧交互样机

用户明确要求彻底删除旧交互样机，并禁止增加墓碑测试。本轮删除前后端样机入口、独立 Store/conversation/DataPaths、样机 MCP 程序、旧 SQL、专用 probe 和验证脚本及测试；移除正式宿主从旧目录导入模型配置的逻辑与旧目录特判。正式共用图标独立保留，基础样式改为 `src/base.css` 并清除旧页面专用规则。源码导读和当前开发说明同步更新。没有新增检查旧文件或旧入口不存在的测试，也没有删除用户磁盘上的历史资料库。

原模型超时后的本地保存/搜索测试改用 MemoryStore；配置路径保护测试移入正式 storage 模块并使用正式应用路径。模型连接 probe 改为独立的 `model_probe`，不再打开旧数据库。正式 MCP 进程验证脚本修正过时的默认开关假设，改为显式关闭后验证禁用行为；不改变产品开关默认值。

实际验证：

- `npm run build` 通过；前端 233 项中 232 项在沙箱中通过，文件监听测试因沙箱 EMFILE 失败，沙箱外单独重跑通过。
- `cargo test --workspace --all-targets --offline`：264 项通过、2 项显式性能测试忽略。迁入配置保护测试后，`cargo test -p memivy storage:: --offline` 的 5 项全部通过。
- `cargo fmt --all -- --check`、`cargo clippy --workspace --all-targets --offline -- -D warnings`、`git diff --check` 通过。
- 重建正式 `memory_probe` 与 `memivy-mcp`；`verify_memory_store.py` 和 `verify_mcp.py` 的真实进程、stdio、并发写入、崩溃持久性、备份恢复及完整性检查通过。
- 正式 debug app 构建通过。浏览器确认列表与阅读页面布局；额外构建独立 identifier `com.memivy.sourceqa` 的临时测试包，在 `/private/tmp/memivy-source-removal-qa` 检查原生主窗口、中文粘贴草稿收起保留和桌面快捷输入窗口。
- 首次沙箱内启动未能使用测试资料库，发现后停止该检查进程，再在沙箱外以显式隔离目录启动，确认测试数据库创建后继续。未在默认资料库进行测试输入。

本轮未进行真实模型或麦克风转写复验，不将入口与布局检查扩展为完整产品验收。未提交或推送。


本轮全量改动 Review（2026-09-13）：复查全部 tracked diff 与新增 `base.css`、独立模型 probe、源码导读，核对删除模块的正式调用方、样式选择器、配置路径保护与正式数据库边界，未发现新的代码缺陷。修正 AGENTS.md 中已失效的延迟诊断入口说明与原生构建命令参数位置，明确先核对实际测试资料库再做原生输入检查；没有增加墓碑测试。复跑前端构建、i18n、fmt、Clippy、全 workspace/all-targets Rust 测试（268 通过、2 项显式性能测试忽略）。前端 233 项中 232 项在沙箱通过，文件监听项沙箱 EMFILE 后在沙箱外通过。文档本地链接和 diff 检查通过。本轮原生证据沿用上面的同版代码检查，没有再次运行真实模型或录音。无待用户决策项，未提交或推送。


## 功能命名清理（2026-09-14）

用户要求源码不以开发阶段命名。验证脚本按记忆库、整理恢复和 MCP 职责改名，整理夹具改为 `organization_cases.json`，同步调用、导入和文档路径。移除主窗口／快捷入口的 `formal` 样式前缀及注释、测试名和错误文案中的样机区分措辞。历史阶段与验收记录保留；拖动事件和磁盘恢复协议中的 `phase` 表示实际运行状态，不属于开发阶段命名，不改动持久化格式。

验证中发现整理恢复脚本仍使用立即生成记忆之前的版本数假设及旧 capture 返回字段，已按当前 `capture_id` / `memory_id` / `version_id` 修正，并验证原版本保留、整理只增加一个版本、不另建记忆、重试不重复写入和原话逐字保留。

实际检查：Rust fmt、Clippy、全 workspace/all-targets 测试及前端构建通过；前端 232 项在沙箱内通过，文件监听项因沙箱 EMFILE 单独在沙箱外通过，共 233 项通过。记忆库多进程、整理强杀恢复、核心故障、MCP stdio、活动库备份恢复检查通过。初次汇总保留失败记录于 `research/core-tests/20260914T022027Z-4426df51/`，修复后单独重跑失败项，不改写原始记录。浏览器检查主窗口及快捷入口布局正常，样式值和交互逻辑未改变。本轮未重新打包原生 app，未复验原生输入或真实模型；未提交或推送。

## 2026-09-14 发布数据库基线

用户授权开发资料库重新创建，将结构彻底合并为单一 schema 1，不保留开发期兼容迁移。

- `migrations/memory/001_initial.sql` 直接创建当前完整结构；目录仅保留此一个初始化文件。运行时 FTS 重建脚本移至 `crates/memivy-core/src/memory/sql/rebuild_search.sql`。
- 删除逐版本升级、原话提升和草稿转换函数及专用测试；移除无写入方的结论意图表、旧原话关键词表、对应六个触发器和消息旧回答字段，清除其导出与删除分支。
- 保留数据库身份／版本检查、同快照与写锁后二次校验、初始化事务和外键检查、WAL/FULL，以及当前版本／来源／撤销业务语义。
- 核对合并前最终结构与新库：170 个保留对象定义一致（忽略空白，消息表仅移除上述字段），外键检查通过。全工作区离线测试 262 通过、2 忽略；fmt、全目标 Clippy（warnings denied）、diff 空白检查通过。
- 已重建 memory_probe 与 memivy-mcp，真实进程脚本 verify_memory_store、verify_core_faults、verify_mcp 通过，覆盖多进程首次建库／写入、幂等、SIGKILL 回滚与持久性、备份恢复、MCP stdio 和完整性。MCP 首轮误用未重建的旧二进制，独立重建后复测通过。
- 本轮没有修改用户磁盘上的开发数据库；没有原生 UI 验收、发布构建或发布操作。


复审修复：应用内恢复备份曾残留按 schema 数字判断向量能力的分支，归一版本后跳过索引重置。新增含实际合成向量的应用恢复回归，先复现修订号未变化，再移除版本条件，恢复时始终清空派生向量并刷新修订。发布基线同时使用独立 application ID `0x4d454d59`，避免版本重新从 1 编号时发生格式身份碰撞；无旧格式识别或兼容分支。补充错误 application ID 的打开、写入及备份恢复拒绝测试。复审重新从 HEAD 迁移生成参考数据库，核对 170 个保留结构对象、初始数据和运行时 FTS 重建结构一致。

复审后验证：全工作区离线测试 **263 通过、2 忽略、0 失败**；fmt、全目标 Clippy（warnings denied）、diff 检查通过。重建后的 memory_probe／memivy-mcp 通过记忆库多进程、核心强杀故障和 MCP stdio 三套进程检查。本次未做原生 UI／真实模型验收，未提交或推送。


## 2026-09-14 GitHub 0.1.0 发布准备

用户确认 MIT / Huang Bo、`boh5/memivy`、`0.1.0` / `v0.1.0`，不标记预发布；采用免费 ad-hoc 签名，不购买 Apple Developer，不做公证。本轮授权为「先做出来我看看」，仅制作本地可审查结果，未提交、推送、创建远程仓库或发布。

- 添加中英文 README、MIT、安装/升级/隐私/贡献/安全说明、变更记录、问题和 PR 模板、Dependabot 配置。GitHub 链接为预定发布位置，当前尚未发布；未使用私人资料截图或旧样机图片充当正式界面。
- CI 在 `macos-26` 跑现有合成回归及恢复进程检查。tag 工作流复用 CI，核对版本与 main 历史，构建同源码 app/三个 sidecar、检查包内 MCP，生成草稿 Release。Action 固定 SHA；构建只读 token，上传任务仅 contents write / actions read；不替换已存在版本。
- `build:release` 替代旧 beta 名称，保留签名与 audio-input 检查，补齐所有 workspace/npm/lock/Tauri/tag 版本校验；主应用及 DMG 均附许可证。第三方依赖声明从锁定依赖与上游许可快照生成，模型权重不入包。少数上游只提供许可声明，保留来源与范围，不声称法律审查完成。
- 实际验证：release 元数据及 actionlint 通过；Rust fmt / 严格 Clippy 通过，workspace all-targets **264 通过、2 ignored**；前端构建与 i18n 通过；完整 UI **238/238**。首次沙箱运行仅 Vite 文件监视测试出现 EMFILE，提高句柄上限仍失败；同测试与完整 UI 在沙箱外通过。其余 core-assets 阶段均通过，保留首次 suite 的失败记录，不覆盖证据。
- 合成 MemoryStore、组织恢复、核心故障、MCP 检查通过；另跑 live MCP 备份恢复通过。打包回归验证新产物选择、篡改拒绝、麦克风 entitlement、版本和缺失辅助进程等边界。
- Gitleaks 8.30.1 扫描全部 30 个可达提交及导出的待公开源码均无发现；npm audit 0。cargo-audit 0.22.2 报告 vulnerabilities=0，同时有 6 项 unmaintained 和 1 项 glib unsound 信息警告。glib / proc-macro-error 不在当前 ARM64 macOS 依赖树；unic 系列经 Tauri/urlpattern 引入，保留维护观察，不借发布准备擅自升级依赖图。
- 最终 DMG 已只读挂载复验：严格签名检查通过，包内四个可执行文件齐全，版本 0.1.0，应用内与 DMG 中的三份许可证文件一致；挂载后的真实 MCP 合成协议/并发/故障检查通过，SHA-256 与旁侧校验文件一致。
- 本地正式 app 与 DMG 已构建。沙箱内 hdiutil 报设备未配置，获运行环境批准后在沙箱外成功生成 `target/release/bundle/dmg/Memivy_0.1.0_aarch64.dmg` 和校验文件；未进行 Apple 公证。
- 干净临时源码目录完成 npm ci / 前端构建；本地 native 使用现有工具链和 Cargo 下载缓存，不能替代首次 GitHub runner 的冷构建证据。当前 SDK 为 27.0，部署目标仍为 26.0，不等于在 macOS 26.0 最低系统实测。
- 尚未验证：首次 GitHub Actions 实跑、另一台 Mac 从浏览器下载安装与 Gatekeeper 放行、完整最终包原生输入/语音/升级验收。专用合成预览包已构建，原生 UI 工具多次超时，未取得正式截图；截图和外机验收待补，不使用构建成功代替。

证据：`research/core-tests/20260914T112037Z-362353e8/`（第一次 suite），`/private/tmp/memivy-release-ui-verified.log`，`/private/tmp/memivy-release-complete-build.log`，`/private/tmp/memivy-release-{npm-audit,rust-audit,gitleaks,worktree-leaks}.json`；临时源码验证 `/private/tmp/memivy-source-{install,build}.log`。对外操作与最终安装验收仍按 docs/RELEASING.md 执行。


## 2026-09-14 English source and repository guidance

- Converted project-authored source, comments, prompts, errors, scripts, ordinary test examples and executable demos to English. Preserved localized UI catalogs and moved necessary multilingual/Unicode payloads into dedicated data fixtures without changing their original text. User libraries were not modified.
- Localized editor placeholders, language labels and native export dialog text. Expanded the existing language check to repository code, with regression checks that reject executable files hidden under fixture or locale directories.
- Replaced the chronological AGENTS.md with current scope, language, data, UI, verification and release rules. Historical implementation evidence remains here. Independent reviews found and resolved missing fixture references, an export assertion tied to original input and omitted manual collection membership guidance.
- Verification: repository language check, diff check, Rust formatting and all-target Clippy passed. The full core-assets run passed all 241 UI tests, frontend build, harness build and all process checks; its initial Rust failure is retained in research/core-tests/20260914T131842Z-ae98e546. After fixes, workspace all-target tests passed (264 passed, 2 ignored); backup/restore concurrency checks also passed.
- Built an ad-hoc signed isolated native QA app and verified its actual temporary database path before interacting. Native Chinese/English switching and saving mixed-language original input passed; screenshots confirmed readable native and demo layouts. This was pasted input, not physical IME, microphone, global-hotkey or installation acceptance.
- Real-model review of record/question, global focus and state changes confirmed English system requests with Chinese replies and saved content, valid global-memory citations, preserved original captures and correct tentative/decided/executed distinctions. One malformed source ID was rejected and corrected on retry; verbosity and per-part source granularity remain model-quality observations. No translation-related engineering defect was found. Evidence is under ignored research/english-code/model-check.
- The real-model pause scenario did not pass: the model mapped a continuing pause request to pause_this_turn, then wrote a tentative idea on the next turn. Independent comparison with the original prompt confirmed the continuing-pause rule was preserved in English; the actual tool call chose the wrong scope. Record this as unwanted persistence in synthetic data, not a passing pause check or a demonstrated translation defect. No semantic classifier or unrelated agent behavior change was added.
- All four real-model cases completed (15 turns). Final all-target tests: 264 passed, 2 ignored; UI tests: 241 passed. No commit, push or release was performed.


## 2026-09-14 Settings copy refinement

Updated English and Chinese settings copy and the settings demo: AI assistant naming, local/API choices, floating button, voice model unloading, reasoning controls, restore wording and keyword/semantic search terminology. Clarified that automatic note organization does not disable memory updates during chats. Kept data-sharing information visible and moved semantic-search details into an expandable explanation. No model, storage or permission behavior changed.

Validation: frontend build, repository language check, demo JavaScript syntax and diff check passed; 52 related settings, voice and localization tests passed. Browser checks covered English AI settings, Chinese settings and external access, and the updated demo. Native packaging and model calls were not rerun for this copy-only change. No commit or push.


## 2026-09-14 Development startup recovery

Confirmed startup abort: the existing default library still had development schema 17 while the public baseline accepts schema 1. A synthetic schema-17 database reproduced Tauri setup panic and exit 134. Development commands now default to a separate persistent com.memivy.app.dev directory; explicit MEMIVY_DATA_DIR overrides remain supported, and the old library is retained. Library initialization/recovery stays after single-instance admission and handles failure explicitly instead of returning an error into Tauri's native setup callback.

Validation: default npm run dev:app started and remained running; lsof confirmed the development database path. A second launch returned successfully without opening its incompatible test library. With no first instance, the incompatible fixture now exits 1 with a clear error, no panic, and unchanged schema/tables. Native all-target Clippy, formatting, language and diff checks passed. Independent review caught and resolved an initial recovery-ordering risk before final verification. The unbundled dev executable was unavailable to the UI automation app selector, so no visual window acceptance is claimed. No old data was deleted or migrated; no commit or push.

The user subsequently explicitly requested deletion of the old library. Verified that its schema was 17 and no process held it open, then removed the old memivy.db and checked for SQLite side files. Model configuration and shared model downloads were retained.


## 2026-09-14 Inline multilingual test samples

The user corrected the English-only rule: necessary Chinese test and diagnostic samples belong directly in code. Removed fixture maps, small sample files and lookup/formatting wrappers created solely to move Chinese out of source; retained existing shared corpora and language cases. Restored the focused UI/catalog i18n check instead of banning Chinese throughout tests. AGENTS.md now requires stopping for user input on major problems, requirement conflicts or substantial changes to the agreed approach.

Validation: independent review passed; all-target Rust tests passed (264 passed, 2 ignored), followed by 64 focused tests after final cleanup. Frontend tests passed (239 total; the sandbox-blocked file-watcher test passed outside the sandbox), and the final Markdown test passed. Formatting, Clippy, i18n and diff checks passed. No app behavior change, commit or push.


## 2026-09-14 Full change review and leftover cleanup

Reviewed the full pending diff in three independent scopes: production runtime, tests/process scripts, and UI/demo/documentation. Removed the unnecessary memory/locales vocabulary files and restored the original inline arrays; also removed empty extraction directories. Cleaned remaining formatting-only test churn, restored real Chinese in samples marked CJK, and corrected demo grammar and search terminology. Model compatibility failures now state that the check failed rather than claiming the model definitively lacks a capability. No new directory, data loader or product behavior was introduced.

Independent reviews found no further confirmed runtime regression. Frontend build, strict all-target Clippy, i18n, syntax and diff checks passed; 58 focused UI tests and the 7 model-settings tests passed. Full all-target Rust regression passed (264 passed, 2 ignored); evidence is in /private/tmp/memivy-full-review-rust.log. No commit or push.

The user rejected automatically overriding the development data directory. Reverted the launcher change completely, removed the new startup-message suggestion, and changed README commands to plain npm run dev:app. The existing explicit override remains because MCP and restore process tests use it for isolation; existing MCP library routing is unchanged. Native dev startup passed with the normal com.memivy.app database path verified via lsof. Launcher checks, formatting, i18n and diff checks passed.


## 2026-09-15 Reading hierarchy and workspace refinement

Implemented the approved detail comparison across the formal workspace: stronger title/body hierarchy, centered reading width, labeled editing/discussion actions, expandable sources and history, compact reversible change summaries, clearer list/sidebar typography, hideable and resizable sidebars, and quieter Quick capture spacing and controls. Kept the white/yellow branding, selected logo, three-column structure and shared selection-based Markdown editor. Applied Apple HIG principles of content hierarchy, familiar navigation and accessible controls to the existing React/Tauri interface; material and dark-mode experiments remain outside this implementation.

Extracted MemoryEvidence to load archives only on demand and reuse them across disclosure changes. Same-record refresh retains historical selections and comparison controllers, while restores wait for refreshed archive data. PaneResizeHandle changes CSS widths without rerendering sibling editors, persists bounded preferences and supports keyboard adjustment with actual visible bounds. Existing storage, model prompts, receipts and concurrent-edit checks remain authoritative; no dependency or backend changes.

Validation: frontend build, i18n, 244 UI tests, Rust regression (264 passed, 2 ignored), formatting and all-target Clippy passed. The resize test also passed after removing its i18n initialization warning. Independent review found and resolved visible-width keyboard bounds and historical-state refresh issues; final review found no additional concrete defects. Browser QA caught and fixed main-column collapse when hiding the sidebar; an 800px viewport passed detail/list navigation. Native ad-hoc QA package passed reading/history expansion, Chinese text paste, draft save/reopen, Command+S version creation, sidebar hiding and pointer resizing, Quick Enter newline and unsent draft handoff to the main window. lsof verified the synthetic /private/tmp/memivy-design-library database. Repository-local QA configuration was rejected by the existing model-config path protection, so the fixture was moved outside Git.

Limits: native Chinese IME composition and microphone recording were not revalidated in this pass (Chinese pasted text was verified). The synthetic Alt+M shortcut did not visibly activate through the automation tool; sidebar activation and native Quick handoff worked, so no global-shortcut acceptance is claimed. Existing bundle-size warning remains (main JS approximately 696 kB before gzip); no broad code-splitting change was introduced. Build/test logs are under ignored research/design-qa. No commit, push or release.

## 2026-09-15 Independent code review follow-up

At the user's request, added another independent code reviewer and reviewed the entire pending UI diff without visual inspection. Fixed the list resize observer remaining attached to a detached pane after switching collection/review/trash scopes: App now shares the list identity with the resize effect so observation is reconnected. Preserved the saved preferred width at narrow startup instead of overwriting the CSS preference with the temporary window limit. Fixed archive restore eligibility when an external update returns a newer head while the reader deliberately retains the old version: restoration now requires matching archive and displayed state/head, in addition to the request identity.

Added regression coverage for replacement list nodes, narrow startup and subsequent widening, keyboard bounds, and newer archive heads remaining read-only until the reader adopts them. The independent reviewer rechecked both fixes and reran all 7 focused cases. Full UI regression passed (246 tests); build, i18n and diff checks passed. No backend code changed, no visual review was performed, and no commit or push was made. Logs: ignored research/design-qa/review-*.log.


## 2026-09-15 已授权的三项简化

- 模型配置删除 Connection 列表、名称、引用 ID、查找与清理链路，各能力直接保存地址、模型和 Key。旧配置在文件边界原子转换，保留凭据、revision 与已有录音；语音重试只保留一份私有会话配置，不把 Key 写入录音文件或返回前端。保留既有旧 LLM 文件导入路径。
- 后台整理提交记忆与回执后立即结束，共享工具循环增加明确的成功结束结果；不执行同批后续工具，也不再发出无用确认请求。来源、版本、事务和重试校验保留。
- `workspace_settings` 缩为 configured 状态，通过统一配置读取并验证。
- 用户明确要求保留追问建议：生成、超时、存储与界面均未裁减；讨论回调只适配成功结束结果类型。
- 验证：全工作区 Rust 测试、Clippy、前端构建与 i18n 检查通过；UI 246 项中的 245 项在沙箱通过，文件监听项在沙箱内 EMFILE，单项沙箱外复跑通过。配置与整理变更经独立审查、修复与复审。
- 原生 QA：独立应用标识、显式 `/private/tmp` 合成资料库，确认实际数据库与格式 2 配置落盘；旧模型地址与已保存 Key 正确显示，修改模型后经本地合成服务完成测试及应用，5 次协议请求均使用原 Key，其他能力配置保持不变，配置权限为 0600。构建保留既有大 chunk 提示；没有进行真实服务质量或真人录音验收。证据在忽略目录 `research/simplification-qa/`。

## 2026-09-15 Rig BYOM integration

- Added pinned rig-core 0.42.0 with reqwest and rustls only. All LLM entry points use Rig messages, tool definitions and completion responses directly. Provider selection supports OpenAI-compatible Chat, OpenAI Responses, Anthropic and Gemini with custom endpoints and model IDs. No Rig Agent, runner or vector store was added; embedding and speech retain their existing implementations.
- Preserved the bounded tool loop, cancellation, committed-operation retries, source filtering, reversible receipts and follow-up suggestions. Legacy execution checkpoints are decoded at the storage boundary without a schema change or rewriting displayed history. Provider tool IDs and signatures survive tool-result round trips.
- Retained request/response byte limits, deadlines and terminal checks through the shared HTTP transport. Added checks for Rig behaviors that can otherwise conceal refusals, missing Chat terminal markers, incomplete sibling calls or malformed arguments. No tool executes until the complete response passes validation; output truncation remains a distinct error.
- Settings default old configurations to OpenAI-compatible. Changing protocol clears the draft credential and test proof; Gemini uses the provider root URL. Native isolated QA verified old settings display, protocol switching, five synthetic Gemini probe requests, saving/reopening settings and private 0600 configuration. Actual provider credentials and live-provider model quality were not tested.
- Independent runtime and configuration reviews found and resolved SDK argument-handling gaps and the Gemini default URL issue; final review found no remaining scoped issue. Focused four-protocol tests cover structured requests, arbitrary model IDs, tool signatures, refusals and truncated writes. Backup/restore concurrency verification passed with library integrity intact. Evidence is under ignored research/rig-evaluation and research/rig-native-qa. No commit, push or release.
- Final core-assets regression passed in research/core-tests/20260915T104819Z-f9e23150: formatting, all-target Clippy, 274 Rust tests (2 ignored), 247 UI tests, frontend and harness builds, memory-store, organization-recovery, fault and MCP process checks. The i18n check and native QA build also passed. The earlier aggregate run retained a test-only lint failure; it was fixed before this clean final run.

## 2026-09-15 Direct AI assistant settings save

- Removed the required connection test from AI assistant saving in both UI and host. Save validates configuration locally; an optional matching test can still supply its candidate and capability cache. Preserved existing credentials only for the same provider and normalized endpoint. Removed the Disable AI assistant button and use Save settings for both initial setup and edits. Voice and embedding activation retain their existing rules.
- Organization now checks capabilities on first use when no matching result is cached, consistent with discussion. Unsupported capabilities still produce an explicit failure and preserve the original capture.
- Verification: 249 UI tests, three host configuration tests, 16 provider/organization tests, frontend build, i18n, formatting and all-target Clippy passed. Independent review found no issue. Native isolated QA saved an edited model without testing against an unavailable synthetic endpoint, reopened the saved setting, verified the existing synthetic key and 0600 file permissions, and visually confirmed removal of the disable button. No personal library or actual provider credential was used. Evidence: ignored research/rig-evaluation/settings-save-*. No commit or push.

## 2026-09-15 Remove Agent control and activation gates

- Removed mandatory capability probes and their disk cache from discussion and organization. Actual user requests run immediately. Optional connection diagnostics remain available in settings; LLM tests no longer issue or store activation proofs, and LLM saves no longer consult proofs or roll back for capability-cache failures. Voice and embedding keep their existing activation checks.
- Removed set_turn_options, response classification, maintenance state APIs and their write guards. Agent follows the user's natural-language request about whether to write memories; completed answers always attempt follow-up suggestions. Removed classification-dependent quick-window auto-dismiss so answers and suggestions remain visible. Original text, source checks, receipts, versions, undo, cancellation and idempotent retries retain their existing boundaries.
- Kept the public initial schema unchanged; retired columns and old cache files are unused, without deleting user data. Retried legacy checkpoints use the current system instruction only in the outgoing request view, preserving stored protocol history. A pending removed control call receives a tool error and can continue normally.
- Independent runtime and settings reviews passed after fixing stale retry instructions. Synthetic tests exercise actual first requests without cache, write arguments and receipts, suggestions after brief acknowledgments, inactive legacy flags and retry from an old control checkpoint. Evidence is under ignored research/agent-simplification. No commit or push.
- Final core-assets run research/core-tests/20260915T112059Z-f466dbb0 passed: 273 Rust tests (2 ignored), 249 UI tests, format, all-target Clippy, frontend/harness builds and memory-store, organization-recovery, fault and MCP process checks. Backup/restore concurrency and i18n checks also passed.
- Native isolated QA saved settings with a directory occupying the retired cache-file path, preserving its synthetic key. Actual requests offered only the six remaining memory tools, without preflight calls. Complete Chinese pasted input produced one memory and applied receipt; the quick window displayed its answer, undo control and two suggestions together. An earlier automation typing attempt delivered only a digit; its unsupported write quote was rejected without creating a memory. The fixed synthetic reply is not evidence of live-model semantic quality. QA processes were stopped; real credentials, physical IME composition and live-provider quality were not tested.

## 2026-09-15 Independent review of all pending changes

Three independent reviewers inspected the complete pending implementation and related tests in separate scopes: model transport/dependencies, memory execution/data handling, and host/frontend settings. No new actionable code defects were found. The parent reviewed remaining documentation and examples, corrected stale references to removed turn controls and capability-cache activation, and preserved the concurrently updated AGENTS.md isolation rules.

No UI QA, native build or app launch was performed for this review. Verification evidence from research/core-tests/20260915T112059Z-f466dbb0 and backup/restore checks was inspected; compared source hashes differ only in an explanatory model-tools comment. Full tests were not repeated because no executable code changed. Diff check passed. No commit or push.

## 2026-09-15：固定开发身份与 QA 隔离

- 用户授权将安装版与开发版分开。`dev:app` 和新增 `qa:app` 共用 `Memivy Dev` / `com.memivy.app.dev`，固定运行包为 `~/Applications/Memivy Dev.app`。Cargo runner 将每次热重建的程序放入同一应用包，保留 Tauri/Vite 开发生命周期；不再按任务创建 QA 身份。
- Dev 默认库为 `~/Library/Application Support/com.memivy.app.dev/`。QA 必须显式指定绝对 `MEMIVY_DATA_DIR`，拒绝正式库、持久开发库及指向受保护目录的符号链接。原生宿主再次校验开发库不能覆盖正式目录；语音状态跟随实际打开的 Workspace 库。未迁移或删除既有个人数据。
- 原生会话使用用户级文件锁，覆盖 sidecar 准备、运行和退出清理。第二个会话明确拒绝进入；进程组退出采用有界等待，覆盖忽略退出信号的直接子进程与遗留后代。保留持久库，合成 QA 数据由执行者在确认结果后清理。
- 新开发库的快捷入口默认为 Option+Shift+M，语音快捷键为 Option+Shift+R；已有偏好保留。开发版不自动注册登录启动。安装版默认与发布身份保持不变。
- Tauri 开发模式自动嵌入根 `Info.plist`；该文件明确声明 Dev 身份。发布使用 `Info.release.plist` 显式覆盖名称、标识和可执行文件，避免 Tauri 合并元数据时遗留开发身份。已实际生成发布配置的 debug 验证包，核对为 `Memivy` / `com.memivy.app` / `memivy`，未安装或发布。
- 已验证：Python 4 项路径/进程隔离测试；宿主 50 项测试；宿主 Clippy 全目标、格式检查；前端构建和 i18n 检查。UI 249 项中 248 项在沙箱通过，文件监听项因沙箱 EMFILE 失败，在正常权限下单独复测通过。独立检查发现的语音目录、辅助程序来源、退出清理问题均已修复并复审。
- 原生证据：固定 Dev 窗口正常呈现；Rust 文件变更后同一路径重启；第二个 QA 被锁拦截；lsof 分别确认显式 QA 库和默认 Dev 库；正式库全部文件在本轮前后 SHA-256 一致。已退出验证进程并删除本次合成 QA 库。快捷键默认值已在原生设置显示；安装版与开发版同时运行、真实麦克风及完整安装升级流程未做本轮验收。
- 剩余系统历史项：刷新后的菜单栏设置仍显示旧小写 `memivy`。裸程序无法通过普通 LaunchServices 应用注销清除，控制中心配置受系统隐私权限保护；未重置其他菜单栏设置，未将该历史项记为清理完成。此前重新生成但已停止运行的 Rig QA 包已再次注销并删除。
- 临时证据位于忽略的 `research/dev-isolation-20260915/`。本轮未提交、推送或发布。

## 2026-09-15：全部未提交改动的独立审查与修复

- 两个独立子代理分别审阅核心模型／记忆执行改动，以及宿主／前端／开发启动器与相关文档，覆盖全部待提交文件。
- 修复 QA 路径比较遗漏 macOS 大小写及 Unicode 等价路径的问题；受保护目录尚未创建时也拒绝大小写别名。合成测试覆盖持久 Dev 库经符号链接移动后的 Unicode 别名，未访问或修改个人资料库。
- 修复终端 SIGHUP 导致会话包装器提前退出、留下独立进程组的问题；挂断现在进入已有的有界退出清理。启动器 7 项回归通过，并已接入 `test:core-assets`；路径与进程修复均经独立复审。
- 修复前完整回归 `research/core-tests/20260915T122905Z-6b682fdc` 通过：275 项 Rust 测试（另 2 项忽略）、249 项 UI 测试、格式、Clippy、前端与测试程序构建、存储／恢复／故障／MCP 测试。随后备份恢复并发检查、修复后宿主路径测试与全工作区 Clippy 通过。
- 修复旧执行记录恢复及新 Chat 流丢失推理签名的问题：保留三个既有推理字段的原始内容，用 Rig 工具调用的私有附加数据随 checkpoint 保存；请求副本对所有 Provider 移除内部键，仅在 Chat 请求中按工具消息出现次序及 wire ID 补回字段。补回后检查实际请求字节上限，跨轮复用工具 ID 不会串用签名。
- 独立复审补充了真实流中不同类型推理块复用 index 的情况：只合并连续且兼容的 text／summary 分片，加密块保持独立和原始顺序。合成测试验证新流回传、序列化后重试、旧 checkpoint 已提交回执不重复、内部字段不泄漏和请求大小限制；最终相关核心测试 31 项及核心 Clippy 通过，复审无剩余问题。
- 最终完整回归 `research/core-tests/20260915T124954Z-030a63f3` 全部通过：282 项 Rust 测试（另 2 项忽略）、249 项 UI 测试、7 项启动器测试、格式、全工作区 Clippy、前端与测试程序构建，以及全部离线进程检查。回归期间受检源码未变化；随后使用最终测试程序的备份恢复并发检查通过，i18n 与最终 diff check 通过。本轮未运行真实服务商、原生交互或安装验收，未提交或推送；菜单栏历史项按用户要求暂停处理。

## 2026-09-15：足迹统计页（原生验收待协调）

- 按用户本轮授权，将侧边栏「回顾」替换为「足迹 / Activity」。移除三条旧记忆轮播入口；展示当前记忆总数、记录天数、当前连续与最长连续天数，以及白黄配色的记录热力图。支持最近一年 / 历年、日期提示、键盘移动、日期记录分页与现有详情跳转。里程碑不在本轮范围。
- 新增 `MemoryStore::activity_summary` / `activity_records` 和两个主窗口只读命令；没有迁移、表结构、依赖或 AI 请求变更。按 active 原始 capture 去重，AI 分拆 / 版本编辑 / 同请求重试不增加次数，未保存讨论不计入。废纸篓和彻底删除的原文不计入，恢复后重新计入。讨论原文保留原消息时间，删除讨论后仍保留足迹。现有产品没有独立批量导入入口，整库恢复沿用原时间戳。
- 统计扫描元数据并返回稀疏日计数；日期明细使用时间索引与 40 条分页，只读取短摘要。SQLite 使用本机历史时区规则；前端日边界按日历计算，覆盖夏令时。当前连续允许今天尚未记录，但昨天有记录。
- 新增 5 个 Rust 数据测试，覆盖重试 / 编辑去重、同一讨论分拆为两条记忆、删除讨论、废纸篓恢复、历史原文跳转、时间边界、分页与 1 万条记录。新增 3 个前端测试覆盖闰年 / 跨年、23 / 25 小时日界线、异步响应乱序与详情跳转；替换已移除回顾的测试。
- 验证：`npm ci`、`cargo fetch --locked`、`npm run build`、`npm run i18n:check`、`cargo fmt --all -- --check`、全 workspace / all-targets Clippy 均通过。全 workspace Rust 测试通过；最终增补的 5 个足迹测试单独通过。前端全套 250 项在沙箱内通过，唯一文件监听测试报 EMFILE；正常系统环境单独复跑通过。最终前端相关 11 项复跑通过。
- `npm run test:core-assets` 的 Rust 测试、构建、存储 / 整理恢复 / 故障恢复 / MCP / 合成数据阶段通过。首次格式阶段撞上新增测试未格式化的中间状态，最终格式检查已通过；启动器进程测试和文件监听测试受沙箱限制，分别在正常环境复跑通过（启动器 7 项）。`python3 scripts/verify_restore.py` 通过，100 次并发 MCP 写入在恢复前备份中保留，完整性正常。首次汇总如实保留失败状态，没有改写测试日志。
- 性能：Apple Silicon、本机 debug 构建、隔离合成库中，1 万条原文聚合为 1000 个日期，输出 33027 字节；正常系统环境一次测量约 23.7 ms（沙箱测量约 476 ms）。此数值是本机样本，不是所有库的性能保证。
- 独立只读审查发现并修复了历史原文跳转和缓存失效顺序；复审无剩余可操作问题。浏览器已检查 1140×790 与 760×560 布局、中英文、年份、日期列表和现有详情跳转，窄窗口默认可见最近日期。
- **尚未完成原生验收**：既有 Memivy Dev 会话持有 `native-session.lock`（检查时 PID 20608），已请求用户在该会话所属任务或终端结束会话后继续。没有获取该锁、启动另一个原生会话、覆盖共享 Dev app、停止其他会话或触碰生产库。需要锁释放后，以绝对 `MEMIVY_DATA_DIR` 合成库和 `npm run qa:app` 完成原生显示、刷新与窗口缩放验收。未提交或推送。

### 足迹当天标记样式调整

- 按用户反馈，将今天格子内的深色框改为下方金色小圆点，选中日期改为 1px 柔和金色描边；保留键盘焦点样式，并给圆点留出底部空间。
- 前端构建和 diff 检查通过；浏览器确认点击当天后使用金色选中描边与圆点。既有原生会话仍持有测试锁，本次未进行原生复测。

### 全量未提交改动独立审查

- 按用户要求，由一个独立子代理检查全部 19 个原有改动文件（含未跟踪文件），确认 1 个 P2：年初显式年度视图只生成已过去的周列，正方形格子被撑大，造成数千像素高的热力图。
- 已修复：显式年度视图始终保留全年周列，未来日期占位但不可见、不可交互；窄窗口按今天的位置滚动，避免停在空白的年末。新增普通年 / 闰年 1 月 1 日、1 月 15 日测试，测试 DOM 补充必要的空查询方法。
- 子代理复审无剩余可确认问题。正常系统环境完整前端回归 252/252 通过，构建、i18n 与 diff 检查通过。真实浏览器合成日期检查：1 月 1 日 / 15 日均为 53 列，窄窗口网格约 91.5px 高，今天可见，分别只有 1 / 15 个可交互日期。临时测试页面与预览进程已清理。原生会话锁限制未解除，未触碰已有会话；未提交。


## 2026-09-16：CI 缓存与 Release 并行化

- 保留单个 macOS CI job；CI 与 Release 打包启用 npm 下载缓存，以及固定提交版本的 Rust 依赖／编译缓存。Rust 缓存区分 job、工具链、锁文件、Xcode 和编译环境，继续执行全部安装与验证步骤。
- Release 验证和打包并行，创建草稿必须等待两者成功。未修改人工安装验收及发布流程；验证失败时并行打包仍可能消耗算力。
- 验证：actionlint 1.7.12 检查两个工作流通过，git diff --check 通过，独立静态审查无发现。仅修改工作流与文档，未执行应用构建或原生 QA。
- 未触发远端 Actions；缓存命中率及提速效果待实际运行确认。GitHub 缓存受 ref 作用域限制，不保证不同 Release 标签间复用打包缓存。本轮未提交、推送或发布。

## 2026-09-16：快捷窗口首次发送后的布局修复

- 原因：`discussion_submit` 先通过 `remember_topic` 设置了当前讨论，前端随后执行的 `desktop_update` 已看不到 topic 变化，因而跳过原生窗口从输入模式到讨论模式的扩展。输入区占满约 300px 高的小窗，消息区被挤空。
- 修复：在主线程记录首次讨论时同步扩展为 500×620 的讨论窗口，并继续按屏幕工作区约束位置和尺寸。后续消息不重复调整几何，已收起的悬浮按钮不被后台提交拉大；窗口布局失败不阻止已持久化的输入继续处理。
- 小窗讨论输入框不再允许手动拖高；输入区最高占讨论区的一半，超出部分内部滚动，并与消息区保留间距。主窗口编辑器未改动。
- 验证：新增首次提交／后续提交／新讨论与收起状态回归测试，补充可选偏好保存失败时的扩展断言。`npm run build`、`npm run i18n:check`、前端 256 项测试、Rust workspace 288 项测试（既有 2 项 ignored）、fmt、全 workspace/all-targets Clippy 与 diff 检查通过。独立只读审查无可确认问题。
- 原生验证：使用现有 QA 启动器和共享锁，确认资料库为 `/private/tmp/memivy-readme-media-rufdeg6k`。检查了中英文首次发送立即扩展、连续对话、长草稿、附件展开、Command+Enter，以及主窗口接力后草稿保留。未测试真实麦克风、输入法候选组合或更小的物理显示器；工作区约束由现有几何测试覆盖。
- 必要的故障与修复截图、测试输出保留于忽略目录 `research/quick-window-layout-20260916/`。原生热重载期间一次签名进程 SIGBUS，正常退出并通过原启动器重启后完成验证；没有更换应用身份或绕过签名。该隔离库包含用户现场输入，保留其内容供后续工作。README 素材制作暂停，未修改 README、未提交或发布。

## 2026-09-17: Release workflow repair and 0.1.1 preparation

- The v0.1.0 release run built the DMG but failed packaged MCP verification because the independent package job had not built `memory_probe`. Build the release/arm64 probe in that job and pass its explicit absolute path through `MEMIVY_TEST_PROBE`.
- Upgrade checkout, setup-node and upload-artifact to official Node 24 versions pinned by commit SHA. Preserve the application's Node 24.12.0 toolchain and existing permissions/caches.
- Prepare 0.1.1 and retain the failed v0.1.0 tag unchanged, following the new-version rule for source fixes. No public release or database migration occurred.
- Validation: release metadata, 15 release tests, actionlint, diff check and independent review passed. Built the probe with the exact new Cargo command; the packaged MCP protocol/concurrency/durability/integrity suite passed using this probe and the existing local packaged MCP binary. The new CI-generated 0.1.1 package still requires remote validation and downloaded-app installation acceptance.
