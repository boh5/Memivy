# Memivy MVP 产品需求文档

状态：产品方向已确认；2026-09-06 阶段 1 第二版已通过用户手工验收，阶段 2 核心数据层已实现并完成数据验证
更新日期：2026-09-12

视觉规范：[DESIGN.md](DESIGN.md)（已选定 Miro；仅约束视觉，不改变本文功能范围）

Demo v0.1 保留原有视觉参考地位。原阶段 1 已验收通过；用户在交互研究后另行授权改造现有代码，制作包含桌面悬浮助手的阶段 1 第二版原生交互样机。该授权用于亲手体验和技术验证，不代表正式产品交互已验收或阶段 2 已启动。记录见 [DEVELOPMENT_PLAN.md](DEVELOPMENT_PLAN.md)。

2026-09-06 后续授权：用户在核心数据层研究与方案汇报后确认“好，做吧”，批准阶段 2 及下述删除规则。该授权不包含新界面、正式 AI 整理或后续阶段的实现。

2026-09-06 后续验收：修复模型配置路径与原生点击/输入焦点问题后，用户确认已给出的检查与测试点全部通过，阶段 1 第二版在该样机检查范围内验收完成。具体清单与证据边界见开发计划；阶段 2 正式数据层尚未接入界面。

2026-09-12 当前授权：实施[第二 Memory Agent 与统一输入](docs/goals/second-memory-agent-plan-goal.md)。以下现行行为按该计划同步；历史阶段验收保留，当前实现与未完成验收见[实施进度](docs/goals/second-memory-agent-progress.md)，不因更新需求文档视为完成。

## 1. 产品定义

> **Memivy 是一个随时可以“记一下、问一问、接着想”的 AI-native 个人记忆工具：留下想法，找回过去的自己，结合已有记忆继续讨论，并自动留下新的想法、事实和决定。**

用户负责表达和判断，AI 负责整理记忆、找回相关背景、协助思考和维护当前理解。原话先安全保存，AI 对记忆的修改有回执、有来源、可纠正、可撤销。用户可以浏览和编辑记忆库，也可以直接围绕自己的记忆提问。

“第二大脑”是产品愿景，落实为可使用、可核对、持续生长的个人记忆。即使不连接任何外部 Agent，Memivy 本身也应让用户愿意打开和使用；MCP 让这份记忆还能被其他 AI 使用。

可以直接借鉴有需求、体验好的成熟做法。是否有人已经做过，不是排除功能的理由；是否帮助用户更轻松地记录、找回和继续思考，才是取舍依据。AI-native 的重点是 AI 承担了哪些工作，不要求取消笔记、卡片或列表，也不要求所有操作都通过聊天完成。

## 2. 目标用户与问题

第一批用户：经常记录工作或生活想法、希望以后找回并继续思考的 Mac 用户。其中包括使用一个或多个外部 AI Agent 的人，但连接 Agent 不是使用前提。

他们遇到的问题：

1. 想法出现时只来得及写半句话，过后难以恢复当时的背景；
2. 内容存了很多，却不知道该翻哪一条、问什么，相关想法难以接起来；
3. 曾经的判断会变化，但旧记录和当前理解混在一起；
4. 与 AI 讨论有了收获，真正值得留下的结论却没有进入长期记忆；
5. 换一个 Agent 或新开会话后，要重新解释目标和历史决定；
6. 自动整理可能丢失来源、改坏原意，或把试探性讨论误当成用户的确定观点。

## 3. 产品目标

让用户以很低的成本保存想法，在需要时通过浏览或统一的 AI 检索提问入口找回相关记忆，结合这些背景继续思考，并由 Agent 及时保存表达中的新想法、条件和决定。

首要价值是用户在 Memivy 内完成这条闭环；跨 Agent 使用扩展同一份记忆的价值。产品不以功能独有、页面新奇或接入 Agent 数量代替实际使用价值。

## 4. 核心产品闭环

> **表达先作为会话原文落盘 → 主动联系全局记忆 → Agent 保存有价值的新表达并回执、自然回答问题 → 点击建议或直接继续讨论 → 下次继续使用。**

以下描述当前行为合同；统一输入、快捷键和窗口交接按第二 Memory Agent 计划执行，沿用现有视觉语言。

### 4.1 记一下：轻量捕捉

- 快捷窗口支持输入一句话或粘贴链接，不要求先填标题、选文件夹或打标签；
- 主窗口和记忆库同样可以创建内容、补充想法和编辑当前记忆；
- 默认只附带当前前台应用名称；网页地址和文件路径只有用户明确选择后才附带；
- 外部 Agent 只保存用户明确要求记住的内容；
- 统一聊天先逐字保存会话表达，再由 Agent 维护记忆；独立显式 capture（如 MCP）仍在一个事务中保存立即可用的 Memory 与输入归档；模型失败不丢原话；
- 不让用户先选记录／提问模式。纯记录给简短回执，纯问题回答但不把问题存成事实，混合表达同时记与答。

### 4.2 AI 整理、回执与维护

Agent 在讨论中按需新建或更新记忆，保留用户原话、来源和版本。独立显式捕捉已先创建可用 Memory，其后维护复用相同工具和校验：

- **保留独立记忆**：整理同一个 Memory ID 的初始正文，不重复创建记录；
- **归并到已有记忆**：原子更新目标，将输入 Memory 标为已归并，并登记可撤销的双方变更；
- **暂不判断归属**：保留当前 Memory，不猜测归属。用户有手工编辑或草稿时停止自动处理，转人工核对。

系统显示简短动作回执，例如：

> 已归入「首次体验原则」 · 输入归档已保留　[查看变化] [撤销整理]

回执说明实际做了什么、落到哪里，并提供纠正入口；不能用一段总结替代。普通捕捉不要求用户阅读长分析或完成问答。确需补充信息时可以轻量追问，用户可以跳过；呈现方式待研究。

多个原话片段可以汇入同一条记忆。AI 整理的是“当前记忆版本”：补充或更新时创建新版本，旧版本和来源仍可核对。不能重写、合并或删除原始输入，也不能把用户尚未确定的想法改成已确认事实。

2026-09-07 阶段 5 决策确认：自动续接以补充和局部更新为主，保留未涉及的正文，不自动做整篇压缩重写。用户明确改变判断时可以更新当前理解；犹豫、假设和质疑保留其不确定性，不能当成已经确认的立场变化。

模型不可用时只暂停整理或标明失败；Memory 浏览、编辑与字面检索继续可用。

内部维护共用 `write_memory` 工具及核心事务；无须写入时自然结束，不保留强制终结协议。新建标题由 AI 提供且不能为空。聊天写入不再进入后台整理器处理第二次。

#### 手动整理正文（2026-09-09 已授权）

记忆详情右上角提供「整理正文」，处理当前这一篇的完整正文；已有未保存的编辑草稿时，以草稿为输入。默认只改善结构、标点与表达，保留原意、事实、日期、数字、不确定性、代码、链接、表格及任务状态，不自动总结或补写事实。标题沿用当前标题或用户的编辑草稿标题。

生成期间保留原文并可取消；候选稿在原阅读区域预览，支持查看改动、直接编辑和「再调整」。普通图标栏临时换成再调整、接受并保存、取消。只有接受才将完整审核稿保存，确认后不再调用模型。保留原话、来源和历史，显示可撤销回执及「AI 整理，经我确认」标记。如果输入来自未保存草稿，接受时同时保留整理前的草稿版本，撤销恢复这份整理前内容。取消或离开记忆会丢弃临时预览，原编辑草稿不受影响。

普通工具栏顺序为接着想、整理正文、编辑、分隔线、专题、置顶、更多，使用有可访问名称、悬停及键盘焦点提示的图标按钮；导出、删除保留在更多菜单的文字项中。输入归档只供人工查看与恢复，不提供 AI 整理、讨论、置顶或专题操作。首版不做全库整理、选区整理或额外 MCP 工具。

模型失败、截断或版本/草稿冲突均不得覆盖内容；接受时同时校验正文版本及来源草稿。取消后到达的结果不得进入新记忆。模型输出不再固定限制为 2500 token，默认遵循服务商配置；高级设置可指定上限及其参数名。不同服务商仍有上下文、输出与超时限制，不能声称无限长或自动适配所有模型。

### 4.3 问一问：基于记忆回答

用户可以问：

> “我之前为什么决定让新用户先看结果，再做配置？”

Memivy 在首次模型调用前结合消息、近期讨论及指定材料检索全局当前记忆，Agent 按需补查，并提供可打开的实际证据引用。涉及过去决定、时间变化或冲突时，应区分当时判断与当前理解；没有找到足够证据，就明确说明缺少什么，不编造用户经历或结论。

主窗口顶栏使用同一输入接受文字、语音、问题和想法；明确发送才调用 Agent，键入只保存草稿。专题和用户添加的记忆提供重点，不能排除相关全局记忆。短材料带全文，长材料带有稳定 ID 和读取位置的片段；专题带名称、说明、有界目录及相关成员。保留全局补充预算，目录不能充当正文证据。

### 4.4 接着想：围绕记忆继续讨论

- 从一次回答、一个近期话题或一条具体记忆继续讨论，保留本次对话上下文和相关来源；
- 可以比较旧想法、梳理变化、追问理由、推演下一步；
- 回忆已有内容与 AI 新提出的分析、建议必须区分，不能给推测挂上不支持它的来源；
- 支持中止、失败后重试和之后继续，未完成回答不能伪装成完整结果；
- 会话原文和摘要不整体进入记忆搜索或 MCP。Agent 只将有价值的用户表达整理为记忆；AI 建议、提问和操作指令不能冒充用户事实；
- 以自然 Markdown 流式回答；提问／讨论完成后给 2—3 条续聊建议，点击提交可见原文，保留已有草稿；纯记录不强制建议；
- 长会话压缩较早内容，保留近期完整消息、条件、数字、否定和未决事项，原文可分页回读；纠正和撤销进入后续上下文。

这是围绕个人记忆的思考能力。首版不扩展成独立写作工作台、联网研究助手或替用户执行外部任务的通用 Agent。

2026-09-08 已确认：当前记忆正文下方按需展示最多 3 条本地相关记忆，可打开或选中一起讨论。使用词语及已有同义词关联，无合适结果时不凑数；打开详情不调用模型。选择来源沿用现有讨论上限与引用规则。

### 4.5 自动留下有价值的新表达

- 用户提出想法、说明事实、条件或决定时，Agent 可在本轮工具调用中及时新建／更新记忆，无须逐次确认；“考虑”“决定”“已执行”必须保持区别。
- “这轮别记”只暂停本轮；“接下来先别记”暂停当前会话维护，直到明确恢复。仍保存会话原文并读取全局记忆。
- 只归档本次写入实际依据的用户原话，保留原记录时间、会话／消息、执行者和版本；AI 整理结果不得标成用户逐字审核。归档不创建额外当前记忆或整理任务。
- 一轮多次修改汇总为“已更新 N 条”，可展开差异并整次撤销；失败准确展示已提交的部分。版本或编辑草稿冲突不能覆盖用户内容。
- 删除会话保留已经形成的记忆、独立原话、版本和回执；来源注明原会话不可用，仍可从记忆查看变化并撤销。
- 保留低频“保存文字”：用户可修改文本与去向，新建或追加，复用现有编辑器和核心事务；不再为每条回答生成结论审核／融合链路。

## 5. 产品形态

### 5.1 菜单栏与快捷窗口

- 菜单栏让 Memivy 随时可唤起，并显示服务状态、提供主窗口入口；
- 快捷窗口用统一输入接收记录、提问及混合表达，不先选择模式；
- 捕捉保持低打扰，保存成功与 AI 整理结果可见；提问能够获得回答并继续讨论；
- 2026-09-07 已确认：统一用 ⌘Enter 提交，Enter 换行，输入法组词与按键长按不触发提交；
- 叶子 m 可拖动、隐藏；菜单栏使用单色品牌图标，提供统一输入、打开主窗口、暂停和退出；全局快捷键默认 ⌃⌘M，可修改，注册失败仍保留菜单入口；
- 小窗宽约 500，输入状态高约 310，讨论状态高约 620，并限制在当前显示器工作区；全局新讨论和每个会话分别保留草稿；
- 纯记录的实际记忆写入成功后，普通窗口收起；固定窗口保留并清空输入。仅原话落盘或处理失败不能显示记忆更新成功，可原输入重试；Esc、主动收起或非固定时失焦先保存草稿；
- 收起不会取消回答，完成后不自动弹窗；“停止”才取消当前回答。展开主窗口沿用同一话题、引用、上下文和草稿，主窗口准备好后再收起小窗；
- 唤起时只读取前台应用名称，可移除；用户主动附带的链接或文件路径和文字一起保存在草稿中。收起时仅在不会抢走第三个应用焦点的情况下返回原应用；
- 关闭主窗口先保存草稿再隐藏；⌘Q 或菜单“退出”等待各窗口确认草稿保存。有未完成的确认弹层或草稿失败时保留窗口；
- 登录时启动默认关闭，按 macOS 实际登录项状态呈现；暂停快捷入口只停用快捷键与叶子，本地记录与菜单入口仍可使用。

关闭主窗口后继续菜单栏常驻。彻底退出应用后，已开启的 MCP 仍可保存原话，并返回“已保存，等待应用运行后整理”；下次启动应用再处理。关闭 MCP 总开关则拒绝全部数据工具调用；协议发现与不读取记忆的本地检查仍可用。

随时待命不表示默认监控屏幕、麦克风或键盘。

### 5.2 统一的记忆工作区

主窗口保留紧凑列表、宽阅读区、白黄品牌及左侧记忆／最近讨论／回收站／设置。顶栏与续聊框、快捷窗复用统一输入。

- ⌘K 展开并聚焦顶栏；发送按钮或 ⌘Enter 提交，Enter 换行；中文组词和长按不误提交。Esc 收起并保留草稿。
- 全局讨论可添加／移除指定记忆，后续每轮可调整；移除只停止优先带入，不抹去既有讨论。
- 当前会话继续当前会话；快捷入口恢复最近快捷会话，显式“新讨论”才新建，不按闲置时间切断。
- 主窗和快捷窗交接保持会话 ID、草稿与来源。录音停止只结束转写，不自动发送；显式提交等待尾段完成。
- 未配置模型先保存原话并明确失败／可重试；本地浏览、编辑、关键词搜索和原有 MCP 不受影响。空库不制造示例记忆。

### 5.3 可浏览、编辑的记忆库

保留记忆列表、来源／项目／日期筛选、当前内容、原始输入、来源和版本历史；找回内容统一使用顶栏 AI 检索。用户可以直接查看和纠正 AI 的理解，也可以从某条记忆开始讨论。

编辑当前内容会创建新版本，原始输入与可编辑正文分层。2026-09-06 用户确认记忆库采用“紧凑列表＋宽正文”，正文以当前内容、原话与来源、版本历史分区；沿用当前 dev 界面作为阶段 3 视觉基准。正式讨论的进一步交互仍按后续阶段确认。

### 5.3.1 置顶、回顾与专题

- **置顶**：在正文工具区固定／取消固定一条记忆，左栏直接打开。记录正文和版本不因此变化；原始记录的置顶保留它的原话身份，不自动改指向 AI 整理后的另一条记录。
- **回顾**：用户主动打开，每组显示三条较早更新的记忆，支持下一组、阅读和“接着想”；来源、项目和日期筛选继续可用。不新增通知、每日任务或自动生成总结。
- **专题**：创建名称与可选说明；一条记忆可加入多个专题。用户在正文“专题”中主动加入／移出，也可让 AI 根据名称、说明和最多三条已有记忆节选推荐最多八个候选，再逐条确认加入。推荐失败不更改成员；记录时不强制分类。
- **整理后专题推荐（2026-09-08 用户授权）**：AI 整理成功后，在正文标题下以轻量标签推荐最多三个已有且未加入的专题，点击问号展开理由；列表显示本地推荐数量，没有合适专题时不显示。忽略本次建议会按回执持久保存，新输入对应的新回执可重新推荐。用户逐条点击“加入”才保存成员关系，并可当场撤销。推荐失败不影响原话或整理结果，可显式重试；不自动创建专题或强制分类。推荐绑定整理回执及目标版本，过期、撤销、删除或专题已变化时拒绝加入。
- **专题讨论**：专题是讨论重点，首次上下文带名称、说明、有界目录及相关成员，同时补充专题外关联记忆。新建记忆加入当前专题；更新已有记忆保留原成员关系。专题移除保留会话、草稿与记忆，显示专题不可用，停止向它添加成员。资料库的显式专题筛选仍按成员过滤。
- **管理与恢复**：专题支持编辑名称／说明、移除并当场撤销；移除专题保留所有记忆和已有讨论。删除记忆后从置顶、专题和回顾隐藏，恢复后再次可见；永久擦除清理对应关系。置顶与专题元数据随整库备份／恢复一起保留。

### 5.3.2 就地反馈（2026-09-08 用户授权）

普通保存成功使用底部约四秒自动收起的轻提示，不挤动页面，悬停或键盘聚焦时暂停计时。整理中、未完成状态显示在对应列表项及正文标题附近；普通新建成功不常驻，补充已有记忆给出可查看的短提示。整理回执、变化和撤销保留在版本历史，正文不重复展示。专题推荐在正文标题下显示，确认后成为已有专题标签；加入和移除专题的短提示可当场撤销。实际保存失败留在输入处并保留草稿，备份／恢复等需要核对的错误继续保留明确提示。

### 5.4 视觉与交互状态

继续使用官方 Miro 视觉规范与选定的叶子 m 标志。阶段 3 已获明确开发授权：首页沿用当前 dev 源码，记忆库采用用户选择的紧凑列表与宽正文。Demo v0.1 保留，不把旧 Demo 当作新问答、讨论或结论保存流程的验收基准。实际实现与验证状态见 DEVELOPMENT_PLAN.md。

## 6. 外部 Agent 与 MCP

外部 Agent 是同一份记忆的输入和使用入口。它扩展产品价值，但 Memivy 自身的记录、问答和思考闭环不依赖它。

MVP 只提供两个能力：

| 能力 | 行为 |
|---|---|
| `memory_capture` | 按原文保存用户明确要求记住的内容；Agent 名必填，项目与会话来源仅在明确提供时附带 |
| `memory_search` | 使用本地关键词检索返回少量相关记忆，返回当前 Memory 的有界片段与版本引用；本地 Embedding 可选增强，不调用生成模型 |

Agent 只有在用户明确要求保存时才能调用 `memory_capture`；不接收 Agent 自主判断的“可能值得保存”候选。MCP 提供能力，不能保证任意客户端会主动调用。

- 用户手动开启或关闭本机 MCP；
- 搜索结果数量受限，不提供全库读取，也不返回未保存为记忆的本地讨论；
- MCP 不提供修改、移动或删除记忆的能力；
- Agent 写入保留真实提供的 Agent、项目、会话和时间来源，不编造上下文；
- 应用内问答不新增 MCP 工具，也不把本地对话默认分享给外部 Agent。

阶段 6 已确认范围（2026-09-07）：支持标准本机 stdio MCP，不设客户端白名单，不承诺所有客户端自动调用。默认关闭；设置提供开关、配置复制和本地诊断，明确区分开关开启、本机协议检查通过与外部 Agent 实际调用成功。外部 Agent 可能将获得的记忆片段发送给它使用的模型，开启前在设置说明这一边界。

搜索必须给出关键词，默认 5 条、最多 8 条，返回受限片段及实际命中的当前版本引用，不提供分页读取全库。保存先确认 Memory 和输入归档原子落盘，AI 整理由运行中的 Memivy 异步完成；即使应用退出或模型不可用，记录与搜索仍可用。

本轮 macOS 开发测试包仅面向 Apple Silicon、macOS 26 或更新系统。用户暂无 Developer ID 签名条件，本轮为 ad-hoc 签名且未公证的开发包；后续补正式签名与公证，不因此加入旧 Mac、其他平台或自动更新器。

## 7. 数据、检索与信任边界

### 7.1 本地数据与导出

- SQLite `memivy.db` 是唯一事实库，保存原始捕捉、记忆、版本、来源及本地会话；
- 会话与长期记忆分开存储和检索；只有明确的用户表达可作为自动维护依据，保留其想法／事实／决定状态；
- 2026-09-08 编辑体验补充：当前正文和低频手动保存文字支持 Milkdown 所见即所得 Markdown 编辑，不区分正文与源码模式；仅在选中文字时浮出一行直接格式按钮，不使用下拉框；按照用户提供的 Mem 截图完整提供格式功能，并额外增加 H3：H1/H2/H3、加粗、斜体、下划线、删除线、清除格式、行内代码、编号列表、项目列表、待办列表、引用、代码块、链接、表格；保持截图顺序与分组。标题按钮再次点击或清除格式可恢复普通文本；待办支持勾选与回车续写，表格支持单元格编辑和行列调整；不设常驻工具栏，标题、段落、列表、引用和代码沿用统一阅读排版。快捷记录与提问继续用轻量输入框，原话不经过富文本转换，讨论引用按钮仍绑定实际来源版本。
- Markdown 是可读、可迁移的导出格式，不是运行时主存储；用户从单篇记忆的折叠“更多”菜单中按需导出该篇标题和当前正文为一个 `.md` 文件，设置不提供整库导出入口；原话、历史和会话仍保留在本地数据库及完整备份中；
- 通过应用生成一致性备份，或在应用和所有写入进程停止后复制完整数据库；
- 2026-09-10 已授权分阶段加入可选本地 Embedding 与 sqlite-vec；不引入图数据库或跨设备同步。未完成能力不得在 UI 宣称可用。

2026-09-08 已确认备份恢复：设置提供“创建备份”和“从备份恢复”。恢复整个内容库，不合并；先校验并确认，重启时生成恢复前副本再替换。覆盖数据库中的原话、记忆、版本、讨论、草稿及回收站；密钥不进入备份，同机模型配置与 MCP 开关保留。恢复期间协调外部 MCP 访问，失败保留原库或从副本回退。

### 7.2 原始输入、当前理解与讨论分层

- `captures` 保存用户或 Agent 提交的原始内容，以及讨论写入实际依据的用户原话及手动保存文字；AI 永远不能修改原始内容，用户可以主动删除；
- `memories` 表示持续生长的逻辑记忆；`memory_versions` 保存历次当前理解及来源；
- 普通录入立即创建第一版；暂缓或失败保留该 Memory，归并更新目标与输入状态，撤销原子恢复双方；
- 本地会话保留消息角色、上下文、来源引用和处理状态；用户发问、AI 建议、自动记忆维护与手动保存是不同事件；
- 自动写入归档实际依据的用户原话；手动保存文字单独标注出处。删除会话不连带删除已形成的记忆、来源、版本和回执；
- 新回答引用实际读取的正文版本或相关历史／来源，显示当前与历史状态；后来编辑记忆不能悄悄改变过去回答的依据。来源被用户删除时明确标记不可用，不伪造仍可追溯；
- 删除记忆时，该记忆、历史版本和专属原话一起进入回收站，退出正常搜索及 AI 使用；仍被其他有效记忆使用的原话保留。恢复时恢复相应内容；用户单独删除的原话不因恢复记忆而自动恢复；
- 回收站由用户主动恢复或永久删除，首版不自动到期清空。永久删除清除相应正文，引用只保留不可用状态；永久删除原话时也清除关联的已撤销隐藏版本，不影响其他有效记忆或回收站中的记忆。共享来源不因清空另一条记忆而丢失；
- 撤销整理保留输入归档；撤销归并恢复输入 Memory，不把原话重新投入工作流程。纠正归属的撤销同时恢复涉及的两条记忆；任何一边出现后续修改，都先提示重新核对，不能覆盖新内容；
- MVP 不另建关系图谱或自动用户画像系统。

### 7.3 关键词搜索与记忆问答

| 路径 | 行为 | 模型使用 |
|---|---|---|
| 关键词搜索 / `memory_search` | 统一核心搜索检索当前标题、正文、出处元数据与当前版本派生词；可按项目、来源、时间过滤 | 不调用生成模型；可选本地 Embedding |
| 记忆问答 / 继续讨论 | 结合问题和有限会话上下文，检索少量相关记忆，再基于返回证据回答 | 调用已配置模型；可提取或改写检索词 |
| 捕捉后的整理 | 通过统一搜索检索其他当前 Memory，在合法项目范围内决定保留、归并或暂缓 | Memory 落盘后调用模型 |

自然聊天与独立捕捉维护共用有限工具循环，按需搜索、分页阅读、写入或撤销；没有强制三段答案、固定 RAG 或能力不足后的旧路径。普通检索只查当前正文；追溯过去按稳定 ID 读相关历史／原话。单篇手动整理和专题推荐继续保留其已有独立用途。

模型必须通过流式文本和多轮工具测试才能执行 Agent；不支持时明确停止，原话仍保存，不换模型。内部预算、持久化与失败边界见 TECH_STACK 第 7 节。
问答不能把整个记忆库发送给模型，模型也不能绕过 Rust 核心自行读写数据库。候选范围、调用次数和上下文长度需要有上限；具体参数在实现时验证，不预先建设多 Agent 检索系统。

关键词、全文索引与相关性分数是可重建的派生数据。统一搜索使用 FTS5 和可选的本地 Embedding；设置里下载并启用后，所有检索消费者共同增强，不按功能分裂检索行为。仍不承诺任意换一种说法都一定找得到，需保留固定多语言回归与真实漏搜记录。支持自然语言问答不等于已经具备无限制的语义召回。

### 7.4 BYOM 与失败处理

- 2026-09-11 用户认可 `design-demo/settings-models/`，确认 ASR 接口及 Embedding 切换规则后授权实施：设置分为「AI 与模型」「快捷入口」「数据与存储」「外部连接」。AI 页按「问答与整理」「语义检索」「语音输入」管理，每项能力直接填写各自 API 地址、模型名称及 Key，不提供连接选择或独立连接管理；问答、整理、讨论仍共用一个生成模型。
- 语义检索和语音输入分别选择内置本地模型或 OpenAI-compatible 服务；Embedding 使用 `/embeddings`，ASR 首版仅支持 `/audio/transcriptions` 分段 WAV 上传，不做专用厂商协议或实时 WebSocket。兼容本机服务可不填 Key。
- 模型先测试后应用；失败保留当前配置。Embedding 测试校验正文、查询和维度，确认处理范围后重建唯一索引；准备期间使用基础搜索，可暂停、继续、取消、重试。地址、模型、维度或查询指令变化需重建，单纯改连接名称或 Key 不触发重建。
- 启用外部 Embedding 会发送当前记忆片段及搜索查询，统一搜索的消费者（含相关记忆与 MCP 搜索）共用该选择；历史、原话归档、回收站和未保存草稿不参与。界面明确数据流向，不自动切换其他服务。
- 录音开始时固定模型及连接地址，设置修改用于下次录音；失败保留分段暂存供重试；停止后的失败录音，在原地址和原模型下修正 Key 后可以继续重试。停止并完成收尾后才可提交，转写文字可编辑。接口测试使用合成静音，只证明请求可用，不代表识别质量。
- 自动整理可关闭；关闭期间新记录保存原文，重新开启不自动补整理。模型文件统一在「数据与存储」管理，清理共享文件会提示影响，本地功能可重新下载恢复。
- 配置保存在独立本机应用数据文件，使用当前系统用户的文件权限；不接 Keychain，不放入内容 SQLite、导出、备份元数据或日志；
- Memivy 直接调用用户配置的端点，没有云端后台、账号、模型代理或托管密钥；
- 首次配置通过连接测试；未配置或模型不可用时，记录、浏览、编辑和版本历史仍可用，顶栏问答明确显示不可用原因；底层关键词检索和 MCP 保持可用；
- 调用前能知道端点位于本地还是远程，以及会发送当前问题、必要会话上下文和有限相关记忆；本地优先不意味着配置远程模型时内容不会离机；
- 回答支持取消和失败重试；没有证据、不完整输出与请求失败各有明确状态，不制造成功回答或自动写入记忆。

## 8. MVP 范围

### 必须完成

1. 本地捕捉、AI 新建/续接/暂不判断、可见回执、纠正和撤销；
2. 菜单栏与全局快捷窗口，支持“记一下、问一问”；
3. 顶栏统一文字／语音输入与全局记忆关联、最近讨论、可浏览编辑并查看来源和历史的记忆工作区；
4. 基于有限记忆证据的问答、引用查看和继续讨论；
5. 本地会话续接，以及自动记忆维护、版本更新及整次撤销；
6. BYOM 配置、连接测试、取消与失败处理；
7. 不调用模型的 FTS5 关键词搜索；
8. `memory_capture` 与 `memory_search` 两个 MCP 能力及总开关；
9. 单篇 Markdown 导出、一致性备份和全文索引重建；
10. 本机短语音输入（2026-09-11 授权）：记录、提问及快捷入口共用语音转写；停止后可编辑、显式提交，设置管理启用、模型下载/加载/释放、启动预加载与语音快捷键。

### 本版不做

- 待办、提醒、日历、每日摘要和主动系统通知；
- 长录音与会议转写；短语音输入已于 2026-09-11 单独授权，单次最长 5 分钟；
- 全天录屏、录音、键盘监听和全量外部 Agent 对话归档；
- 自动 Wiki、知识图谱、复杂标签、自定义字段、模板和 Supertag 系统；
- 邮件、Slack、日历等连接器；
- 手机端、多设备同步、团队空间或云端托管服务；
- 通用聊天/联网研究助手、独立 AI 写作工作台和自动执行外部任务；
- 自动用户画像、后台定时洞察或无关内容推荐；
- 外部 Agent 未经用户要求自主保存候选、待确认收件箱、细粒度权限矩阵；
- AI 改写或删除原始输入、无人确认的全库批量整理，把问题、AI 建议或推测当成用户既定事实。

## 9. 用户故事

以下是说明产品方向的示例，不预定最终布局与操作步骤。

### 故事一：先记下一个还没想清楚的念头

小林看到一个产品网站，想到：“第一次打开时，也许应该先看到做好的结果，再去配置。”他唤起 Memivy 记下这句话，不填标题或目录；只有主动附带网页时才保存 URL。

原话落盘后，AI 将它续到「首次体验原则」，并给出目标和撤销入口。判断错了可以改为新记忆；模型失败也不影响原话保存。

### 故事二：找回以前的判断

过了几天，小林问：“我当时为什么想先给结果？”Memivy 根据「首次体验原则」及相关原话回答，引用当时的记录。若后来想法变了，就区分旧判断和新理解。小林可以打开来源核对，也能不用 AI，直接搜“先给结果”。

### 故事三：结合过去，接着想

小林继续问：“如果配置是必需的，首次体验还能怎么做？”Memivy 带着刚才的背景讨论可能方案，明确哪些来自既有记忆，哪些是新建议。小林在这里提出的假设不会自动变成自己的长期观点。

### 故事四：把确定的结论留下

讨论后，小林说：“那就先用示例结果解释价值，再按需配置。”Memivy 自动将这个决定续到「首次体验原则」，保留原话和来源，显示实际修改与撤销入口。小林不用另找保存按钮；下次追问时，能看到新决定及此前的原因。
### 故事五：同一份记忆也能交给其他 AI 使用

小林使用外部 Agent 设计首次体验时，可通过 `memory_search` 取回少量相关记忆和来源，无需重述全部背景。外部讨论形成新结论后，只有他明确要求“记住这个”，Agent 才通过 `memory_capture` 保存。未确认保存的 Memivy 本地对话不对外提供。

## 10. 验收标准

本轮完成条件以[计划 A01—A17](docs/goals/second-memory-agent-plan-goal.md#5-验收全部满足才算完成)为准，必须全部通过；以下概览不替代真实模型、故障和原生验收。

| 能力 | 验收条件 |
|---|---|
| 捕捉 | 快捷键到可输入、提交到原话事务完成分别不超过 1 秒；排除打字与模型时间，AI 不阻塞保存 |
| 意图区分 | 同一输入处理纯记录、纯问题和混合表达；不把问题或 AI 建议存成事实 |
| 来源采集 | 默认只附带前台应用名；未经主动选择不保存网页 URL 或文件路径 |
| AI 整理 | 返回新建、续接、暂不判断之一；回执说明状态、目标与纠正方式，模型失败保留原话 |
| 来源与版本 | 每个记忆版本可追溯到形成它的原始输入；AI 不修改原始输入，编辑和更新保留历史 |
| 记忆工作区 | 顶栏统一输入、最近讨论和回收站；可浏览编辑；跨窗口保持草稿和所选记忆；空库不伪造内容 |
| 底层关键词检索与 MCP | FTS5 返回匹配内容，不产生模型请求；未配置模型仍可用；主窗口统一 AI 检索按第 4.3 节执行 |
| 记忆问答 | 回忆性陈述有可核对的引用，区分历史与当前理解；证据不足时不编造 |
| 继续讨论 | 保留有限会话上下文与来源，区分建议和既有记忆；支持取消、失败重试和之后继续 |
| 自动记忆维护 | 想法、事实和决定保持原状态与来源；实际写入才发回执；同输入重试不重复写入，整次撤销原子生效 |
| 引用生命周期 | 回答引用固定来源版本；来源删除后标记不可用，不把新版本冒充旧依据；已保存结论不依赖会话仍存在 |
| 跨 Agent | MCP 默认关闭，可手动开启；只有明确保存意图才写入；搜索有数量与来源边界，不暴露未保存的讨论 |
| 服务与 BYOM | 主窗口关闭仍常驻；彻底退出后的 MCP 行为符合第 5 节；模型配置与数据分离，失败不阻塞记录和关键词搜索 |
| 数据所有权 | 单篇记忆可按需导出标题与当前正文；完整备份保留原始输入、版本、来源及独立会话，可恢复并重建全文索引 |

## 11. MVP 成功标准

核心观察是：**用户留下的想法，后来是否被找回，并帮助他继续思考、作出判断或完成工作。**

- 首次记录后，用户能否理解保存结果并立即找回或围绕它提问；
- 隔一段时间再来时，是否成功找回需要的内容；
- 讨论是否有帮助，是否自然形成并准确留下用户的新想法和决定；
- 被保存的结论，后来是否再次被使用；
- 每周主动记录、找回和继续讨论的天数；
- 错误落位、错误引用、无结果、过期理解和撤销的原因；
- 对使用外部 Agent 的用户，是否减少重复解释背景。

“有效续接率”可用于观察被记录的想法在之后被采用并推动进一步思考或工作的比例，既包括 Memivy 内部使用，也包括外部 Agent 使用。不能以捕捉总量、聊天轮数或 AI 生成字数代替有效使用。

首轮可邀请 10–15 名有记录与回顾习惯的 Mac 用户连续试用四周，兼顾使用和不使用外部 Agent 的人。这是产品完成后的观察安排，不新增开发前审批门槛，也不把新颖性当作需求成立的条件。

## 12. 已确认方向与待研究交互

2026-09-06 后续授权：用户选择直接改造阶段 1 原生样机来体验新交互。当前候选是“常驻叶子 m → 原地展开输入 → 面板内回答 → 按需展开主窗口”，支持固定输入、拖动、隐藏与快捷键。首页提供近期话题续接，讨论区区分原话依据、新建议和待确认结论，来源按需展开。阶段 1 第二版只验证新记录式结论保存与撤销，其中正式编辑与版本维护现已进入获授权的阶段 3；正式 AI 自动整理仍属后续范围。这些是已授权的实验内容，尚未作为正式产品交互验收；具体记录见 DEVELOPMENT_PLAN.md。

### 已确认

- 以“记一下、问一问、接着想”为核心，首页围绕近期话题使用记忆，保留可浏览和编辑的记忆库；
- AI 主动联系全局记忆并维护用户表达，保留想法／事实／决定状态；回答有实际来源；
- 原始输入先保存，AI 修改可查、可纠正、可撤销；
- 字面检索不依赖模型；可选本地 Embedding 在统一核心增强搜索，记忆问答与讨论使用用户配置的生成模型；
- 本地优先、macOS MVP、按能力选择 BYOM 模型、SQLite 主库、独立本机配置及现有两项 MCP 能力；
- 可以借鉴成熟产品做法，以用户喜欢和实际使用为判断依据；
- 2026-09-06 本次只同步相关文档，Demo 和应用代码保持现状，阶段 1 已通过的记录保留。

### 后续重点研究，当前不冻结

阶段 4 的快捷入口交互已于 2026-09-07 经研究、决策与授权确定，见 5.1；沿用当前 dev 视觉基准，不重新审批旧 HTML Demo。下列未完成的产品扩展仍按各自阶段研究：

1. 主窗口入口组织已按第 5.2 节获得开发授权，置顶、回顾与专题也已获一次性实现授权；实际交互仍待用户体验验收；
2. 来源如何展开核对，历史变化与新建议如何清楚表达；
3. 自动维护、手动保存文字、回执与整次撤销按第二 Memory Agent 计划实施并验收；
4. 处理中、取消、无证据、模型失败、重试及未配置状态如何呈现；
5. 续聊建议按第 4.4 节及 Agent 计划验收；详情下方的本地相关记忆沿用现有功能。

先研究这些交互，再按后续授权调整 Demo、记录确认版本。本文不预先指定聊天占满首页、分栏数量、按钮位置或固定弹窗流程。
