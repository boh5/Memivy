# Memivy MVP 产品需求文档

状态：核心交互与数据路线已确认
研究及更新日期：2026-09-04

视觉规范：[DESIGN.md](DESIGN.md)（已选定 Miro；仅约束视觉，不改变本文功能范围）

## 1. 产品定义

> **Memivy 是一个 AI-native 备忘录：你只管写下想法，或让正在使用的 Agent 把结论发过来；Memory Agent 当场判断它是一条新记忆，还是已有想法的继续，并让你和不同 AI 在以后接着使用。**

它不是普通笔记软件加 AI 按钮，也不是独立聊天助手。用户不是先创建文件、起标题、选目录，再让 AI 整理；而是直接告诉 Memivy 一件希望未来的自己和 AI 记得的事。原话先安全保存，Memory Agent 随即执行一次可见、可撤销的“记忆动作”。

MVP 只做记忆闭环，不做待办、提醒、日历和通用 Agent 管理。

MVP 的核心承诺只有三条：

1. 用户负责表达，AI 负责判断这段内容在记忆里应该落到哪里；
2. AI 每次采取记忆动作都必须当场回执，不能只在后台悄悄处理；
3. 无论 AI 判断是否正确，用户原话都不会丢失，并且可以一键纠正。

## 2. 目标用户与问题

第一批用户：经常记录工作或生活想法，并同时使用一个或多个 AI Agent 的 Mac 用户。

他们遇到的问题：

1. 想法出现时只来得及写半句话，过后难以恢复当时的背景；
2. 相关内容散落在笔记、网页和多个 Agent 会话中；
3. 换一个 Agent 或新开会话后，又要重复解释自己的目标、判断和历史决定；
4. AI 生成了大量内容，但真正值得留下的结论没有成为自己的长期记忆；
5. 自动整理容易制造噪音、丢失来源或改坏原意。

## 3. 产品目标

让一条来自用户或 Agent 的重要想法能够立即保存，在不篡改原话的前提下被 AI 连接和更新，并在用户或另一个 Agent 需要时准确取回、继续使用。

## 4. 核心产品闭环

> **表达想法 → 原话落盘 → AI 当场落位并回执 → 浏览、搜索或供 Agent 使用 → 新结果再沉淀**

### 4.1 捕捉

- 全局快捷键输入一句话或粘贴链接，回车立即保存；
- 笔记编辑器中正常记录和修改；
- 默认附带当前前台应用名称；网页地址和文件路径只有在用户明确选择后才附带；
- 外部 Agent 只能保存用户明确要求“记住”的结论；
- 原话必须先在本地落盘，AI 处理不能阻塞保存；
- 保存后，Memory Agent 只做三种动作：建立新记忆、续到已有记忆、暂不判断归属。

### 4.2 AI 落位、回执与维护

每次捕捉后，Memory Agent 先判断这句话在个人记忆里应当落到哪里：

- **建立新记忆**：这是一个此前没有出现过的想法；
- **续到已有记忆**：这是对某个旧想法的补充、更新或质疑；
- **暂不判断归属**：信息太少，但原话已经保存，用户不必当场整理。

系统随后显示一条短回执，例如：

> 已续到「首次体验原则」 · 保留原话和本页来源　[撤销] [另存为新记忆]

回执不是 AI 总结，而是把 AI 的理解和动作暴露给用户。只有当缺少的一个信息会明显影响以后找回时，系统才允许追问一句；用户可以不回答。

落位后，Memory Agent 在后台生成检索线索、寻找更新或冲突，并保留来源、时间和处理记录。一次捕捉不必等于一篇新笔记：多个原话片段可以生长在同一张记忆页中。AI 整理的是“记忆当前版本”；补充、更新或质疑已有想法时，系统创建新版本并保留旧版本，不能重写、合并或删除用户原话。

AI 只在原话落盘后开始处理。搜索不调用 AI；调用失败时，捕捉进入“等待理解”，记录与关键词搜索继续可用。

### 4.3 召回

用户可以用关键词查找，例如：

> “我之前是不是想过一个让新用户先看真实项目的方案？”

Memivy 使用全文检索，并结合项目、来源、时间和 AI 在保存时生成的检索词返回少量结果。结果默认显示当前记忆；用户打开详情后可以查看原话、来源和历史版本。

外部 Agent 搜索记忆时，Memivy 只返回少量相关内容，并附带原话、来源和时间，不加载整个记忆库。

## 5. 产品形态

### 菜单栏常驻服务

- 提供全局快捷捕捉；
- 接收本机 MCP 请求；
- 在后台完成 AI 落位和全文索引；
- 显示“可用”或“已暂停”，并可打开主窗口。

stand by 表示随时可以接住输入和续接上下文，不表示默认监控屏幕、麦克风或键盘。

### 捕捉框与 AI 回执

这是 MVP 最重要的 AI 界面：

1. 用户输入原话后，捕捉框立即消失；
2. 一个小回执显示“已建立”“已续到”或“已记下，暂未归类”；
3. 回执展示目标记忆和来源，并提供“撤销”与“改为新记忆”；
4. 回执几秒后自动收起，用户不需要进入收件箱处理。

如果 AI 不可用，原话仍然保存，回执只显示“已记下，等待理解”。

### 桌面备忘录

只保留三个主要区域：

1. 笔记列表与搜索；
2. AI 整理出的“当前记忆版本”；
3. 按时间排列、不可被 AI 修改的原话与来源记录。

用户可以直接编辑当前记忆版本；每次保存都会创建新版本，上一版仍可回看。原话不混入可编辑正文，也不会因为用户修改当前版本而改变。

不设置独立的聊天首页、知识图谱、任务页或复杂收件箱。

## 6. 外部 Agent 与 MCP

外部 Agent 是记忆的输入和使用入口，但不能自主整理或删除用户记忆。

MVP 只提供两个能力：

| 能力 | 行为 |
|---|---|
| `memory_capture` | 保存用户明确要求记住的内容，并附带 Agent、项目和会话来源 |
| `memory_search` | 搜索少量相关记忆，同时返回当前内容和必要的原话引用 |

Agent 只有在用户明确说“记住这个”时才能调用 `memory_capture`；Memivy 不接收 Agent 自主判断的“可能值得保存”候选。MCP 只提供能力，不能保证任意客户端会主动调用。

MVP 权限只保留最小边界：

- 用户手动开启或关闭本机 MCP；
- 搜索结果数量受限，不提供读取全部记忆的接口；
- MCP 不提供修改、移动或删除记忆的能力；
- Agent 写入的内容保留 Agent、项目、会话和时间来源。

## 7. 数据、检索与信任边界

### 7.1 一个本地事实库

- 一个本地 SQLite 数据库 `memivy.db` 是唯一事实来源，保存原始捕捉、记忆、当前内容、必要历史和来源；
- Markdown 不是运行时主存储，而是完整、可读、可迁移的导出格式；
- 数据库文件可以直接复制备份；停止使用 Memivy 时，用户可以完整导出 Markdown；
- MVP 不引入 embedding、向量数据库或图数据库。

竞品采用了两类路线：Screenpipe 用本地 SQLite 与 FTS5 搜索电脑活动；Reor、Hindsight 等产品增加向量检索以处理语义召回，但也增加模型、索引和运行时复杂度。Memivy 首版先采用一个 SQLite 主库与可重建的全文索引，验证记忆闭环后再根据真实漏搜情况决定是否增加向量。[Screenpipe](https://github.com/screenpipe/screenpipe/blob/main/README.md) · [Reor](https://github.com/reorproject/reor) · [Hindsight](https://hindsight.vectorize.io/developer/storage)

### 7.2 原话与 AI 记忆分层

- `captures` 保存用户或 Agent 提交的原话。AI 永远不能修改原话，只有用户可以主动删除；
- `memories` 表示一条持续生长的逻辑记忆，例如「首次体验原则」；
- `memory_versions` 保存 AI 整理后的历次版本，并记录它由哪些原话形成；
- 建立新记忆时创建第一版；续到已有记忆时创建新版本；暂不判断时只保存原话；
- 补充内容与旧内容可以同时有效；更新或冲突内容会产生新的当前版本，旧版本保留为历史；
- MVP 不建立单独的关系模型；记忆版本直接引用形成它的原话即可。

这一设计结合了两种竞品思路：Mem0 的新版采用追加式记忆以避免删除历史；Supermemory 更新记忆时创建新版本，并用 `isLatest` 区分当前与历史。Memivy 进一步把用户原话作为不可由 AI 改写的证据层。[Mem0](https://docs.mem0.ai/migration/platform-v2-to-v3) · [Supermemory](https://supermemory.ai/docs/api-reference/content-management/update-a-memory-creates-new-version)

### 7.3 MVP 检索策略

- SQLite FTS5 索引标题、当前记忆版本、用户原话、网址和 AI 生成的检索词；
- Memory Agent 整理记忆时，在同一次输出中生成少量同义词和主题词，不额外调用模型；
- 搜索直接执行全文检索，不再调用 AI；
- 时间、项目和来源作为可选过滤条件；
- 捕捉后的自动落位只比较全文命中的候选、当前项目记忆和少量最近记忆；信心不足时选择“暂不判断”，不强行续写；
- 全文索引、检索词和相关性分数都是派生数据，可以从原话与记忆版本重建。

MVP 不创建 `memory_embeddings`，也不下载或调用 embedding 模型。稳定的记忆及版本 ID 已足够让后续增加向量索引，无需迁移原始内容。只有真实使用中反复出现“换一种说法就找不到”时，才评估加入 embedding。

### 7.4 BYOM 与 AI 使用

- MVP 不内置或代售模型，用户自行提供模型；
- 首版只支持一套 OpenAI-compatible 配置：API Base URL、模型 ID 和 API Key；本地兼容端点可以不填密钥；
- 同一时间只启用一个模型，不提供按功能选择模型或供应商预设矩阵；
- API Key 与模型配置一起由 Memivy 本机常驻服务保存在应用数据目录，依赖当前系统用户的文件权限；MVP 不接入 macOS Keychain；
- API Key 不进入日志和内容导出；Memivy 本机常驻服务直接调用用户配置的模型端点；
- MVP 不建设 Memivy 云端后台，不提供账号、云同步、模型代理或托管密钥服务；
- 首次配置必须通过连接测试；未配置模型时，记录、编辑、版本历史和关键词搜索仍然可用，AI 功能显示“等待配置”；
- 界面说明当前端点位于本地还是远程，发送到远程模型的内容范围在调用前可知；
- 证据不足时不假装记得；所有回答都能回到原话，整理、更新和召回结果均保留处理记录并可撤销。

## 8. MVP 范围

### 必须完成

1. 本地记忆的创建、编辑、删除和列表，以及原话与版本历史查看；
2. 菜单栏与全局文字捕捉，原话即时落盘；
3. 本地常驻 Memory Agent、BYOM 配置和连接测试；
4. “新建、续写、暂不判断”三种记忆动作及可撤销 AI 回执；
5. 基于 FTS5 和保存时生成的检索词进行搜索；
6. `memory_capture` 与 `memory_search` 两个 MCP 能力及一个总开关；
7. 完整 Markdown 导出与全文索引重建。

### 明确不做

- 待办、提醒、日历和每日摘要；
- 主动系统通知；
- 语音转写和长录音；
- 全天录屏、录音、键盘监听和全量 Agent 对话归档；
- 自动生成 Wiki、知识图谱界面和复杂标签体系；
- Tana 式自定义字段、模板和 Supertag 系统；
- 邮件、Slack、日历等连接器；
- 手机端、多设备同步和团队空间；
- 通用聊天助手；
- “继续想”、AI 写作草稿和主动关系提示；
- Agent 自主保存候选、待确认收件箱和按客户端、项目、笔记划分的权限矩阵；
- AI 或外部 Agent 改写、合并或删除用户原话，以及无人确认的全库批量整理。

## 9. 用户故事

下面用同一个用户“小林”的真实工作过程解释产品，而不是单独描述功能。

### 故事一：两秒钟记下一个还没想清楚的念头

小林正在浏览一个产品网站，突然想到：

> “AI 产品第一次打开时，也许不该先让用户配置，而应该先让他看到一个做好的结果。”

1. 他按下全局快捷键，屏幕中央出现一个很小的输入框；
2. 他输入这句话，按回车，输入框立即消失；
3. 输入框默认显示当前应用名称；他主动点击“附带当前网页”后，网页地址才会随原话保存；
4. Memivy 先原样保存这句话，不要求标题、文件夹或标签；
5. Memory Agent 发现小林上个月已经有一张「首次体验原则」，于是没有制造第二篇孤立笔记，而是把这句原话作为新的来源片段续到那张记忆页；
6. 屏幕角落出现短回执：

> 已续到「首次体验原则」 · 保留原话和本页来源　[撤销] [另存为新记忆]

7. 小林不用处理；如果 AI 判断错了，他点“另存为新记忆”即可纠正。

所以这个场景下**不一定生成新笔记**。每次都先生成一条不会丢失的原始捕捉记录；AI 决定它是新记忆还是旧记忆的继续，并把决定当场告诉用户。若没有足够相关的旧内容，回执会显示“已建立新记忆「先给结果，再做配置」”。

### 故事二：打开一条持续生长的记忆

两天后，小林重新打开「首次体验原则」。页面顶部是 AI 根据两次输入整理出的当前内容；下面按时间显示两段原话和来源。

小林可以直接修改当前内容，保存后仍能回看上一版。无论他怎样编辑，下面的两段原话都不会被 AI 改写。

### 故事三：Codex 做出的关键结论，进入同一份记忆

几天后，小林让 Codex 研究竞品。研究结束时，Codex 得出：

> “这个品类的主要摩擦不是用户不会整理，而是想法尚未成熟时就被迫分类。”

小林对 Codex 说：“把这条结论记住。”

1. Codex 通过 `memory_capture` 把结论发送给 Memivy；
2. 因为这是用户明确要求保存，Memivy 直接保存原文，并判断应续到「首次体验原则」还是建立新记忆；
3. AI 回执显示实际落位；记忆页保存来源为 Codex、项目、会话链接和写入时间；
4. 新内容以来源片段进入记忆页，不修改此前任何原文。

这一步解决的是：AI 对话中真正有价值的结论，不再随着会话结束而消失。

### 故事四：换了一个 Agent，也不用重新解释背景

一周后，小林改用 Claude Code 设计这个产品的首次使用流程。

1. Claude Code 调用 `memory_search` 搜索与首次使用流程相关的记忆；
2. Memivy 根据当前项目和目标，只返回三条记忆，而不是整个笔记库；
3. 结果包括当前内容以及必要的原话、来源和时间；
4. Claude Code 根据这些内容开始工作，小林不需要再次复述此前的研究。

这就是“跨 Agent 外置大脑”的核心体验：不是让不同 Agent 共享全部聊天，而是共享用户已经沉淀的可靠记忆。

### 故事五：只记得几个关键词，也能找到当时的自己

三个月后，小林只记得“先给结果、后做配置”几个词，想不起原话和项目。

他在 Memivy 中输入：

> “先给结果 配置”

1. FTS5 在标题、当前记忆、原话和保存时生成的检索词中找到相关结果；
2. 结果默认显示当前记忆内容；
3. 结果同时显示网页、手写笔记或 Agent 会话来源；
4. 如果一条旧判断后来被另一条内容质疑，结果会明确显示“可能已经变化”；
5. 小林可以直接打开原始笔记核对，而不是只能相信 AI 的总结。

Memivy 只在用户再次需要时，帮他恢复当时的思想上下文。

MVP 不承诺在完全没有共同词语或检索词的情况下仍能找到语义相近内容；这是后续是否引入 embedding 要验证的问题。

## 10. 验收标准

| 能力 | 验收条件 |
|---|---|
| 捕捉 | 任意应用中按快捷键可在 1 秒内让原话本地落盘，AI 处理不阻塞 |
| 捕捉来源 | 默认只附带前台应用名称；网页地址和文件路径未经用户明确选择不得保存 |
| AI 落位 | 每次捕捉都返回“新建、续写、暂不判断”之一，并明确目标记忆 |
| AI 回执 | 用户无需打开主窗口即可看见 AI 做了什么，并能撤销或改成新记忆 |
| 原话保护 | AI 和 Agent 均不能修改用户原话；每个记忆版本都可追溯到来源捕捉 |
| 记忆维护 | 补充或更新已有记忆时创建新版本、保留旧版本，并允许用户纠正 |
| 记忆页 | 顶部展示可编辑的当前版本，下面展示原话来源时间线；编辑当前版本会创建新版本 |
| 召回 | 搜索不调用 AI；FTS5 可从标题、当前内容、原话和保存时生成的检索词中返回少量结果 |
| 跨 Agent | 开启本机 MCP 后，Agent 可搜索记忆；只有用户明确要求时才能写入，并保留来源 |
| stand by | 本机服务显示“可用”或“已暂停”，暂停后不接受 MCP 请求 |
| BYOM | 用户可配置并测试一个 OpenAI-compatible 模型端点；密钥由本机常驻服务保存且不进入日志或内容导出，模型不可用不影响记录和关键词搜索 |
| 数据所有权 | 内容可完整导出为 Markdown；全文索引删除后可以重建 |

## 11. MVP 成功标准

核心指标：

> **有效续接率：被记录的想法中，后来在另一个时间或另一个 Agent 中被正确召回，并推动一次继续编辑、判断或工作的比例。**

辅助观察：

- 用户每周主动捕捉的天数；
- 搜索结果被采用、展开原文或判定无用的比例；
- 用户减少重复向 Agent 解释背景的次数；
- AI 落位被撤销或改目标的比例，以及“暂不判断”长期堆积比例；
- 重复记忆、错误召回和过期记忆的比例。

首轮建议邀请 10–15 名重度使用 AI Agent 的 Mac 用户，连续试用四周。若用户只不断存入内容，却不发生跨时间或跨 Agent 的有效续接，产品逻辑就没有成立。

## 12. 当前产品决策

1. 每次捕捉都先保存原话，再由 AI 决定新建、续写或暂不判断；这项核心交互已确认；
2. Memory Agent 是核心，外部 Agent 是一等参与者，不再称为“额外入口”；
3. 记忆页顶部是可编辑的当前版本，下面是不可被 AI 修改的原话来源时间线；用户编辑当前版本会创建新版本；
4. AI 只在捕捉落盘后运行，搜索不调用 AI；
5. MCP 只提供 `memory_capture` 和 `memory_search`，使用一个总开关，不建设细粒度权限系统；
6. MVP 采用 BYOM，只配置一个 OpenAI-compatible 模型端点；密钥由本机常驻服务保存，不接 Keychain，也不存在 Memivy 云端中转；
7. 快捷捕捉默认只附带前台应用名称，网页地址和文件路径必须由用户明确选择；
8. 只保存有意图、有来源的内容，不自动归档所有电脑活动和 Agent 对话；
9. SQLite 是唯一事实数据库，Markdown 只用于导出；
10. MVP 不使用 embedding；先以 FTS5 和保存时生成的检索词完成召回，后续只在真实漏搜证据充分时增加向量；
11. 当前 MVP 只验证记忆的捕捉、理解、召回和续接，不引入任务系统；
12. 当前只支持 macOS；Windows、iOS、Android 暂缓，Linux 不支持，均不进入本轮设计、开发、测试与发布范围。
